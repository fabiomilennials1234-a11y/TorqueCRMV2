node_modules
dist
dist-ssr
*.local
.DS_Store

# Editor
.vscode/*
!.vscode/extensions.json
.idea

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Env — ignore by default, but ship the committed dev/example files.
# .env.development is safe to ship (VITE_DEV_AUTH=1 is the whole point of
# dev mode). Secrets live in .env / .env.local which stay ignored.
.env
.env.*
!.env.example
!.env.development

# Test
coverage
.vite
