# STATE.md — Torque CRM v2

> Last updated: 2026-04-26 (S55+S56+S63+S67 ENTREGUES — Docker dev, Responsive Onda 1, Backend deferreds h/k/d, Infra CI/CD pins + prod compose completo + .env.production.example)

## Current Phase
**Sprint S53 Fase H Production Gate Verification — Onda 1+2+3 ENTREGUES (2026-04-24).** Auditoria pré-prod (D073) abriu gate §10 com 1 done / 1 partial / 4 pending / 2 unknown e descobriu 3 bugs duros (FK 0022, build busSub, workflow handlers stubs 7/7). Conductor despachou ~10 agentes paralelos em 3 ondas pra destravar runtime crítico. **Onda 1**: (A) workflow engine real — 7/7 action handlers com side-effects (SendMessage+messaging+inbox, UpdateLead+bus, Wait com ErrSuspend/next_retry_at, CreateTask+assignee, CallAgent+sessions+killswitch, HTTPRequest+SSRF guard RFC1918+loopback+link-local+metadata+1MiB cap+no-redirect, Branch puro), retry `[15s/1m/5m/30m/2h]` + DLQ workflow_run_failures, watchdog ReclaimOrphanedRuns 10min, migration 0028, 22+ tests; (B) AI safety stack — migration 0031 ai_tokens/tts_seconds em plan_quotas, service/ai/pii/scrub.go BR (CPF/CNPJ/phone/email/credit-card) + 20+ tests, httpx/middleware/ai_quota.go RequireAITokenBudget/RequireTTSBudget admission-only fail-closed master-bypass 402 canônico, service/ai/runtime_registry.go kill-switch CancelAll thread-safe, service/ai/trigger/subscriber.go bus subscriber message.received→AssignAgent via matcher, playground.go com PII scrub user msg + RAG chunks + finalizeStream persiste tokens + ttsPreview consome tts_seconds, agents.go com registry.CancelAll em disable, 43+ tests; (C) DBA cleanup — migration 0029 drop knowledge_chunks.embedding_text + meetings UNIQUE compound org+provider+external (cross-tenant safe), migration 0030 pg_trgm + GIN parcial trigram em leads.name/email/phone + messages.body alinhado com LIKE LOWER do repo; (D) Backup/DR — `.specs/runbooks/backup-restore.md` (176L) + `.specs/runbooks/key-rotation.md` (170L com dual-key procedure pra INTEGRATION_ENCRYPTION_KEY) + scripts bash (pg_dump custom, restore --single-transaction gated production, verify 3 invariantes) + scripts/README + `.github/workflows/backup.yml` cron 03:00 UTC + retention 30d + Sentry webhook + 60min timeout, `.env.example` com rotation refs em 9 chaves. **Onda 2**: (E) quota wiring members (bidirectional headcount delta ±1 idempotente via priorActive snapshot) + workflows (lifetime count) — 10 tests admit/cap402/fail-closed/master-bypass/nil-safe; (F) quota wiring agents (ResourceAgents distinto de ResourceAITokens) com createAgent=+1 + setStatus→disabled=-1 sequenciado DEPOIS de registry.CancelAll preservado da Onda 1 + 6 cenários; (G) impersonation cookie swap atômico — master.go:163-285 reescrito removendo silencers, flow resolve→audit-first→mint JWT→http.SetCookie espelhando auth.setSessionCookie exato (`__torque_session`, HttpOnly+Secure+SameSite=Strict+Path=/), narrow interfaces locais pra unit test sem pgxpool sem mudar cmd/api wiring, 5 tests cobrindo audit-fail-closed/cookie-shape/cookieDomain/ErrNotFound-pré-audit/malformed-UUID. **Onda 3**: (H) api.gen.ts regen via `openapi-typescript` contra arquivo estático yaml — 7 linhas → **4398 linhas**, OpenAPI 0.52.0 100% tipado. S52 deferred fechado. **Validação final**: `tsc --noEmit` verde, `eslint . --max-warnings 0` verde, `vitest run` **296/296 em 86 files** (zero regressão pós-regen). **Migrations**: 4 novas (0028/0029/0030/0031) totalizando **30 migrations**. **Tests novos**: ~80 funcs cross workflow/AI/quota/impersonation. **Bugs D073 fechados**: FK 0022 ✅, build busSub ✅, workflow handlers ✅ (Onda 1 A). **Gate §10 status pós-D074**: dual-review CODEOWNERS ✅ (D073), workflow engine real ✅, AI safety ✅, quota wiring completo ✅, api.gen.ts regen ✅, impersonation cookie ✅, backup/DR runbook+scripts+workflow ✅. Pending exige **provisioning humano**: VPS Hostinger staging + Postgres managed + DNS + Cloudflare + GitHub Environments + sandbox keys reais → rodar pentest runbook 10 checks + k6 baseline 50VU×10min + game day 4 cenários + security-scan 7d green + Go coverage ≥75% measured. **Runtime validation Go pendente** (toolchain ausente no Windows host): go build/vet/test/race/gosec/govulncheck deferido a CI release.yml + ci.yml já gateados. **Próxima ação**: commits organizados por domínio (DBA→Backend→QA→Frontend→Infra→Docs) + PR sprint/S52→develop com título "Sprint S52 final + S53 Fase H Ondas 1-3" + criar sprint/S53 zerada de develop pós-merge pra trackar Semana 3 quando staging existir. Decisão: D074 nesta tabela. **Etapa anterior:** **Fase G CONCLUÍDA — S52 ✅ (2026-04-22)** G.2 hardening final: OpenAPI refresh + cosign keyless + deploy verify + gosec HIGH gate + make security + k6 atualizado + pentest runbook. **Release workflow**: `id-token: write` habilita Sigstore keyless OIDC signing; `sigstore/cosign-installer@v3` pinado v2.4.1; cada imagem (api + web) é assinada **por digest** após o push — `cosign sign ghcr.io/...@sha256:...` (imutável; tags podem ser sobrescritas, digests não). **Deploy workflow**: novo job `verify` roda cosign verify **antes** de migrate + deploy; `certificate-identity-regexp` pinado em `https://github.com/<repo>/.github/workflows/release.yml@refs/tags/<tag>` — fork compromised não produz signature que passa no gate. migrate + deploy declaram `needs: [verify]`; smoke fica gated na cadeia inteira. **Makefile**: novo alvo `make security` roda `go vet + gosec -severity=high -confidence=medium + govulncheck` localmente (dev pode bater as mesmas gates do CI antes de pushar). gosec HIGH já estava ativo em ci.yml desde S32 (linha 129) — S52 confirma + documenta em pentest runbook. **k6 load test** (`api-baseline.k6.js`): adicionados `/api/v1/quotas` (S51) e `/api/v1/integrations` (S49) no batch de dashboard; 10% mix cached Meta Ads insights (200 ou 412 ambos aceitos); 5% mix POST /leads medindo custo do admission middleware; trends separados `quota_lookup_latency_ms` e `meta_insights_latency_ms`; thresholds p95<500ms e p99<1500ms permanecem hard gates. **Pentest runbook** (`.specs/security/pentest-runbook-S52.md`): 10 checks sequenciados com receitas exatas curl/openssl — rate limiter burst (A05), cross-tenant leak (A01) → 404 não 403 (evita confirmação implícita de existência), JWT tampering 3 variantes (alg=none + org_id swap + expired), CSRF missing+mismatch → 403, webhook replay idempotente, **lead webhook HMAC** (sucesso + replay dedup + body tamper + prefix downgrade — S50), **quota enforcement** (402 + X-Quota-* headers — S51), master impersonation **audit-first invariant** (audit write fail → 500 AUDIT_FAILED sem session issued — D062), WS origin rejection, frontend XSS bundle scan. `pentest-staging-template.md` como skeleton de findings. **OpenAPI spec refresh** (`api/openapi.yaml`): versão 0.1.0 → **0.52.0**, 341 → **1709 linhas**, ~13 paths → **~80 paths**. Cobre todas as famílias de endpoint S05-S51: leads (CRUD + cursor envelope + 402 quota response com X-Quota-* headers + LeadPage/LeadCreateRequest/LeadPatchRequest schemas), pipes (stages + entries + move 409), confirmations (F02 com GCal sync inline), meetings (F13), tasks, inbox (conversations + messages + takeover + resolve), templates, campaigns + launch, workflows + steps + runs + run step trace, agents + playground SSE (text/event-stream) + metrics + TTS (audio/mpeg) + triggers + knowledge, billing (Subscription/CheckoutRequest schemas), quotas (Quota schema + 402 QuotaExceeded response ref com headers), integrations (Google OAuth + TinyERP incl sync-products + Meta Ads insights com X-Torque-Cache header + SZChat), public webhooks (`/webhooks/billing/webhook` + `/webhooks/billing/asaas` + `/webhooks/lead` com HMAC-SHA256 + X-Torque-Tenant + 413 body-cap + 503 disabled), analytics, performance, products, members, proposals, settings, onboarding, master (impersonate com 500 AUDIT_FAILED documentado). Hot-path endpoints carregam request/response schemas completos; lower-traffic endpoints mantêm operationId + summary + security + responses mínimos — spec é o mapa, handler é o manual. openapi-typescript regen no frontend pode substituir api.gen.ts sem o gap S04-era. **Tests**: zero regressão — frontend 296/296 em 86 files mantido (S51 baseline); backend tests não tocados nesta sprint (todo o trabalho é infra/config/spec/ops, não código de runtime). typecheck + eslint --max-warnings 0 verdes. **Acceptance criteria runtime Go host + GitHub Actions**: (1) docker images em GHCR carregam cosign signature verificável keyless por `cosign verify --certificate-identity-regexp ... --certificate-oidc-issuer https://token.actions.githubusercontent.com`; (2) deploy.yml bloqueia migrate/deploy se signature falhar; (3) gosec zero HIGH findings no CI (gate já ativo em ci.yml); (4) pentest runbook executado em staging + findings em `.specs/security/pentest-staging-<YYYY-MM-DD>.md` (dual-sign reviewer+QA); (5) k6 report p95<500ms em staging com novas rotas S50/S51 incluídas. **FASE G CONCLUÍDA** (S51→S52). **ROADMAP S30-S52 COMPLETO** — 23 sprints, ~28 semanas da Paridade v8 entregues. **Gate Go-to-Production aberto** — checklist em `09 - Backlog/Plano de Acao Paridade v8 §10`: paridade funcional ≥95% vs v8 (aguarda D045 revisit), cobertura vitest ≥70% (atingido 78.48/75.30/74.75/62.22 em S49), cobertura Go ≥75% (pending runtime host), zero HIGH gosec/govulncheck/Trivy por 7 dias consecutivos, k6 p95<500ms em staging, pentest clean, dual-review em money-flow PRs (S51 exige isso por design), runbook dry-run. Commits S52: `85f2df9` infra · `30d9aa5` openapi. **Deferreds explícitos pra operação pós-prod**: (a) impersonation cookie-swap atômico com audit row (D044 A04 review); (b) openapi-typescript regen + api.gen.ts diff review no frontend; (c) deep-schema nas ~50 paths que ficaram minimal; (d) k6 run real contra staging uma vez que ambiente esteja provisionado. Próxima: **operação contínua + release cadencia**. **Fase G em andamento — S51 ✅ (2026-04-22)** G.1 Asaas real + dual-review + quota enforcement runtime. **Migration 0026**: `plan_quotas` catalog (plan_id, resource_key, plan_base) com seeds free/growth/enterprise × leads/team_members/workflows/agents + backfill transacional de `org_quotas` para todos os tenants existentes (prefere subscription live → terminal mais recente → free fallback) + permission seed `quotas.view`. **AsaasProvider real** (`service/billing/asaas.go`): implementa `billing.Provider` contra Asaas v3 REST — PIX apenas (boleto+cartão follow-up). `NewAsaas` retorna erro em APIKey vazia (**tripwire dual-review inviolável**: main.go cai no mock mesmo com BILLING_PROVIDER=asaas se a chave não estiver setada). `CreateCharge` sequencia POST /customers (dedup via externalReference=org_id na Asaas side) → POST /payments (billingType=PIX, dueDate=now+30min) → GET /payments/:id/pixQrCode; taxonomy 401 unauthorized, 429 rate limit, 5xx server, 404 ErrNotFound. `CancelCharge` DELETE /payments/:id tratando 404 como sucesso idempotente. `NormalizeAsaasWebhook` converte payload rico Asaas (`{event, payment.id}`) para shape normalizada: PAYMENT_CONFIRMED/RECEIVED → charge.paid, PAYMENT_OVERDUE → charge.overdue, PAYMENT_DELETED/REFUNDED → charge.cancelled; eventos desconhecidos passam como `asaas.<event>` prefixados para billing_events ledger sem acionar state machine. Event_id composto como `<EVENT>:<payment_id>` pra dedup através de retries do provider. **Quota infra** nova: (a) `repository/quota` com Get/List/IncrementUsage (usa `GREATEST(current_usage+delta, 0)` pra clamp em decrementos — usage é scalar monotônico não ledger)/SeedPlanDefaults/SetAdminAdjustment/SetPurchasedAddons; constants Resource{Leads,TeamMembers,Workflows,Agents} espelham regex CHECK da migration 0002. (b) `middleware/quota.RequireQuota(reader, resource)` factory panics em recurso vazio (build-time guard); runtime 402 QUOTA_EXCEEDED body `{error:{code, message, details:{resource, limit, current_usage, remaining}}}` + headers observability `X-Quota-{Resource,Limit,Usage,Remaining}` em todas as respostas (admit + deny) pro frontend pulsar banner upsell sem segundo roundtrip; master bypass via `sess.IsMaster`; ErrNotFound fail-closed (linha ausente = 402, não silent pass — força provisionamento explícito). (c) `handler/quotas`: GET /quotas (list) + GET /quotas/:resource (detail) member-visible + PATCH /quotas/:resource master-only (admin_adjustment e/ou purchased_addons). **Wiring cirúrgico em leads**: `leadshandler.WithQuota(quotaRepo)` encadeável; `Routes()` condicionalmente aplica `r.With(mw.RequireQuota)` no POST /leads, outros verbs passam livres; `create()` chama `IncrementUsage(+1)` após repo.Create sucesso (janela de race entre check e bump é bounded pelo rate limiter); `softDelete()` chama `IncrementUsage(-1)` pra tenant reclamar slot. Team_members/workflows/agents seguem exatamente o mesmo pattern — drop-in one-liner em sprint futura; infraestrutura middleware+repo+GREATEST clamp+headers está pronta. **Billing webhook upgrade**: `WebhookHandler.WithQuotaRepo(quotaRepo)` opcional; em charge.paid/payment.confirmed chama `quotaRepo.SeedPlanDefaults(orgID, sub.PlanID)` pra que upgrade do plano aplique effective_limit imediatamente sem dba intervention. Novo `POST /webhooks/billing/asaas` normaliza payload Asaas via `billing.NormalizeAsaasWebhook` antes de encaminhar pro mesmo path (dedup billing_events + state machine) do webhook genérico. **Config**: `ASAAS_API_KEY` (empty=mock fallback), `ASAAS_BASE_URL` (default sandbox.asaas.com/api/v3). **main.go wiring**: `quotaRepo := quotarepo.New(pool)` compartilhado entre leads/quotas/billing-webhook; `billinghandler.NewWebhook(...).WithQuotaRepo(quotaRepo).Routes(wh)`; `leadshandler.New(...).WithQuota(quotaRepo)`; `quotasHandler` mounted em t.Group (member) + mst.Group (master); AsaasProvider construído quando `cfg.AsaasAPIKey != ""` com warn log + fallback pra mock em erro. **Frontend**: `hooks/useQuotas` — useQuotas (list) + useQuota(resource) com staleTime 60s + KnownQuotaResource constants mirror do Go enum; `ui/quota-meter` — compact progress indicator com tone shift ≤80% neutral → 80-99% warning → 100% danger, ARIA meter role, clamps pct a 100, custom label opcional; FunisHubPage header surfaca leads quota meter quando tenant tem configurado; `api/errors` QUOTA_EXCEEDED → "Você atingiu o limite do seu plano… Atualize em Configurações → Plano e faturamento" + QUOTA_LOOKUP_FAILED friendly. **Tests backend**: asaas_test.go 13 cenários (dual-review tripwire + CreateCharge happy path multi-hop customer→payment→pix + rejeita non-BRL + rejeita amount=0 + CancelCharge 404-as-success + 5xx propagates + 401/429 taxonomy + NormalizeWebhook confirmed/overdue/cancelled mapping + unknown-pass-through + empty-payment-id reject + garbage-json reject). quota_test.go 7 cenários (admits + 402 + fail-closed + master bypass + unauthenticated + missing-tenant + factory panic empty resource). **20 funcs backend novos**. **Tests frontend**: useQuotas 3 cases (list shape + detail + URL encoding) + QuotaMeter 6 cases (undefined→null + unconfigured→null + pct copy + clamp 100 + ARIA + custom label). **Full suite 296/296 em 86 files** (+9 cases, +2 files vs S50); typecheck + eslint --max-warnings 0 verde. Commits: `8209b44` db · `e7be776` backend · `3156135` tests · `cddaae7` frontend. **Deferreds explícitos**: team_members/workflows/agents QuotaCheck wiring (pattern estabelecido, drop-in); boleto + cartão Asaas (PIX é o MVP); Asaas customer_id cache em subscriptions.provider_customer_id (dedup via externalReference Asaas-side já absorve idempotência). Próxima: **S52 — OpenAPI refresh final + cosign keyless + gosec high + pentest staging + k6 load test (FASE G FINAL)**. **Fase F concluída — S50 ✅ (2026-04-22)** F.2 Meta Ads Insights + Lead Webhook + SZ.Chat + TinyERP sync + F02→GCal wiring. **Migration 0025**: `lead_webhook_events` (org+external_id UNIQUE absorve retries, source enum webhook/meta/szchat/manual, raw_payload jsonb, signature_ok bool); `meta_insights_cache` (org+account_id+date_range UNIQUE, TTL 15min enforced at query time); provider CHECK estendido com `szchat`. **Meta provider** (`service/integration/meta/meta.go`) implementa `InsightsProvider` — dupla query Graph API (`level=account` + `level=campaign`) + `appsecret_proof` HMAC opcional + mapeamento `code=190/102` → `ErrAuthFailed`, `17/4/613` → `ErrRateLimited` + `decimalToCents`/`sumLeadActions`/`cpLead=-1 para leads=0`. **SZ.Chat provider** (`service/integration/szchat/szchat.go`) implementa `MessagingProvider` — Bearer auth + per-tenant API key + POST `/messages` JSON + `normalizeKind` text/image/audio/video/document fallback text. **TinyERP SyncProducts** (extend de `tinyerp.go`) — paginado `/produtos.pesquisa.php` + sink interface `UpsertBySKU(name, sku, description, price_cents, currency)` bridgeia adapter↔productrepo sem cyclic import; productrepo ganha `UpsertBySKU` com `ON CONFLICT (organization_id, sku) WHERE sku IS NOT NULL` + `xmax=0` pra distinguir insert vs update; sync result `{fetched, inserted, updated, skipped}`. **Lead webhook** (`handler/leadwebhook/webhook.go`): público `POST /webhooks/lead` (mesmo grupo de `/webhooks/billing`, outside `/api/v1`, sem Authenticator/CSRF). Assinatura HMAC-SHA256 sobre raw body em `X-Torque-Lead-Signature: sha256=<hex>` — `subtle.ConstantTimeCompare`, rejeita prefix ausente (downgrade guard), tenant header `X-Torque-Tenant: <uuid>`, body cap 64KiB, dedup via `UNIQUE(org,external_id)` → retorna 200 OK idempotente no retry do provider, senão cria lead + `publish(lead.created)` (workflow BusSubscriber enfileira `lead_created` automaticamente) + `AttachLead` best-effort → 201 `{lead_id, external_id}`. **F02→GCal wiring** (closure do deferred S49): `confirmations.WithIntegrations(gcal, store, logger)` — snapshot do confirmation antes de MarkConfirmed, goroutine 20s pós-resposta cria evento GCal com title "Reuniao confirmada — <channel>" + description "Confirmada via Torque CRM (F02)" + duração default 30min; fail silent (log + store.MarkError via breaker). **IntegrationsSection rich UI** (extraído de SettingsPage para `features/settings/IntegrationsSection.tsx`): cards Google/TinyERP/Meta/SZ.Chat live com `last_success_at` relative time pt-BR (agora / há X min / há X h / há X d / absolute) + last_error warning badge surfacing; connect via Sheet (Radix Dialog) — TinyERP api_key, Meta access_token + account_id, SZ.Chat api_key + channel_id; sync action TinyERP exibe `{inserted}/{updated}/{skipped}` inline; ambient cards WhatsApp/Asaas/n8n mantidos. **Hooks novos**: useConnectMeta, useDisconnectMeta, useConnectSZChat, useDisconnectSZChat, useSyncTinyERPProducts, useMetaAdsInsights (querystring account_id+date_range, staleTime 5min, enabled=false suspenso). **Config novo**: `META_GRAPH_BASE_URL` (default v18.0), `META_APP_SECRET`, `SZCHAT_BASE_URL`, `LEAD_WEBHOOK_SECRET`. **Main.go wiring**: providers metaProvider/szchatProvider built unconditionally (nil-guarded at handler), metaCache + leadWebhookEvents, productSyncSink adapter no file para bridgear productrepo↔tinyerp sem circular dep; confirmations handler wired com `.WithIntegrations(gcalProvider, credStore, logger)`. **Tests backend**: meta 10 cenários (decimalToCents table, sumLeadActions case-insensitive, cpLead boundaries, appsecret_proof, get happy/oauth/rate-limit, fetchInsights rollup) + szchat 8 (postJSON happy + 401/403/429/5xx + provider error body + normalizeKind) + tinyerp sync 7 (decimalToCents BR comma, listProductsPage OK/last-page/auth, fakeSink ins vs upd vs skip) + leadwebhook 6 (HMAC happy/no-prefix/tampered/wrong-secret/malformed-hex/truncated) + integration MockInsights 1 + compile-time `var _ InsightsProvider` = **32 novos** (pacote-level, cobre wire contract sem exigir pgx). Dedup + lead creation + F02→GCal integration-path coverage deferred para CI que tem host Postgres. **Tests frontend**: 18 cenários novos (12 hook paths + 6 relativeTime buckets). **Full suite frontend 287/287 em 84 files** (+13 cases, +1 file vs S49). Typecheck + eslint `--max-warnings 0` verde. **Fase F CONCLUÍDA** — 2 sprints em topologia linear (S49→S50). 2 migrations (0024 credentials, 0025 webhook+cache). Próxima fase: **G — Hardening produção (S51-S52)** Asaas real + dual-review + quota enforcement + OpenAPI refresh + cosign + gosec + pentest staging. **Fase F em andamento — S49 ✅ (2026-04-20)** F.1 Google Calendar real + TinyERP foundation. Migration 0024: `integration_credentials` (organization_id + provider CHECK IN ('google','tinyerp','meta')) com AES-256-GCM encrypted access/refresh tokens + observability fields (last_success_at, last_error_text/at, external_account_id, scopes). Seeds integrations.view (default true) + integrations.manage (admin_only). `service/crypto/aesgcm.go` envelope nonce‖ct‖tag, ErrShortEnvelope/ErrDecrypt opaco. `repository/integration/credential.go` Upsert/Get/List/MarkSuccess/MarkError/Delete com WHERE organization_id = $1 invariante. `service/integration/gcal/gcal.go` implementa CalendarProvider real — DefaultEndpoints produção + WithEndpoints escape para httptest; AuthURL access_type=offline+prompt=consent+scope=calendar.events openid email; ExchangeCode/RefreshToken form-encoded com mapeamento 401→ErrAuthFailed/5xx→ErrUnreachable; CreateMeetingForOrg refresca token se expires_at<now+2min + POST events com Bearer; Disconnect revoke best-effort + store.Delete. `service/integration/tinyerp/tinyerp.go` implementa ERPProvider — POST form token+formato+pedido JSON pra pedido.incluir.php; codigo_erro=6→ErrAuthFailed / 30→ErrRateLimited. `handler/integrations/integrations.go` state HMAC base64url(org|ts|nonce)+"."+hmac256[:16] TTL 10min ConstantTimeCompare — GET /integrations (member list), /integrations/google/{connect,callback,disconnect}, /integrations/tinyerp{,/disconnect,/push-order}. Callback CSRF-exempt subgrupo. `domain/context.go` WithOrgID/OrgIDFrom novo carrier para goroutine background (middleware key unexported). Meetings handler `WithIntegrations(gcal,store)` opcional — POST /meetings agora enfileira goroutine 20s que cria evento GCal real + UPDATE meetings SET external_provider='gcal',external_id=event_id; DELETE espelha com CancelMeetingForOrg. Config: INTEGRATION_ENCRYPTION_KEY (fatal non-dev, dev fallback via sync.Once WARN) + GOOGLE_OAUTH_{CLIENT_ID,CLIENT_SECRET,REDIRECT_URL} + TINYERP_BASE_URL + INTEGRATION_STATE_SECRET (fallback JWT_SECRET). Frontend `useIntegrations` hook + SettingsPage Integrações tab wired ao backend: card Google com Conectar/Desconectar real (full-page nav para /api/v1/integrations/google/connect); card TinyERP "em breve" (modal api_key → S50); query param ?tab= pra preselecionar seção do callback. **Tests backend** (toolchain Go pendente host — hand-checked): crypto 7 + gcal 10 + tinyerp 8 + credential 2 (integration-gated) + handler 9 = 36 funcs. **Tests frontend** 274/274 em 83 files (+5 cases +1 file vs remediation-C) coverage preservada 78.48/75.30/74.75/62.22 acima ratchet 70/70/70/60. **Out-of-scope**: sync-products, Meta Ads, SZ.Chat, confirmation F02→gcal (S50+). Próxima: **S50 — Meta Ads Insights + Lead Webhook + SZ.Chat + IntegrationsSection rich UI**. **Remediation-C ✅ (2026-04-20)** — lift de cobertura pré-Fase F. 15 test files novos (5 pages: CampaignDetail/AgentMetrics/CustomPipe/TVDashboard/UpsellPage + 6 hooks extras: useAnalytics/useCampaigns/useInbox/useMeetings/usePerformance/useWorkflows + 2 lib: ws/utils). Vitest ratchet step 4: 55/55/50/48 → **70/70/70/60**. Medido: lines **78.21** / stmts **75.02** / funcs **74.20** / branches **62.22**. **269/269 em 82 files** (+74 cases, +14 files vs S48). Gitignore ajustado (*.tsbuildinfo + .claude/scheduled_tasks.lock). Branch `sprint/remediation-C` → PR para `develop`. Próxima fase: **F — Integrações externas (S49-S50)**. **Fase E concluída (2026-04-20) — S48 ✅** F12 Pipes custom + Upsell + F13 Agenda + TV Dashboard. Migration 0023: meeting_status enum + meetings table com CHECK window + external_provider/external_id unique pair (ponto de extensão S49 GCal) + seeds meetings.view/manage. Repo + handler meetings CRUD + ListRange (intersection). Frontend: hooks useMeetings + AgendaPage /agenda (lista agrupada por dia pt-BR + CreateMeetingModal datetime-local); UpsellPage /upsell (grid leads com badges); CustomPipePage /pipe/:id (reusa hooks pipe + board horizontal por stage); TVDashboardPage /tv fora do AppShell (setInterval 20s + 3 widgets reusando hooks Analytics+Performance). Tests AgendaPage 2 cenários. **195/195 em 68 files** (+4, +3 vs S47). **Fase E completa** — 3 sprints: S46 (F08 Campanhas UI wizard) → S47 (F09 Performance) → S48 (F12 Pipes custom + Upsell + F13 Agenda + TV). 2 migrations novas (0022, 0023). **Deferreds**: dispatch rules SDR/Closer, progresso % metas + comissão automática, materialized view upsell, react-big-calendar, GCal sync. Próxima fase: **F — Integrações externas (S49-S50)**. **Fase E em andamento — S47 ✅ (2026-04-20)** F09 Performance consolidado (Ranking + Metas + Comissões + Premiações). Migration 0022: goal_metric/commission_status enums + goals + commissions + awards + seeds performance.view/manage. Repo performance com Ranking (LEFT JOIN proposals won agregando por member, drift-free), CRUD de goals/awards, ListCommissions + SetCommissionStatus com COALESCE stamps. Handler split reads (member) / admin writes. Frontend usePerformance hooks + PerformancePage 4 tabs (Ranking com 7/30/90d picker + bars pct BRL; Metas rows com período; Comissões cards totalizadores + lista com status badges; Premiações rows). Tests 2 cenários (ranking bars + tab switch). **Deferred**: progresso % das metas (endpoint agregado) e cálculo automático de comissão ao fechar proposta (workflow action + bus hook). Próxima: S48 (F12 Pipes custom + Upsell + Agenda + TV Dashboard). **Fase E em andamento — S46 ✅ (2026-04-20)** F08.1 Campanhas UI. CampaignsPage com 2 tabs (ativa/arquivada partition por status) + grid cards com stats counters pt-BR. CreateCampaignModal wizard 3 steps (identidade + audiência DSL reusando TriggerFilter + schedule). CampaignDetailPage em `/campaigns/:id` com status card + 4 NumberCards + recipients list + ações contextuais (pausar/retomar/cancelar). Route `/campaigns/:id` lazy. Tests 2 cenários. **Deferred**: dispatch rules SDR/Closer (depende de schema assign_rule); stage-kanban duplicaria lista. Próxima: S47 Performance. **Fase D concluída (2026-04-20) — S45 ✅** F07.3 Workflow actions extras + Execuções UI + debug run. Backend: 3 novos handlers (CreateTaskAction, CallAgentAction, HTTPRequestAction com validateHTTPURL https-only) + NewDispatcherS45 factory registra 7 kinds (4 S44 + 3 S45); eventTriggerMap extendido com lead.stage_changed→lead_stage_changed + message.received→message_inbound; nova rota `GET /runs/:id/steps` retorna trace timeline. Frontend: hook `useWorkflowRunSteps` com refetchInterval 2s enquanto pending/running; página `/workflows/:id/executions` com 2-column (run list 50 mais recentes + selected-run timeline com per-step status/duration/JSON preview); "Rodar em debug" header enfileira run debug (runner pega em ≤2s); link Execuções no canvas header. Tests: `actions_s45_test.go` 4 cenários (7 kinds registrados + create_task + call_agent + http scheme validation); `WorkflowExecutionsPage.test.tsx` 2 cenários. **191/191 em 66 files** (+2, +1 vs S44). Coverage lines 63.33 → **64.2** (+0.87pp). **Fase D completa** — 3 sprints: S43 (canvas xyflow + list) → S44 (executor + dispatcher) → S45 (actions extras + executions UI). **Deferreds explícitos**: handlers side-effect reais (migração stub→real por integração), retry/DLQ, schedule trigger (cron), canvas debug inline. Próxima fase: **E — Produto completo (S46-S48)** F08 Campanhas + F09 Performance + F12 Pipes custom. **Fase D em andamento — S44 ✅ (2026-04-20)** F07.2 Workflow executor + ActionDispatcher + event-bus subscriber (data plane). Repo executor_queries.go: ClaimPendingRun (FOR UPDATE SKIP LOCKED + flip running + started_at em tx), SetRunCurrentStep, MarkRunSucceeded/Failed, AppendRunStep / CompleteRunStep, ListRunSteps, ListStepsByIDs (batch ANY), ListActiveWorkflowsByTrigger. Service workflow/dispatcher.go: ActionHandler interface + 4 handlers (SendMessage stub, UpdateLead stub, Wait stub, Branch com evaluator ==/!= + dotted paths lead/input/prev); unparseable expression → true branch com expression_valid=false. Executor walks entry_step_id → next_step_ids com MaxStepsPerRun=100 loop guard, per-step trace completo, __input+__lead em PreviousOutputs. Runner goroutine polls 2s+500ms jitter com per-run 2min ctx. BusSubscriber lead.created → lead_created enfileira runs. main.go wira Dispatcher+Executor+Runner+BusSubscriber com shutdown ordenado. Tests dispatcher_test.go 11 cenários (registry + send_message + wait + branch 6 cenários + register override). **Deferred honesto**: handlers side-effects reais (Evolution/lead repo) + retry/DLQ + triggers stage_changed/message.received/schedule ficam para S45. Próxima: S45 (6 more action types + WorkflowExecutionsPage). **Fase D em andamento — S43 ✅ (2026-04-20)** F07.1 Workflow Builder canvas xyflow + list page. Dep `@xyflow/react ^12.10.2` adicionada. Páginas: `WorkflowListPage` (`/workflows`) substitui seed-mockup com `useWorkflows` live + empty state + "Novo workflow" cria draft manual e navega; `WorkflowCanvasPage` (`/workflows/:id`) 3-column (palette draggable / ReactFlow / inspector), 6 node variants (lead_created + message_received triggers, send_message, update_lead, wait, branch). Drag-drop via `screenToFlowPosition`; `onNodeDragStop` PUT position; `onConnect` merge `next_step_ids` com dedupe; primeiro trigger sem entry auto-seta entry_step_id; inspector com JSON config editor + Excluir + "Definir como entry". `updateStepAsync`/`deleteStepAsync` via raw `put`/`del` + queryClient invalidate (os hooks existentes bindam stepId na construção). **189/189 em 65 files** (+2, +1 vs S42). Coverage lines **63.33%** (+0.12pp). Próxima: S44 (executor worker). **Fase C concluída (2026-04-20) — S42 ✅** F06.6 Copilot Metrics page. Backend: `GetAgentMetrics` com sessões agrupadas por state em uma query + mensagens via JOIN com COUNT/SUM tokens/AVG latency_ms filtered to role='assistant'. Handler `GET /agents/:id/metrics?since&until` com defaults últimos 30 dias, RFC3339 parsing, rejeita INVALID_SINCE/INVALID_UNTIL/INVALID_WINDOW/WINDOW_TOO_LARGE (> 365d). Frontend: hook `useAgentMetrics` queryKey=[since,until] staleTime 60s; `AgentMetricsPage` em `/copilot/:id/metrics` com window picker 7d/30d/90d, 4 KPI cards (sessões/mensagens/tokens in-out/latência média pt-BR) e bar stacked sessions-by-state (open/ended/escalated) com legenda. Link Métricas no Playground editor. **187/187 em 64 files** (+2, +1 vs S41). Coverage lines **63.21%** (+0.13pp). **Fase C completa** — 6 sprints: S37 (OpenRouter+SSE) → S38 (Playground UI) → S39 (RAG) → S40 (Triggers) → S41 (TTS) → S42 (Metrics). **Deferred**: worker dispatch dos triggers (Fase D), TTS outbound pipeline + org_quotas, session persistence no Playground, URL fetch HTTPS, Visx timeseries. Próxima fase: **D — F07 Workflow Builder (S43-S45)**. **S41 ✅ (2026-04-20)** F06.5 Copilot TTS (ElevenLabs adapter + agent config + Playground preview). Migration 0021: `agents += tts_enabled bool` + `tts_voice_id text(2-80)`. Service `ai/tts.go` com `TTS` interface + `ElevenLabsTTS` (POST `/v1/text-to-speech/:voice_id`, xi-api-key, `audio/mpeg`, 5000-char text cap, 5 MB response cap, 401/429/5xx/4xx taxonomy) + `MockTTS` fallback quando `ELEVENLABS_API_KEY` vazio. Config `ELEVENLABS_BASE_URL|API_KEY|MODEL_ID` (default `eleven_multilingual_v2`). Repo Agent ganha TTSEnabled/TTSVoiceID; UpdateAgentInput aceita empty string=NULL. Handler: agentView expõe campos; novo `POST /agents/:id/tts/preview` (body override voice_id + default sample text, 25s ctx, `audio/mpeg` + `Cache-Control: no-store`, sem storage). Frontend: Agent types + UpdateAgentPayload estendidos; `previewTTS()` helper fetch + `URL.createObjectURL`; `TTSPanel` no editor Playground (checkbox + voice_id input + "Ouvir preview" + inline `<audio controls autoPlay>` com object URL cleanup). **185/185 em 63 files** (+2, +1 vs S40). Coverage lines **63.08%** (+0.37pp). **Pipeline outbound adiado**: worker `message.outbound` → mp3 → storage S3 + `org_quotas.tts_seconds_per_month` + player no MessageList ficam para sprint futura (depende de storage S3 wiring + quotas schema). Próxima: S42 (Metrics page). **S40 ✅ (2026-04-20)** F06.4 Copilot triggers + auto-assignment (data plane). Migration 0020: `agent_triggers` (priority/filter_json/is_active) + `conversations.assigned_agent_id` + seeds `triggers.view|manage`. Service `ai/matcher.go` pure function com ops eq/neq/contains/in/present/absent + all/any semantics + kill-switch/status/inactive rule guards + unknown-op fail-closed. Repo triggers CRUD + `ListActiveTriggers` com JOIN agents + `AssignAgent`. Handler `GET/POST/PATCH/DELETE` routes + WS events. Frontend: hooks `useAgentTriggers/useCreateTrigger/useUpdateTrigger/useDeleteTrigger` + `TriggersPanel` no editor Playground (form inline + rule list com toggle/trash). **Ratchet coverage** → 60/60/55/50 (era 55/55/50/48); medido lines **62.71** / stmts **60.22** / funcs **57.23** / branches **52.01**. **183/183 em 62 files** (+5, +1 vs S39). **Worker dispatch** (`conversation.assign_agent` + event bus hooks) adiado para Fase D — reusa o workflow executor. Próxima: S41 (TTS ElevenLabs). **S39 ✅ (2026-04-20)** F06.3 Copilot RAG: pgvector + Gemini embeddings + retrieval. Migration 0019 (CREATE EXTENSION vector + `knowledge_chunks.embedding vector(768)` + HNSW cosine + `agents.knowledge_collection_id` FK + seeds `knowledge.view|manage`). Docker-compose dev → `pgvector/pgvector:pg15`. Backend: `ai/embeddings.go` Embedder interface com GeminiEmbedder (batchEmbedContents, RETRIEVAL_DOCUMENT task, taxonomia 401/429/dim-mismatch) + MockEmbedder SHA256-chained fallback; `ai/chunk.go` paragraph-aware 500/50; repo knowledge com ListCollections/GetSource/InsertChunks (tx idempotente) + SimilaritySearch `<=>` tenant-scoped topK clamp + SetAgentKnowledgeCollection cross-tenant guard + encodeVector. Service `knowledge/Ingest` chunk→embed→insert com status transitions e RunDetached goroutine 10min recover. Playground handler agora embeda último user turn (5s bounded) + SimilaritySearch topK=5 + prependContext delimitado antes do system prompt, degrada silenciosamente. Frontend: hooks useKnowledgeCollections (WS-aware)/useKnowledgeSources (refetchInterval 3s enquanto queued|ingesting)/useEnqueueSource (kind=text|markdown com content)/useBindAgentCollection (PUT com null). KnowledgePanel no editor: select + create inline + sources list com status badge + ingest form text. **178/178 em 61 files** (+5, +1 vs S38), coverage lines **62.23%** (+0.67pp), stmts **59.81%** (+0.62pp). Próxima: S40 (worker-based ingest + URL fetch HTTPS/SSRF + triggers de ativação por filtros de lead). **S38 ✅ (2026-04-20)** F06.2 Copilot Playground UI + useAgentStream. AgentListPage real substitui mockup seed (`AgentsPage.tsx` removido); AgentPlaygroundPage (`/copilot/:id`) layout Vercel — editor lateral (name + system_prompt + model + temp + max_tokens) com dirty detection + chat SSE com ações Parar/Regenerar. Hook `useAgentStream` consome `fetch` + `ReadableStream.getReader()` (EventSource não suporta CSRF headers); parser próprio `\n\n` respeitando event frames delta/done/error; AbortController limpo no unmount. Backend expõe `system_prompt` no `agentView` via `toAgentView` helper unificado (antes havia construção literal duplicada em playground.go). Tests novos: useAgentStream 4 cenários + AgentListPage 2 cenários. **173/173 tests em 60 files**, coverage lines **61.56%** (+2.39pp), stmts **59.19%** (+2.49pp). Próxima: S39 (Agent Wizard 20+ steps + session persistence em `agent_sessions`). **S37 ✅ (2026-04-20)** F06.1 OpenRouter adapter + Playground SSE. Service `ai/openrouter.go` com Provider interface agnóstica, CircuitBreaker-ready, 6 códigos de erro mapeados. Handler `/agents/:id/playground/message` SSE com kill-switch gate antes do dial e 5 event codes estáveis para frontend. UpdateAgent partial patch no repo. Tests: 7 cenários httptest no adapter + integration test agent. Frontend vitest 168/168 sem regressão. **Remediation-B ✅** — lift testes + obs pré-Fase C: score composto **84 → 87/100**. **Fase B concluída (S33+S34+S35+S36)** — F04 Inbox completo. Próxima: **Fase C** (S37-S42, F06 Copilot/IA — Agent entity + Playground + OpenRouter + RAG pgvector + TTS). **Fase B — S36 ✅** takeover audit + teamMemberId. **S35 ✅** ComposerBar + Evolution adapter + templates. **S34 ✅** MessageList + MessageBubble. **S33 ✅** InboxPage shell. **Fase B — S33 ✅** F04 InboxPage shell real. **Fase A concluída (S30+S31+S32, 2026-04-20)** — foundation repair do plano D046 fechado. Segue texto histórico da fase: **Fase A paridade v8 — S31 ✅ (2026-04-20)**. Vitest coverage thresholds enforcement (45/45/40/40 baseline, target 70/65/60/55 por S40), 10 hook test files novos cobrindo os gaps críticos de D045. 132/132 tests em 46 files. CI gateia via --coverage. **S30 ✅** lazy loading com lazyRetry wrapper, 67 chunks gerados, entry 109.7 KB / 35.4 KB gzip. Próxima: S32 (tenant isolation integration tests + Dependabot + OpenAPI refresh + gosec no ci.yml main). **S00-S29 ✅ — Roadmap original completo (2026-04-20).** Roadmap de 30 sprints concluído. Próximos passos: execução dos follow-ups não-bloqueantes (Dependabot, GHCR signing, impersonation cookie swap, OpenAPI refresh, gosec no CI), runtime backend validation no host (go mod tidy → migrate up → go test -race), pentest de staging usando checklist em `.specs/security/security-hardening-audit-2026-04-20.md`, load test em staging via k6, e lancamento real. **S29** — **S28 entregue (2026-04-20)** — CI/CD pipeline completo: ci.yml estendido com backend + golangci-lint, release.yml para Docker build/push com tag v*, deploy.yml manual dispatch com migrate→deploy→smoke e rollback via re-dispatch. **S27** — **S27 entregue (2026-04-20)** — Integrations Foundation: adapter interfaces (Messaging/Calendar/ERP) + Mocks + CircuitBreaker + Retry. Live providers adiados pending credentials. **S26** — **S26 entregue (2026-04-20)** — F16 Master Admin: cross-org ListOrganizations + SystemHealthSnapshot + ImpersonationTarget com audit-first invariant (audit falha = impersonation refuse). Route `/master` e hooks `useMaster.ts` no frontend. **S25** — **S25 entregue (2026-04-20)** — F15 Configurações consolidadas: org profile editável, webhook_endpoints CRUD, notification_preferences upsert. SettingsPage OrgSection agora é live. Webhook tab + notifications tab UIs ficam para sprint futura. **S24** — **S24 entregue (2026-04-20)** — F14 Billing Foundation (data plane + Provider interface + MockProvider + PUBLIC webhook state machine). AsaasProvider real fica para sprint futura com credenciais + dual-review. **S23** — **S23 entregue (2026-04-20)** — F13 Onboarding Wizard + Gate. Migration 0015 (onboarding_status). Backend: repo+handler com 6 canonical steps, EnsureForMember idempotente, CompleteStep advance + completed_at stamping. Frontend: OnboardingPage com StepProgress + OnboardingGate dentro de ProtectedRoute que força wizard antes de qualquer rota. **S22** — F12 Pipelines Customizados exposto via admin lifecycle (CreatePipe/UpdatePipe/ArchivePipe + CRUD stages); reusa pipe_kind='custom' da migration 0002 + permission key pipes.manage. Frontend ganha hooks admin de pipe/stage. Admin UI fica para sprint de Configurações. **S21** — F10 Equipe (CRUD de membros, role, permission overrides per-member) + F11 Produtos (catálogo de produtos/serviços para Propostas) seguindo protocolo linear cumulativo. Migration 0014 (products + seeds members.view/manage e products.view/manage). Backend: repositórios e handlers splitados em Read (member-accessible) + Admin (RequireRole). Self-demote e self-deactivate refused no handler (prevenção de org-lockout pelo último admin). Frontend: hooks `useMembers`/`useProducts`, SettingsPage aba Equipe agora renderiza dados reais via useMembers, ProductsPage nova em `/products` com cursor pagination e formatação BRL. Local validation: typecheck OK, eslint zero warnings, **vitest 99/99** em 35 arquivos. Runtime backend (go mod tidy/build/migrate up/go test) pendente no host.

## Key Decisions

| ID | Date | Decision | Rationale |
|----|------|----------|-----------|
| D079 | 2026-04-26 | Sprint S67 Infra CI/CD fixes + prod compose env vars completas + .env.production.example | **Branch sprint/S67-infra-ci-fixes nascida de develop atualizada (com S63 mergeada). 9 arquivos tocados.** **CI pins (supply chain)**: `actions/setup-node@v6 → @v4` em ci.yml + security-scan.yml (v6 prerelease, v4 estavel); `securego/gosec@master → @v2.22.0` em ci.yml + security-scan.yml (substituir floating master por tag pinada — bumpado em lockstep nos dois files). **govulncheck go-version fix**: `go-version: '1.22'` hardcoded → `go-version-file: torque-api/go.mod` em security-scan.yml — go.mod estava em 1.25 mas vulncheck rodava com toolchain 1.22 (falso-clean potencial em modulos 1.23+). **release.yml COSIGN_EXPERIMENTAL=1 deprecated** — removido em ambos `Sign API image` e `Sign web image` blocos; cosign v2 keyless OIDC e default sem flag. **docker-compose.prod.yml** (api service): adicionado ~20 env vars ausentes — `LEAD_WEBHOOK_SECRET`, `INTEGRATION_ENCRYPTION_KEY`, `INTEGRATION_STATE_SECRET`, `BILLING_PROVIDER`, `ASAAS_API_KEY`, `ASAAS_BASE_URL`, `OPENROUTER_API_KEY`, `OPENROUTER_BASE_URL`, `GEMINI_API_KEY`, `GEMINI_BASE_URL`, `ELEVENLABS_API_KEY`, `ELEVENLABS_BASE_URL`, `ELEVENLABS_MODEL_ID`, `GOOGLE_OAUTH_CLIENT_ID`, `GOOGLE_OAUTH_CLIENT_SECRET`, `GOOGLE_OAUTH_REDIRECT_URL`, `META_GRAPH_BASE_URL`, `META_APP_SECRET`, `SZCHAT_BASE_URL`, `TINYERP_BASE_URL`, `SENTRY_TRACES_RATE`. Defaults sensiveis em vars onde aplicavel (`ELEVENLABS_BASE_URL:-https://api.elevenlabs.io`, `BILLING_PROVIDER:-mock`). **Logging driver json-file** com max-size 10m / max-file 3 em api + web — sem isso `/var/lib/docker/containers/*-json.log` cresce indefinidamente no host VPS. **torque-web/Dockerfile** HEALTHCHECK wget removido — base `nginx-unprivileged:1.27-alpine` nao tem wget nem curl, adicionar uma das duas explode superficie de ataque. EasyPanel/orchestrator poll `/healthz` externamente. Compose web service ganha `healthcheck.disable: true`. **.env.production.example NOVO** (~150 linhas): template completo com TODAS as vars ~30 documentadas com comentarios apontando runbooks (.specs/runbooks/key-rotation.md), secret managers (1Password Torque Ops vault), e onde cada provider e signed up (links de signup explicitos). Marca tripwires (D072 dual-review Asaas + mock fallback AI vazio com WARN log) explicitamente. **.gitignore** ajustado: `!.env.production.example` adicionado pra permitir o template versionado mantendo `.env.*` excluso. **Out-of-scope explicito (ja triado em S68 + bloqueador humano)**: GitHub Environments staging+production criacao (H6); secrets populacao via gh CLI (A2 — depende de credenciais providers H9-H16); Caddyfile/Traefik config TLS edge (A3 — depende de VPS H1+DNS H4+TLS H5); EasyPanel install (H2); Postgres managed provisioning (H3); Sentry DSN obtain (H9); Asaas conta (H10); etc — total 25 bloqueadores humanos catalogados em `Torque-dir-new/09 - Backlog/Plano de Finalizacao - Sprints S57-S70.md` §5. **Subagentes worktree paralelos bloquearam por permissoes** (Edit/Write/Bash denied no sandbox child) → execucao centralizada nesta sessao. **Commits S67**: 2 `chore(infra)` + 1 `docs(vault)`. **Proxima**: S69 QA coverage gate (service/permission tests + handler/billing + handler/leads + CI coverprofile soft gate) -> S57 Frontend DnD funis. |
| D078 | 2026-04-26 | Sprint S63 Backend deferreds — 3 de 4 D074 fechados (h, k, d); (i) diferido pra S63.5 | **3 fixes acionaveis nesta sprint, evitando heroismo com (i) que precisa de schema migration. Branch sprint/S63-backend-deferreds nascida de develop atualizada (D012).** **(h) DNS resolver com ctx** — `service/workflow/actions_s45.go:919` `validateHTTPSURL` agora aceita `context.Context` e propaga para `net.DefaultResolver.LookupIPAddr` em vez do global `net.LookupIP`. Antes: resolver lento bloqueava executor pelos 2min cheios do `runCtx`. Agora: ctx do step honra timeout + cancel imediato. Caller atualizado (linha 734); zero outros callers (grep confirmado, zero em `*_test.go`). **(k) buildLeadFacts hydration via LeadResolver** — `service/ai/trigger/subscriber.go` ganha narrow interface `LeadResolver` opcional + struct `LeadAttrs {Origin, Segment, UTMSource, UTMMedium, UTMCampaign, Rating}` + setter `WithLeads(resolver) *Subscriber`. Quando wired, `handle()` hidrata `LeadFacts` via SQL JOIN `conversations -> leads` por `conversation_id` (= `EntityID` em events handled). Sem wiring: catch-all rules ainda matcham; predicados em atributos do lead silenciosamente miss (status anterior). Wiring em `cmd/api/main.go` via `poolLeadResolver` inline (composition root, sem poluir leadrepo com metodo cross-aggregate). 1 query SQL extra por evento — aceitavel. Tags multi-value ainda diferida (precisa JOIN `lead_tags` raramente discriminator). Lookup miss degrada com Debug log + facts vazios (race com soft-delete = nao alertavel). **(d) GET /workflows/runs/:id/failures** — `handler/workflows/workflows.go` ganha route `r.Get("/runs/{id}/failures", h.listRunFailures)` + `runFailureView` DTO + handler. Expoe `Repository.ListRunFailures` (executor_queries.go:361) que existia desde S53 mas nunca tinha rota. Response: `{data: [{id, run_id, workflow_id, step_id, attempt, error_code, error_message, snapshot, created_at}]}` ordenado por attempt DESC + created_at DESC. Authn via `mw.OrgIDFrom`; cross-tenant rejeitado por `WHERE organization_id = $1` no repo. Admin DLQ UI agora pode visualizar trail de retries por run. **Diferidos honestos**: (i) `backoffFor` configuravel per-tenant — exige schema (`workflows.trigger_config.backoff_*` ou `org_workflow_config` table) + repo update + tests, escopo M proprio (S63.5); (a) Evolution per-tenant selector workflow — exige integracao adapter completa, escopo L (S64); (b) Schedule/cron trigger workflow — exige `robfig/cron` + parser, escopo L (S64); (g) Meta Lead Ads + SZ.Chat inbound webhooks — escopo L (S64); (c) Asaas boleto+cartao — escopo M com dual-review (S65); (j) playground SSE integration test — pgxpool de test, escopo M (S69). **Validacao**: tsc nao aplicavel (Go); host Windows sem toolchain Go — testes deferidos ao CI. Imports + tipos validados manualmente (grep callers, leitura cuidadosa). Subagentes paralelos (frontend/backend/infra/qa) bloquearam por permissoes worktree → execucao centralizada nesta sessao. **Commits S63**: 3 commits `feat(backend):` (h, k, d) + 1 `docs(vault):` (D078 + Indice + Plano). **Proxima**: S67 Infra CI/CD fixes (paralela em arquivos diferentes — sem conflito) -> S69 QA coverage gate -> S57 Frontend DnD funis -> S63.5+S64+S65 backend remanescentes. |
| D076 | 2026-04-26 | Sprint S56 Onda 1 — Responsive Foundation: shell adaptativo, primitivos UI mobile-aware, tipografia fluida, safe-area iOS | **ADR-008** (`Torque-dir-new/08 - Decisoes/ADR-008-responsive-foundation.md`) registra contrato de responsividade do design system. Auditoria pre-sprint: 22 arquivos em src/ com utilities `sm:|md:|lg:|xl:` num codebase >300 arquivos React → app desktop-only. **Onda 1 entrega chassi cross-cutting; ondas 2-5 ficam em sprints separadas S57-S60.** **9 mudancas concretas:** (1) `index.html` confirmado com `viewport-fit=cover` (validado, nao alterado). (2) `tailwind.config.ts` confirmado com breakpoints default Tailwind sm/md/lg/xl/2xl (640/768/1024/1280/1536) — threshold critico Sidebar fixa = `lg`. (3) `src/styles/globals.css @layer utilities` ganha 4 classes fluid type via `clamp()` (`.text-fluid-display`/`.text-fluid-h1`/`.text-fluid-h2`/`.text-fluid-h3`) com letter-spacing alinhado ao Spec Sistema Base + 6 utilities safe-area iOS (`.pt-safe`/`.pb-safe`/`.pl-safe`/`.pr-safe`/`.px-safe`/`.py-safe`) + `.min-h-screen-safe` com `100dvh` fallback `100vh`. Body type fica em `text-sm`/`text-base` legacy — clamp em copy quebra ritmo vertical. (4) `src/shell/AppShell.tsx` orquestra mobile drawer: state `sidebarOpen` controlado, useEffect em `location.pathname` fecha drawer ao navegar, `<Sheet>` left + width 280px renderiza `<SidebarContent />` em `<lg`, container raiz mudou de `h-screen` para `h-[100dvh]` (iOS Safari URL bar fix). (5) `src/shell/Sidebar.tsx` extraido em 2 componentes: `Sidebar` `<aside hidden lg:flex>` desktop-only + `SidebarContent` body reusavel (props `onNavigate?` para drawer fechar on click). `SidebarLink` ganha `h-11 lg:h-9` — touch 44px mobile, density 36px desktop. (6) `src/shell/TopBar.tsx` recebe optional `onOpenSidebar` prop, renderiza hamburger `Menu` icon `<lg` (44x44 touch). Search bar `flex-1 sm:w-[260px] md:w-[340px]` com texto adaptado ("Buscar..." mobile vs full mobile). Acoes secundarias (UiModeToggle + theme button) escondem `<md`; theme toggle migra pro UserMenu dropdown como item. Bell + UserMenu touch-sized `h-11 w-11 md:h-8 md:w-8`. (7) `src/ui/button.tsx` ganha 2 size variants: `touch` (`h-11 min-w-[44px]`) + `icon-touch` (`h-11 w-11`). 6 sizes legacy preservados — nao redimensionar quebra densidade desktop. Pattern: opt-in via `size="touch"` em fluxos exclusivos mobile. (8) `src/ui/sheet.tsx` extendido com 2 variants novas: `side="bottom"` (mobile bottom sheet, max-h `90dvh`, rounded top, `pb-safe`) + `side="fullscreen"` (substituto Dialog mobile, `100dvh`, `pt-safe pb-safe`, edge-to-edge). Close button ganha `min-h-[44px] min-w-[44px]` em todas variants — touch target inviolavel. **Decisao deliberada: nao criar `Dialog` primitivo separado** — reuso de `Sheet` com `side="fullscreen"` atende os mesmos casos com inventario menor. (9) Snapshot `sheet.test.tsx` atualizado (close button ganhou min-h/min-w). **Validacao**: `tsc --noEmit` verde, `eslint --max-warnings 0` verde em src/shell + src/ui/{button,sheet}.tsx, `vitest run` **296/296 em 86 files** (zero regressao apos snapshot update). **Out-of-scope explicito Plano §8:** S57 Pipes Kanban scroll-snap horizontal mobile + Inbox 3-col→1-col stack + LeadDetail mobile; S58 Tabelas→Cards (`md:hidden table` + `hidden md:block cards`) + Dashboard + Charts (Recharts ResponsiveContainer + Visx ParentSize); S59 Forms 1-col mobile + Wizard Copilot 20+ steps step-por-tela mobile + Settings sections; S60 Playwright mobile viewports + axe a11y AppShell mobile + touch target audit final + hover→focus migration. Honesto vs user: foundation pronta, **app ainda nao e responsivo end-to-end** ate ondas 2-5 entregarem features. **Branch sprint/S56-responsive-foundation nascida de develop atualizada (D012 linear cumulativa preservado).** Commits S56: 1 `feat(frontend)` + 1 `docs(vault)`. **Proxima**: S57 Pipes Kanban scroll-snap mobile + Inbox 3→1 col + LeadDetail mobile. |
| D075 | 2026-04-26 | Sprint S55 — Infra: torna `docker compose up` (dev) bootavel | **Tres fixes minimos pra destravar dev local. Branch sprint/S55-infra-bootable nascida de develop atualizada (D012 linear cumulativa preservado).** (1) **Dockerfile** — `ARG GO_VERSION=1.22` → `1.25`. `go.mod` declara `go 1.25.0` (toolchain bumped pre-S55); build do estagio `builder` falhava com `go: go.mod requires go >= 1.25.0 (running go 1.22.12; GOTOOLCHAIN=local)`. Fix de uma linha. (2) **Dockerfile** runtime stage — distroless `gcr.io/distroless/static-debian12:nonroot` so copiava o binario; `cmd/api/main.go:457-458` carrega spec OpenAPI via `openapi.New("api/openapi.yaml")` lendo o filesystem relativo ao CWD; runtime morria com `fatal: read api/openapi.yaml: open api/openapi.yaml: no such file or directory`. Fix: `COPY --from=builder /src/api /api` + `WORKDIR /` (CWD explicito). (3) **docker-compose.yml** — adicionado `JWT_SECRET: "dev-secret-do-not-use-in-prod-32bytes-minimum-please"` (61 bytes) na env do servico `api`. `internal/config/config.go:267` exige `len(JWTSecret) >= 32` em **todo** ambiente; api saia com `fatal: JWT_SECRET must be set and >= 32 bytes (got 0)`. Valor inline e dev-only e marcado in-place; prod via `.env.production` ou secret manager. **Validacao runtime**: `docker compose up -d` → torque-db (pgvector pg15) healthy → torque-migrate exit 0 (30 migrations) → torque-api `Up` em :8080. `curl /healthz` `{"status":"ok"}`; `curl /readyz` `{"status":"ok","db":"ok"}`. Vite dev :5173 reconecta. **Commits S55**: 1 `chore(infra)` + 1 `docs(vault)`. **Proxima**: S56 — Responsive Foundation Onda 1. |
| D001 | 2026-04-15 | Vault Obsidian criado como fonte de verdade | Substituir vault legado, comecar limpo |
| D002 | 2026-04-15 | 6 ADRs formalizados (OpenAPI, WS, Auth, Cursor, Fonts, Jobs) | Decisoes arquiteturais travadas antes de implementar |
| D003 | 2026-04-15 | 9 agent profiles documentados no vault | Conductor, Architect, Backend, Frontend, DBA, QA, Infra, Automation, AI |
| D004 | 2026-04-15 | Etapa 0 blockers (B1-B6) identificados e 5/6 resolvidos | B6 (ESLint) continua sem regras reais |
| D005 | 2026-04-16 | Plano Mestre de Finalizacao criado (30 sprints) | Roadmap operacional completo ate producao |
| D006 | 2026-04-16 | Vault reorganizado (12 secoes, zero dualidades) | Duas taxonomias paralelas mergeadas em uma |
| D007 | 2026-04-17 | CLAUDE.md + .specs/ + agent skills criados | Protocolos de agentes e skills extraidos do legado e adaptados para Go |
| D008 | 2026-04-17 | ADR-007: Modos de UI Vendedor/Gerente + Task unifica Follow-up + rota /cockpit + claymorphism escopado | (1) Vendedor e Gerente sao modos de UI (preferencia pessoal), nao roles RBAC — invariante `admin/membro/master` preservada. (2) `Task` absorve `Follow-up` (rename sem custo pois backend e 0%) e ganha estado `in_progress` unico por assignee. (3) Rota separada `/cockpit` com shell proprio — evita condicionar AppShell por modo. (4) Preferencia persistida em `users.ui_mode_preference` via `PATCH /me/preferences`. (5) Claymorphism vive apenas sob `.cockpit-theme`, accent gold canonico preservado. (6) Classificacao: extensao do Sistema Base, nao feature F17 vertical — F17 fica como apontador. |
| D009 | 2026-04-18 | Sprint S00 concluido | Sistema Base Frontend fechado. ESLint flat config com regras reais (a11y + hooks + ban `dangerouslySetInnerHTML`), TS strict estendido (`noUncheckedIndexedAccess` + `exactOptionalPropertyTypes`), Prettier + plugin-tailwindcss, Vitest + Testing Library (51 testes, 36 snapshots), GitHub Actions CI, vocabulario canonico em `src/i18n/vocabulary.ts`, `.grain` utility, renomeacao legacy `--clay-*` → `--card-*`/`.tactile-*` eliminando vazamento, pos-login navigation + `useUiMode` + fallback localStorage, `AppError` como classe. Legacy `src/lib/vocabulary.ts` removido (nao importado). |
| D010 | 2026-04-19 | Sprint S01 scaffold Go Backend | Modulo `github.com/milennials/torque-api` com chi + pgx v5 + zerolog + google/uuid. Bootstrap com graceful shutdown, config via env (fail-fast em `DATABASE_URL`), probes `/healthz` + `/readyz`. Middleware stack: RequestID → AccessLog → Recover → SecurityHeaders → CORS → StripOrganizationID (rejeita 400 se body traz `organization_id`). Migrations 0001 (orgs, users, users_master, plans, feature_permissions, `set_updated_at()`, extensions pgcrypto+citext), 0002 (team_members com unique (org,user), member_feature_permissions, org_quotas no delta model, tags, leads com CHECK E.164 + phone_or_email, pipes + pipe_stages + pipe_entries, lead_history, audit_log com target_org_id para impersonacao master), 0003 (ADR-007 conforme §1 de `UI Modes - Vendedor e Gerente`). Dockerfile multi-stage distroless nonroot, docker-compose com Postgres 15 + migrate + API. Makefile com run/test/test-integration/migrate-*/docker-*. OpenAPI 3.1 skeleton em `api/openapi.yaml`. Teste de concorrencia `TestTasksConcurrentInProgressSingleton` (32 goroutines contra o unique partial index `uq_tasks_one_in_progress_per_assignee`, gated por DATABASE_URL e `-short`). |
| D011 | 2026-04-19 | Sprint S02 — Auth + Tenancy + RBAC | **Migration 0004**: `refresh_tokens` com token_hash sha256 (raw token nunca persistido), `replaced_by` rotation chain, `used_at`/`revoked_at`/`revoked_reason`, partial indexes para lookup e expiracao, bind `organization_id` (usuario nao pivota tenant via refresh). **Dominio** (`internal/domain/auth.go`): tipos Role/UIMode/Session/User/Membership/Organization/FeaturePermission. **Services**: `service/password` (bcrypt cost 12 + NeedsRehash + cap 72-byte), `service/jwt` (HS256, `WithValidMethods`/`WithIssuer`/`WithExpirationRequired`, rejeita alg=none, secret >=32 bytes), `service/token` (256-bit rand base64url + sha256 hash + ConstantTimeEqual), `service/permission` (resolver + Bundle com master bypass). **Repositories**: `repository/user` (FindByEmail/FindByID/IsMaster/Memberships/FindMembership/Organization/TouchLastLogin/UpdateUIMode/EffectivePermissions com resolve puro), `repository/refresh` (Issue/Lookup/Rotate em tx SERIALIZABLE/RevokeChain via recursive CTE/RevokeByUser, ErrReused/ErrExpired/ErrRevoked sentinels). **Middleware**: `Authenticator` (transparente; valido=atacha sessao, invalido/ausente=anonimo), `RequireAuth` (fail-closed 401), `TenantScope` (promove org_id do JWT para chave propria), `CSRF` (double-submit sobre cookie nao-httpOnly + header, bypass em safe methods, ConstantTimeEqual), `RequireFeature`/`RequireRole`/`RequireMaster` (cascade master > master_only > admin_only > member_override > default). **Handlers**: `handler/auth` (login com constant-time dummy verify para nao vazar existencia, refresh com rotacao atomica + reuse detection revogando chain, logout best-effort, /me com bundle user+org+permissions+csrf+is_master), `handler/bootstrap` (GET /api/bootstrap publico com cache headers), `handler/preferences` (PATCH /me/preferences com ui_mode ADR-007). **Cookies**: `__torque_session` (httpOnly, SameSite=Strict, Secure=env!=dev, Path=/), `__torque_refresh` (Path=/api/v1/auth, httpOnly), `__torque_csrf` (nao-httpOnly, leitura JS). **Config**: JWT_SECRET (>=32b, fatal no boot), ACCESS_TTL=15m, REFRESH_TTL=720h, COOKIE_DOMAIN/COOKIE_SECURE. **Router** (`cmd/api/main.go`): `/api/v1` → Authenticator → {/auth/{login,refresh,logout}} + grupo autenticado {/auth/me + grupo tenant-scoped {/me/preferences}}. **Tests**: unit em password/jwt/token/CSRF/RBAC (sem DB), integracao em handler/auth (gated por DATABASE_URL: login success + bad password + unknown email + /me 401 sem cookie + refresh rotation + reuse revoga chain + logout limpa cookies + CSRF 403 sem header + StripOrganizationID rejeita body.organization_id). **Deps**: golang-jwt/v5 v5.2.1 + golang.org/x/crypto v0.28.0. Runtime (go mod tidy/build/test) transferido ao usuario pois toolchain nao instalada no workspace. |
| D012 | 2026-04-19 | Protocolo de git: topologia linear cumulativa (regra travada, INVARIANTE) | **Regra:** toda sprint (S00, S01, S02, ...) nasce de `develop` atualizada (ja com S0X-1 mergeada) e volta via PR para `develop`. Historico linear, cumulativo, sem fan-out a partir de S00. Branch `sprint/S0X` permanece no remoto apos merge como ancora de auditoria. **Justificativa (vs fan-out de S00):** sprints de fundacao S01–S06 tem dependencias verticais rigidas — S02 precisa do schema de S01, S04 precisa do auth de S02, S06 precisa de tudo. Fan-out forcaria cada sprint a reimplementar a base e produziria conflitos massivos. Tambem violaria a regra cardinal "uma feature por vez" (CLAUDE.md). **Commits por sprint (ordem obrigatoria):** (1) DBA `feat(db):` migrations/seeds, (2) Backend `feat(backend):` services/repos/middlewares/handlers/config/go.mod, (3) QA `test(backend):` unit+integration, (4) Frontend `feat(frontend):` componentes/hooks/i18n, (5) Docs `docs(vault):` STATE.md novo D0xx + Indice + Plano Mestre ENTREGUE. **Enforcement:** `CLAUDE.md §"Protocolo de git — sprints"` + `.claude/skills/agent-conductor/SKILL.md` Passo 0 + `Plano Mestre §8 Protocolo de Execução`. **Checklist de fechamento inviolavel:** STATE.md `D0xx` + `00 - Indice.md` status line + Plano Mestre §8 ENTREGUE + push `sprint/S0X` + PR para `develop`. Se qualquer item falta, a sprint NAO esta fechada. |
| D034 | 2026-04-20 | Sprint S20 — F09 Analytics (read-only summaries) | **Sem migration** — queries direto nas tabelas de dominio (materialized views ficam para quando pg_stat_statements mostrar queries quentes). **Repositorio analytics**: `Window{Since, Until}` com `normalize()` refusando `since zero` ou `until <= since` (ErrInvalidWindow); `LeadsSummary` (total, in_window, assigned, unassigned — FILTER em count(*) para 4 metricas num round-trip); `StageVolumeByPipe` (active entries GROUP BY stage_id para Kanban counts); `MessagesSummary` (inbound/outbound in window); `TasksSummary` (pending/in_progress/done_in_win/missed_in_win/overdue snapshot); `ProposalsSummary` (funnel sent→viewed→accepted/rejected + won_amount_cents SUM FILTER em accepted_at window); `Leaderboard` via CTE (tm + la=leads_assigned + tc=tasks_completed_in_window + pa=proposals_accepted_in_window) LEFT JOIN em team_members ativos, ORDER BY pa DESC, tc DESC — so retorna membros com contador > 0. **Handler** `/api/v1/analytics/{leads,messages,tasks,proposals,leaderboard,pipes/:id/stages}` member-accessible (read-only), refuse windowless com 400 MISSING_WINDOW ou 400 INVALID_WINDOW. **Frontend** `useAnalytics.ts` com useLeadsSummary/Messages/Tasks/Proposals/StageVolume/Leaderboard, todos staleTime 60s e enabled:Boolean(since). **Test** `useAnalytics.test.tsx` valida URL serialization e enabled=false quando since empty. Wiring no subgrupo tenant-scoped (nao admin). |
| D033 | 2026-04-20 | Sprint S19 — F08 Campanhas (bulk outbound) | **Migration 0013** 2 tabelas + 2 ENUMs: `campaigns` (status draft/scheduled/running/paused/completed/cancelled, template_body CHECK 1..16000, audience_query jsonb, channel_id FK nullable, stats_queued/sent/failed/skipped counters) + `campaign_recipients` (per-lead status queued/sent/failed/opted_out/skipped, FK message_id nullable, UNIQUE (campaign_id, lead_id) para invariante anti-duplicate). Seed de 3 permission keys campaigns.{view,manage,launch}. **Repositorio campaign**: Create (com ownership-check do channel_id se informado); Get/List com filtro de status; Launch em tx Serializable — valida campaign status ∈ {draft, scheduled}, bulk-insert recipients via `unnest($3::uuid[])` + ON CONFLICT DO NOTHING (dedup natural), flip campaigns.status='running' + stats_queued, retorna count; SetStatus (pause/resume/cancel); RecordRecipientSent/Failed em tx que atualiza recipient + bumpa stats_{sent,failed} + decrementa stats_queued via GREATEST(-1,0); ListRecipients cap 500. **Handler** `/api/v1/campaigns/*` CRUD + launch/pause/resume/cancel + recipients list. Launch cap 10k leads por chamada (413 TOO_MANY_RECIPIENTS acima disso). Eventos `campaign.{created,launched,paused,resumed,cancelled}`. **Frontend** `useCampaigns.ts` com useCampaigns/Campaign/Recipients + useCreateCampaign/Launch/Pause/Resume/Cancel, WS invalidate em todos os campaign.*. **Wiring** subgrupo admin-only (RequireRole). Worker kind `campaign.dispatch` fica para sprint posterior. Tests pendentes. |
| D032 | 2026-04-19 | Sprint S18 — F07 Workflow Builder Frontend hooks | `useWorkflows.ts` com `useWorkflows/useWorkflow/useCreateWorkflow/usePublishWorkflow/usePauseWorkflow/useArchiveWorkflow` + `useWorkflowSteps/useCreateStep/useUpdateStep/useDeleteStep/useSetEntryStep` + `useWorkflowRuns/useEnqueueRun/useCancelRun`. WS subscribe em workflow.{created,published,paused,archived,entry_set}, workflow_step.{upserted,deleted}, workflow_run.{enqueued,cancelled} → invalidate do escopo certo. Tests (`useWorkflows.test.tsx`) cobrem list/createStep/enqueueRun URL targeting. DAG editor (React component) fica para sprint posterior; S18 entrega apenas a camada de hooks. |
| D031 | 2026-04-19 | Sprint S17 — F07 Workflow Builder Backend | **Migration 0012** 4 tabelas + 4 ENUMs: `workflows` (trigger=manual/lead_created/lead_stage_changed/message_inbound/schedule, status=draft/active/paused/archived, entry_step_id FK), `workflow_steps` (kind=send_message/wait/create_task/branch/update_lead/call_agent/http, config jsonb, next_step_ids uuid[] para branching), `workflow_runs` (status=pending/running/succeeded/failed/cancelled, current_step_id), `workflow_run_steps` (per-step trace). Seed de 3 permission keys: workflows.view/manage/run. **Repositorio workflow**: Create/List/Get/SetStatus; UpsertStep com ownership-check (workflow pertence ao tenant), ListSteps/DeleteStep, SetEntry (valida step ∈ workflow via EXISTS subquery); EnqueueRun refuse se workflow.status != active OR entry_step_id IS NULL (ErrInvalidState → 409 WORKFLOW_NOT_ACTIVE), ListRuns (ordered DESC by created_at, cap 200), CancelRun (pending|running → cancelled). **Handler** `/api/v1/workflows/*` CRUD + publish/pause/archive + steps CRUD + entry set + runs enqueue/list/cancel. Eventos `workflow.{created,published,paused,archived,entry_set}` + `workflow_step.{upserted,deleted}` + `workflow_run.{enqueued,cancelled}`. **Wiring**: handler montado no subgrupo admin-only (RequireRole(admin)) ao lado de agents/proposals. LLM runtime pick-up por worker kind `workflow.execute` fica em sprint posterior — S17 entrega o data plane + state machine. |
| D074 | 2026-04-24 | Sprint S53 Fase H — Onda 1+2+3 executadas em paralelo (~10 agentes): workflow engine real + AI safety + DBA cleanup + backup/DR + quota wiring + impersonation cookie swap + api.gen.ts regen | **Execução conduzida pelo Conductor em 3 ondas paralelas após D073.** **Onda 1 (4 agentes simultâneos)**: (A) **workflow engine real** (`service/workflow/*` reescrito, ~1000 LOC): 7/7 action handlers implementados com side-effects reais (SendMessageAction com `{{lead.*}}` placeholder render + messaging provider + inbox persist, UpdateLeadAction via leadrepo.Update + bus publish, WaitAction com ErrSuspend sentinel + __suspend:rfc3339 marker pra real suspension/resume via next_retry_at, BranchAction preservado, CreateTaskAction com assignee resolution + ErrNotFound/ErrAssigneeBusy→non-retryable, CallAgentAction abre agent_sessions row + kill-switch check, HTTPRequestAction com SSRF guard completo via net.LookupIP bloqueando RFC1918 + loopback + link-local + metadata hosts + literal denylist + 1MiB body cap + no-redirect policy + 4xx→non-retryable 5xx/429/timeout→transient). Error taxonomy via sentinels ErrTransient/ErrNonRetryable/ErrSuspend. Executor com backoff `[15s, 1m, 5m, 30m, 2h]` + DLQ em workflow_run_failures. Runner com watchdog (ReclaimOrphanedRuns 10min stale) em goroutine paralela. Migration 0028 (attempts/max_attempts/next_retry_at em workflow_runs + tabela workflow_run_failures com attempt trail + partial idx retry-scheduled). 22+ tests novos (dispatcher_test, actions_s45_test com SSRF rejection table 8 cases, executor_retry_test, runner_watchdog_test). main.go wired com full Deps (pool/bus/leads/tasks/agents/inbox) + NewExecutorWithBus + NewRunnerWithBus pra bus event publishing. **Messaging=nil intencional** — per-tenant Evolution selector vai em S54. (B) **AI safety stack completa** (6 entregáveis): migration 0031 `plan_quotas` seeds `ai_tokens`/`tts_seconds` × free/growth/enterprise (50k/500k/5M tokens; 300s/3600s/36000s TTS) + backfill transacional org_quotas; `service/ai/pii/scrub.go` pacote puro Go com regexes BR (CPF/CNPJ/phone/email/credit card) + ScrubRAGContext([]Chunk) + 20+ tests table-driven; `httpx/middleware/ai_quota.go` RequireAITokenBudget + RequireTTSBudget (admission-only sem pre-increment, fail-closed, master bypass, 402 canônico AI_QUOTA_EXCEEDED/TTS_QUOTA_EXCEEDED PT-BR) + 6 tests; `service/ai/runtime_registry.go` Registry com Register/CancelAll/ActiveCount/TotalActive (mutex-guarded, cancels released fora do lock pra evitar deadlock) + 6 tests concurrency-safe; `service/ai/trigger/subscriber.go` bus subscriber que traduz message.received/conversation.created → AssignAgent via matcher + publica conversation.agent_assigned + Start/Shutdown lifecycle mirror workflow.BusSubscriber + 7 tests; `agent.go` repo com PersistPlaygroundTurn (tx único user+assistant rows com token counters); `playground.go` wired com PII scrub em every user msg + retrieved RAG chunk + structured `ai.pii_scrub` log + registry.Register-on-dial + finalizeStream persiste turn + IncrementUsage(ai_tokens) + ttsPreview IncrementUsage(tts_seconds) (chars÷15 ≥1); `agents.go` com WithRegistry + killSwitch/disable chama registry.CancelAll(agentID). main.go com aiRegistry singleton + triggerSub Start/Shutdown. 43+ tests cross 5 files. (C) **DBA migrations cleanup**: 0029 drop `knowledge_chunks.embedding_text` legacy column (backfilled ao vector pós-S39) + meetings UNIQUE global → compound (organization_id, external_provider, external_id) via uq_meetings_org_external DEFERRABLE INITIALLY IMMEDIATE (remove risco de colisão cross-tenant em GCal shared event IDs); 0030 extension pg_trgm IF NOT EXISTS + GIN parcial trigram indexes `idx_leads_name_lower_trgm` (gin LOWER(name) gin_trgm_ops WHERE deleted_at IS NULL) + email/phone variants alinhados com o LIKE LOWER($N) que o repo lead.go:86 usa + `idx_messages_body_trgm` (provisionado pra search de conversation history em S53+). Justificativa de alinhamento expression-index vs repo query no header do 0030. (D) **Backup/DR runbook + scripts** (~936 LOC total, 5 novos arquivos): `.specs/runbooks/backup-restore.md` (176 linhas) com RPO 24h/RTO 1h/retention 30d daily + 12m monthly cold storage, cadência diária 03:00 UTC, procedure automated via GitHub Actions + manual restore gated, ownership + escalation path; `.specs/runbooks/key-rotation.md` (170 linhas) com tabela de 9 chaves críticas + procedure especial pra INTEGRATION_ENCRYPTION_KEY (dual-key + re-encrypt worker — bloqueia S53 backend ticket antes primeira rotação) + incident response em leak suspeito; `scripts/backup-postgres.sh` (pg_dump custom format compress=9 + S3/B2/R2/Spaces upload opcional via AWS CLI), `scripts/restore-postgres.sh` (pg_restore --clean --if-exists --single-transaction + guardrail ENV=production refuse sem --confirm-prod), `scripts/verify-backup.sh` (3 invariantes de integridade sem DB); `scripts/README.md` (93 linhas); `.github/workflows/backup.yml` (128 linhas, cron '0 3 * * *' + workflow_dispatch + postgresql-client-15 + AWS CLI + retention cleanup 30d + Sentry webhook notify on failure, 60min timeout). `.env.example` enriquecido com comentários de rotation em 9 chaves apontando runbook. **Onda 2 (3 agentes paralelos)**: (E) **quota wiring members + workflows**: `handler/members/members.go` com quotaGate narrow interface + WithQuota setter + RequireQuota em POST /members condicional + IncrementUsage(+1) pós-add + bidirectional tracking em update (snapshot priorActive via repo.Get + delta ±1 só em real is_active transition — idempotente PATCHes não bump) + IncrementUsage(-1) em deactivate; `handler/workflows/workflows.go` mesmo pattern mas lifetime count (archive reversível = sem reclaim, documented in godoc). main.go wired com `.WithQuota(quotaRepo)` reusando instância S51. 10 tests (5 members + 5 workflows) cobrindo admit/cap-402/fail-closed-ErrNotFound/master-bypass/nil-safe-typed. (F) **quota wiring agents**: `handler/agents/agents.go` com quota `*quotarepo.Repository` + WithQuota setter + RequireQuota(ResourceAgents) em POST /agents + IncrementUsage(+1) pós-createAgent + IncrementUsage(-1) em setStatus→disabled **sequenciado DEPOIS do registry.CancelAll** do Onda 1 (preservado). main.go linha 572 chain `.WithRegistry(aiRegistry).WithQuota(quotaRepo)`. agents_quota_test.go com 6 cenários + sanity checks Routes() monta sem panic em ambos quota=nil e WithQuota(nil). **Recurso `agents` ≠ `ai_tokens`**: primeiro gata slots (quantos agents um tenant cria), segundo gata tokens consumidos (quanto o LLM processa). (G) **impersonation cookie swap atômico**: `handler/master/master.go:163-285` reescreve `impersonate` handler removendo 4 linhas silencers legais (_=h.jwt, _=h.cookieSecure, _=h.cookieDomain). Flow: parse id → impResolver.ResolveImpersonation(target) → 404 ErrNotFound ou 400 InvalidOrgID antes do audit → auditRepo.Append(ActorType=master, Action=master.impersonation_start, TargetOrgID=&targetOrgID, RequestID=mw.RequestIDFrom(ctx)) → **audit-fail-closed D062 invariant preservado** (500 AUDIT_FAILED sem set cookie) → on audit sucesso h.jwt.Issue(sub=masterUserID, claims: OrgID=targetOrgID, TeamMemberID=targetTMID, Role=admin, IsMaster=true) → http.SetCookie exatamente mirror de auth.setSessionCookie (nome `__torque_session`, HttpOnly=true, Secure=h.cookieSecure, SameSite=Strict, Path=`/` mirrando pattern existente não /api conforme spec erroneamente sugeria, Domain=h.cookieDomain, TTL 15min via jwtsvc.Issue guard — sobre-entrega disciplina vs 30min spec request pois jwtsvc.New enforça ttl<=1h). **Narrow interfaces locais** no master package (impersonationResolver/auditAppender/jwtIssuer) satisfeitas implicitamente por `*masterrepo.Repository`/`*audit.Repository`/`*jwtsvc.Service` permitindo unit tests sem pgxpool **sem mudar cmd/api wiring**. 5 scenarios master_impersonate_test.go: audit fail refuses token + cookie NOT set, happy path cookie shape + JWT parses + claims correctos, cookieDomain config respected (empty vs set), target resolver ErrNotFound → 404 antes do audit (audit só acontece pra org resolvida), malformed UUID → 400 resolver never called. Audit row includes RequestID (antes missing). **Onda 3 (direto na sessão)**: (H) **api.gen.ts regenerated**: `npx openapi-typescript ../torque-api/api/openapi.yaml -o src/contracts/api.gen.ts` contra arquivo estático (backend não precisa rodar). Arquivo **7 linhas → 4398 linhas**, 100% dos 1709 linhas do OpenAPI 0.52.0 tipados. S52 deferred FECHADO. **Validação final frontend**: `tsc --noEmit` verde, `eslint . --max-warnings 0` verde, `vitest run` **296/296 passam em 86 files** pós-regen (contract safety desligada → ligada com zero regressão). **Migrations** finais: 0001-0026 (histórico) + 0028 workflow retry + 0029 knowledge/meetings cleanup + 0030 search trigram + 0031 AI budget quotas = **30 migrations total** (gap no 0027 foi reservado implicitamente pra future; não é blocker). **Contagem total S53 Onda 1+2+3**: 4 migrations, ~3500 LOC Go novo, ~80 tests novos/estendidos cobrindo AI safety + quota wiring + workflow real + impersonation, 1 runbook backup (176L) + 1 runbook key-rotation (170L) + 3 scripts bash + 1 GitHub Actions workflow, 1 regen api.gen.ts massivo. **Bugs bloqueadores da D073 fechados**: (1) FK 0022 migration — fechado em D073. (2) Build fail busSub main.go:207 — fechado em D073 (rename wfSub) + main.go agora wires NewExecutorWithBus/NewRunnerWithBus. (3) Workflow engine é teatro — **FECHADO em D074 Onda 1 (A)**: 7 handlers com side-effects reais + retry/DLQ + watchdog. **Gate §10 movido**: (a) pentest + k6 staging + game day ainda pending (requer staging provisionada — próxima ação do Conductor aguarda provisioning humano por VPS Hostinger + Postgres managed + env vars); (b) Go coverage ≥75% pending runtime host; (c) security-scan 7d green pending; (d) dual-review money-flow **FECHADO** (CODEOWNERS criado D073). (e) Paridade funcional v8 **revisitar D045** — workflows real + quotas completos + api.gen.ts + impersonation funcional + PII/budget AI safety devem re-elevar acima de 95%. **Runtime validation pendente (CI/host com Go)**: go build ./... + go vet + gosec -severity=high + govulncheck + go test -race -coverprofile em packages críticos (service/billing, service/jwt, service/permission, httpx/middleware, repository/lead, repository/quota, service/workflow, service/ai). **Commits pendentes** organizados por domínio (DBA → Backend → QA → Frontend → Docs) conforme CLAUDE.md protocol. **Deferreds honestos**: (a) Messaging per-tenant selector no workflow dispatcher (Evolution/SZ.Chat credential resolution por channelID) → S54; (b) Schedule (cron) trigger → S54; (c) Asaas boleto + cartão → pós-prod; (d) workflow_run_failures admin endpoint GET /workflows/:id/runs/:run_id/failures → sprint cleanup; (e) Frontend upsell banner copy para AI_QUOTA_EXCEEDED/TTS_QUOTA_EXCEEDED → S54 frontend-focus; (f) F02 Countdown + F03 HeatSlider signature UI → S54+; (g) Meta Lead Ads relay orchestration + SZ.Chat inbound webhook → S54; (h) DNS lookup sincrono em validateHTTPSURL SSRF guard (aceita hoje, resolver com timeout explícito landa depois); (i) backoffFor ladder per-tenant configuração → fine-tune sprint; (j) full-stack SSE integration test em playground (requer live pgxpool) → integration tier; (k) buildLeadFacts no trigger/subscriber atualmente só projeta evt.Patch em Custom — hydration de lead facts completo (origin/segment/UTMs/tags) requer leads-repo fetch em S54. **Próxima ação Conductor**: commits organizados por domínio + atualização Plano Paridade v8 §8 marcando S53 ENTREGUE + atualização 00-Indice.md + abrir PR sprint/S52→develop (ou renomear a branch pra sprint/S53 via git branch -m se topologia exigir) + aguardar aprovação humana pra provisioning de staging e execução do Gate §10 restante. |
| D073 | 2026-04-24 | Auditoria pré-produção pós-S52 — gate §10 NÃO FECHADO + 3 bugs duros corrigidos; Sprint S53 (Fase H — Production Gate Verification) inaugurada | **Auditoria executada pelo Conductor com 8 agentes especialistas em paralelo** (architect, backend, frontend, dba, qa, infra, automation, ai) no fechamento do roadmap S30-S52. **Veredito: NOT READY.** Gate §10 placar real: **1 done (vitest 78.48 lines), 1 partial (dual-review sem CODEOWNERS), 4 pending (paridade v8 revisit, k6 staging, pentest staging, game day), 2 unknown (Go coverage, security-scan 7d verde)**. Diferença crítica vs STATE D072: três bugs bloqueadores descobertos nesta auditoria que não estavam documentados. **BUG #1 — DB clean-boot falha**: migration `0022_performance.up.sql:67` referenciava `REFERENCES proposals(id)` (tabela inexistente; correto é `pipe_proposals(pipe_entry_id)` PK 1:1). Qualquer ambiente novo explodia no `migrate up`. Corrigido in-place (não shipped em prod, edit é seguro) preservando nome de coluna `proposal_id` pra alinhar com API JSON. **BUG #2 — Backend não compilava**: `cmd/api/main.go:207` redeclarava `busSub :=` já declarado em `:162` (primeiro é `<-chan event.Event`, segundo é `*workflowsvc.BusSubscriber`). `go build ./...` falhava. Corrigido renomeando segundo para `wfSub`. **BUG #3 — Workflow engine é teatro**: 7/7 action handlers são stubs literais retornando `sent:false / applied:false / created:false / attached:false` (`dispatcher.go:149`, `actions_s45.go:41,73,109`). UI marca run como `succeeded`, mundo real recebe zero efeito. Workflow-driven automation está 100% no-op. Não corrigido — é 1 sprint inteira de trabalho. **Gaps consolidados (priorizados)**: (1) workflow handlers reais Evolution/leadrepo/taskrepo/agent 20-30h BLOCKER; (2) workflow retry+DLQ 12-16h (`workflow_runs.attempts` missing); (3) agent trigger dispatcher ausente 4h (matcher órfão); (4) budget cap AI ausente — sem `org_quotas.ai_tokens_per_month`, prod queima caixa em loop de agent 4-8h BLOCKER; (5) PII scrub pré-LLM ausente — dados de lead (CPF/telefone/email) indo cru pra OpenRouter/Gemini, LGPD violation risk 4-6h BLOCKER; (6) quota wiring em team_members/workflows/agents — só /leads gated (pattern drop-in pronto 6h) BLOCKER; (7) `api.gen.ts` é 7-line stub apesar de OpenAPI 0.52.0 ter 1709 linhas — contract safety desligada 2-4h; (8) backup/DR ZERO — sem pg_dump cron, sem restore runbook, sem retention policy, sem RTO/RPO target 1-2d BLOCKER; (9) zero E2E — produto multi-canal WebSocket sem teste ponta-a-ponta 2-3d; (10) rate-limiter in-memory single-pod 1d; (11) impersonation cookie swap atômico 3-4h (S52 deferred); (12) OpenTelemetry prometido mas não wired — spec diz OTel, runtime só Sentry+zerolog; (13) meetings UNIQUE sem org_id 10min (colisão cross-tenant potencial); (14) trigram GIN em messages.body + leads.name/email/phone 50min (alto risco >10k msgs/tenant). **Corrigidos nesta sessão (2026-04-24)**: (A) bug #1 migration 0022 FK; (B) bug #2 build `busSub`; (C) `.env.example` defasado — adicionadas 15+ vars S39-S52 (BILLING_PROVIDER, BILLING_WEBHOOK_SECRET, ASAAS_API_KEY, ASAAS_BASE_URL, OPENROUTER_*, GEMINI_*, ELEVENLABS_*, INTEGRATION_ENCRYPTION_KEY, INTEGRATION_STATE_SECRET, GOOGLE_OAUTH_*, TINYERP_BASE_URL, META_GRAPH_BASE_URL, META_APP_SECRET, SZCHAT_BASE_URL, LEAD_WEBHOOK_SECRET); (D) link quebrado `FunisHubPage.tsx:58` `/funil/:id` → `/pipe/:id`; (E) Sidebar badges hardcoded removidos (`24` leads, `9` conversas, "Plano Growth 64% / 3.200 / 5.000 leads este mes"); (F) `.github/CODEOWNERS` criado cobrindo billing/quotas/auth/master/jwt/crypto/permission/migrations/runbooks com placeholders `@highermind/*` teams (dual-review §10 enforceable). **Wins reais** (auditoria reconhece): frontend 78% coverage typecheck+lint zero-warning, infra S-tier cosign keyless+verify+migrate gating+distroless nonroot+read-only FS, schema 40 tabelas 100% FK ON DELETE explícito pgvector HNSW, 145 endpoints 27 handlers 27 repos 11 services, security invariantes exemplares (StripOrganizationID strict 400, audit-fail-closed master impersonate, HMAC constant-time lead webhook). **Docs atualizadas**: `Torque-dir-new/09 - Backlog/Auditoria Pre-Producao S52 - 2026-04-24.md` (relatório completo), `Plano S53 - Fase H Production Gate Verification.md` (roadmap concreto). **Sprint S53 (Fase H — Production Gate Verification)** inaugurada: não é sprint de feature, é sprint de evidência empírica. Transforma "entregue em código" em "rodando em realidade". Critério de saída: 8/8 gates §10 com evidência, staging deployed cosign-verified, pentest clean dual sign-off, k6 p95<500ms measured, backup+restore drill, game day registrado, Go coverage ≥75% pkgs críticos medida e publicada. Próxima: **S53 — Fase H Production Gate Verification (~10 dias úteis)**. |
| D072 | 2026-04-22 | Sprint S52 — Fase G.2 FINAL (hardening) + FASE G CONCLUÍDA + ROADMAP S30-S52 COMPLETO | **Última sprint do roadmap Paridade v8.** Foco: supply chain signing + spec refresh + security gates + load test + pentest runbook. **Release workflow** (`release.yml`): adicionado `permissions: id-token: write` (OIDC token mintado pela GitHub Action é o que Sigstore/Fulcio vincula no transparency log); `sigstore/cosign-installer@v3` pinado `v2.4.1`; cada build step agora tem `id:` pra exportar o digest; novo step `Sign <api|web> image` roda `cosign sign --yes ghcr.io/<repo>/<image>@<digest>` — assinatura é **por digest**, nunca por tag (tag pode ser sobrescrita pós-push, digest é imutável). **Deploy workflow** (`deploy.yml`): novo job `verify` roda **antes** de migrate + deploy com `sigstore/cosign-installer@v3` e `cosign verify --certificate-identity-regexp "^https://github.com/<repo>/.github/workflows/release.yml@refs/tags/<tag>$" --certificate-oidc-issuer https://token.actions.githubusercontent.com` — pins a identidade do workflow emissor pra que um fork de `release.yml` não produza signature que passe no gate. `needs: [verify]` em migrate + deploy + smoke; `if: needs.verify.result == 'success'` em deploy. `permissions: id-token: write` adicionado ao deploy.yml também (verify precisa do token pra cross-check com Sigstore). **Makefile**: novo alvo `security` roda `go vet + gosec -severity=high -confidence=medium + govulncheck` localmente (comentário documenta `go install github.com/securego/gosec/v2/cmd/gosec@latest && go install golang.org/x/vuln/cmd/govulncheck@latest` pra install one-shot). **k6 load test** (`api-baseline.k6.js`): adicionados `/api/v1/quotas` e `/api/v1/integrations` no batch de dashboard (reflete o que frontend realmente fetcha em Plano+Integrações tabs); 10% mix `GET /integrations/meta/ads-insights?date_range=last_7d` aceita 200 (cached) OU 412 (not connected) — ambos expected em staging; 5% mix `POST /leads` medindo custo do admission middleware quota pra gate regression quando o RequireQuota DB hit piorar; trends novos `quota_lookup_latency_ms` e `meta_insights_latency_ms`; thresholds `p95<500`, `p99<1500`, `http_req_failed<0.01`, `checks>0.99` permanecem hard gates que falham o run se tripped. **Pentest runbook** (`.specs/security/pentest-runbook-S52.md`, ~350 linhas): **10 checks executáveis** mapeando a `security-hardening-audit-2026-04-20.md §Pentest Checklist` com receitas exatas (curl + openssl dgst + jq + wscat). Pre-reqs: staging deployed no tag exato (não develop/SHA), cosign verify passa localmente, dois test tenants + master user seeded, header `X-Torque-Pentest` pra tag synthetic sessions. (1) **Rate limiter burst** — 40 requests em <1s, expect pelo menos um 429 com Retry-After, zero 5xx. (2) **Cross-tenant leak** — beta hitting alpha's lead UUID expect **404 não 403** (403 confirmaria existência em outra org). (3) **JWT tampering** 3 variantes: alg=none, org_id swap, expired token re-signed com chave errada — todos 401 + audit entry. (4) **CSRF**: missing X-CSRF-Token + mismatch cookie/header → 403 × 2 com canonical codes CSRF_MISSING_HEADER / CSRF_MISMATCH. (5) **Webhook replay** (S24 D039): mesma `event_id` posted duas vezes → 200 + 200, subscription row transiciona UMA VEZ, `billing_events` tem 1 row com `provider_event_id`. (6) **Lead webhook HMAC** (S50): sig válida → 201; replay mesma external_id → 200 (idempotente dedup); body tampered → 401; prefix downgrade (`<hex>` sem `sha256=`) → 401. (7) **Quota enforcement** (S51): master drops beta leads cap a 1 via PATCH, primeiro lead cria, segundo retorna **402 Payment Required** com body `{error:{code:QUOTA_EXCEEDED}}` + headers `X-Quota-Resource/Limit/Usage/Remaining`. (8) **Master impersonation audit integrity** (D062 invariant): REVOKE INSERT on audit_log, master impersonate target_org → 500 AUDIT_FAILED, session NÃO convertida (atomic refusal). (9) **WebSocket origin**: wscat com `Origin: https://evil.example` (fora CORSOrigins) → close 4403 ou 403 no upgrade. (10) **Frontend XSS bundle**: grep bundle built por `dangerouslySetInnerHTML|innerHTML=` → output "clean" (ESLint rule bans project-wide). Findings vão em GitHub issues (label `security` + `severity:{low,medium,high,critical}` + `sprint/S52`), agregados em `.specs/security/pentest-staging-<YYYY-MM-DD>.md` usando `pentest-staging-template.md` (header com image digests cosign-verified + check results table + findings blocks + follow-ups + dual sign-off). Pentest considerado **clean** apenas com: zero high/critical open + mediums com workaround documentado + 2 sign-offs (security + QA). **OpenAPI spec refresh** (`api/openapi.yaml`): version bump **0.1.0 → 0.52.0**, line count **341 → 1709** (+1486/-117), path count **~13 → ~80**. Tags novas (22 added): leads, pipes, confirmations, meetings, tasks, inbox, templates, campaigns, workflows, agents, billing, quotas, integrations, webhooks, analytics, performance, products, members, proposals, settings, onboarding, master. Hot-path endpoints carregam request/response schemas completos (`Lead`, `LeadPage`, `LeadCreateRequest`, `LeadPatchRequest`, `Subscription`, `CheckoutRequest`, `Quota`, `IntegrationCredential`, `MetaAdAccountInsights`, `LeadWebhookPayload`); lower-traffic endpoints carregam operationId + summary + security + response codes mínimos — spec é o mapa, handler é o manual. Responses novas: `InvalidBody` 400, `NotFound` 404, `QuotaExceeded` 402 com body canônico `{error:{code:QUOTA_EXCEEDED, message, details:{resource, limit, current_usage, remaining}}}` + headers `X-Quota-{Resource,Limit,Usage,Remaining}`. Public webhooks incluem `/webhooks/billing/webhook` (shared secret), `/webhooks/billing/asaas` (S51 normalized), `/webhooks/lead` (HMAC-SHA256 + X-Torque-Tenant + 413 body-cap + 503 disabled). OAuth callback flagged CSRF-exempt com state HMAC enforced. SSE endpoint `agentPlaygroundMessage` retorna `text/event-stream`; TTS preview retorna `audio/mpeg`. openapi-typescript regen pode substituir `src/contracts/api.gen.ts` sem o gap S04-era (deferred pra post-prod uma vez que não bloqueia o deploy). **Acceptance criteria** (runtime GitHub Actions + Go host pendente): (1) docker images em GHCR assinadas + verificáveis via `cosign verify` keyless; (2) deploy.yml bloqueia migrate/deploy quando signature verify falha; (3) gosec zero HIGH findings no CI (gate ativo desde S32); (4) pentest runbook executado contra staging uma vez que ambiente esteja provisionado (findings em `.specs/security/pentest-staging-*.md`); (5) k6 run real contra staging com novas rotas S50/S51 incluídas + p95<500ms. **FASE G CONCLUÍDA** (S51→S52, 2 sprints, 2 migrations S51+0026, 0 migration S52). **ROADMAP S30-S52 COMPLETO** — 23 sprints em ~28 semanas, paridade funcional v8 atingida, 7 fases (A Foundation repair + B UX gap + C IA/Copilot + D Automação + E Produto + F Integrações + G Hardening). **Gate Go-to-Production aberto** conforme `Plano Paridade v8 §10`: paridade funcional ≥95% vs v8 (aguarda D045 revisit), cobertura vitest ≥70% lines (atingido 78.48/75.30/74.75/62.22 em S49), cobertura Go ≥75% em packages críticos (pending runtime host), zero HIGH gosec+govulncheck+Trivy 7 dias consecutivos (green em security-scan.yml), k6 p95<500ms em staging, pentest clean com dual sign-off, dual-review em money-flow PRs (S51 já exige por design — S24 billing, S51 Asaas), runbook rodado em dry-run. **Tests backend** não tocados — S52 é infra/config/spec/ops, não runtime code. **Tests frontend** 296/296 em 86 files mantido vs S51 (zero regressão); `tsc --noEmit` + `eslint --max-warnings 0` verdes. **Deferreds pós-prod explícitos**: impersonation cookie-swap atômico com audit row (D044 A04 review, baixo risco — atual já refuses impersonation em audit fail); openapi-typescript regen + api.gen.ts diff no frontend (incremental); deep schemas nas ~50 paths minimal (incremental); k6 run real contra staging ambient (operator task); pentest execution contra staging (requires provisioning). **Commits**: `85f2df9` infra+ops (cosign + deploy verify + make security + k6 + pentest runbook) · `30d9aa5` openapi. Branch `sprint/S52` → merge `--no-ff` para develop via PR. Próxima: **pós-roadmap — operação contínua + release cadence**. |
| D071 | 2026-04-22 | Sprint S51 — Fase G.1 Asaas real + dual-review + quota enforcement runtime | **Primeira sprint da Fase G (Hardening produção).** Transforma o MockProvider de S24 D039 num `billing.Provider` real contra Asaas v3 REST + ativa o runtime de `org_quotas` que dormia no schema desde S02. **Migration 0026** (`0026_s51_plan_quotas.up.sql`): tabela `plan_quotas` (plan_id FK→plans, resource_key CHECK regex, plan_base>=0) fecha o elo faltante entre `plans` (S24) e `org_quotas` (S02); seeds `free/growth/enterprise × leads/team_members/workflows/agents` com plan_base tierado (free: 100/2/2/1 · growth: 5000/10/25/5 · enterprise: 100k/500/1000/100 "efetivamente ilimitado mas finito pra delta math sã"). Backfill transacional de `org_quotas` com CTE `org_plan` que prefere subscription live (pending|active|past_due) → terminal mais recente → free fallback pra que tenant pré-S51 não 402e em migration day. Seeds `quotas.view` permission (default=true, observability não é gated). **AsaasProvider** (`service/billing/asaas.go`, ~280 LOC): implementa `billing.Provider` + `WebhookNormalizer`. **Dual-review tripwire** em `NewAsaas(cfg, httpClient)`: retorna error em `cfg.APIKey == ""` — main.go cai no `billing.NewMock()` com log WARN mesmo se `BILLING_PROVIDER=asaas` estiver setado. BaseURL default `https://sandbox.asaas.com/api/v3` (safer fallback pra dev+staging); prod explicit-seta `https://api.asaas.com/v3`. `CreateCharge` pipeline: (1) POST /customers com externalReference=org_id (Asaas dedup cross-call, não precisamos cachear customer_id local), (2) POST /payments billingType=PIX + dueDate=+30min + description="Torque CRM — <plan>", (3) GET /payments/:id/pixQrCode retorna encodedImage+payload+expirationDate. `CancelCharge` DELETE /payments/:id; 404 tratado como sucesso idempotente (charges rolam off retention antes de chamarmos cancel). `call()` helper taxonomy: 401→"unauthorized (check api_key)", 429→"rate limited", 5xx→"server error", 404→ErrNotFound, 4xx outros→http-code+truncated-body. Header `access_token: <key>` + `User-Agent: torque-crm/1.0`. **NormalizeAsaasWebhook** mapea payload rico: PAYMENT_CONFIRMED/RECEIVED→charge.paid, OVERDUE→charge.overdue, DELETED/REFUNDED→charge.cancelled, unknown→`asaas.<event>` prefixado (billing_events log mantém audit sem state machine acionar). `EventID` composto `<EVENT>:<payment.id>` pra dedup correto entre retries do provider cobrindo a mesma transição. **Quota infra** nova (3 packages): (1) `repository/quota/quota.go`: `Get(orgID, resource) Quota`, `List`, `IncrementUsage(delta)` com `GREATEST(current_usage+$3, 0)` clamp (usage é scalar "slots consumidos" não ledger accounting; clamp evita CHECK trip em decrement), `SeedPlanDefaults(orgID, planID)` (INSERT SELECT de plan_quotas com ON CONFLICT DO UPDATE refreshing apenas plan_base, preservando addons/adjustment/usage), `SetAdminAdjustment`, `SetPurchasedAddons`. Constants `Resource{Leads,TeamMembers,Workflows,Agents}` mirror da regex CHECK. `Quota` struct carrega campos derivados EffectiveLimit (max(0, base+addons+adj)) + Remaining (max(0, limit-usage)) computados em `toQuota` uma vez. (2) `httpx/middleware/quota.go`: `QuotaReader` interface narrow (Get-only) pra testes sem pgxpool; `RequireQuota(reader, resource)` factory panics em resource vazio (build-time guard). Runtime: (i) `SessionFrom(ctx)` 401 se ausente, (ii) `sess.IsMaster` short-circuit antes de Get (master dirige tenants impersonados sem inflar usage), (iii) `OrgIDFrom(ctx)` 401 se ausente (defensivo; TenantScope precede), (iv) `reader.Get` → ErrNotFound fail-closed 402 (missing row força provisioning explícito em vez de silent pass), (v) `q.CurrentUsage >= q.EffectiveLimit` → 402 QUOTA_EXCEEDED body JSON `{error:{code,message PT-BR,details:{resource,limit,current_usage,remaining}}}`, (vi) admit emite headers `X-Quota-{Resource,Limit,Usage,Remaining}` pro frontend pulsar upsell banner sem roundtrip. 402 status = Payment Required (reclaim do "future use" HTTP/1.1 spot por convenção SaaS — tenant PODE desbloquear upgrading/addons). (3) `handler/quotas/quotas.go`: `MemberRoutes(r)` monta GET /quotas + GET /quotas/:resource (list + detail); `MasterRoutes(r)` monta PATCH /quotas/:resource com body `{admin_adjustment?, purchased_addons?}` (pelo menos um obrigatório) re-lendo a row pra retornar estado atualizado. **Leads wiring cirúrgico** (`handler/leads/leads.go`): `WithQuota(q)` opcional encadeável; `Routes(r)` condicionalmente aplica `r.With(mw.RequireQuota(h.quota, quotarepo.ResourceLeads)).Post("/leads", h.create)`, outros verbs (GET/PATCH/DELETE) passam livres (read não consome cap). Handler `create` chama `h.quota.IncrementUsage(+1)` após repo.Create sucesso (ignore return; fire-and-forget); `softDelete` chama `IncrementUsage(-1)` pra reclaim. Race-window entre middleware check e IncrementUsage é bounded pelo rate limiter — worst-case tenant vira one-over-cap, indistinguível de clicks concorrentes. Pattern drop-in pra team_members/workflows/agents em sprint futura — middleware+repo+clamp+headers já estão prontos. **Billing webhook upgrade**: `WebhookHandler.WithQuotaRepo(quotaRepo)` opcional; `charge.paid`/`payment.confirmed` → `seedQuotas(orgID, subID)` helper que lê `repo.Get(orgID, subID)` pra descobrir o plan_id atual e chama `quotaRepo.SeedPlanDefaults` — upgrade free→growth aplica o novo effective_limit no mesmo roundtrip do provider sem dba intervention. Novo endpoint `POST /webhooks/billing/asaas` aceita payload nativo Asaas: lê `io.LimitReader(r.Body, 1<<20)`, chama `billing.NormalizeAsaasWebhook(raw)`, resolve subscription via `FindByProviderCharge("asaas", chargeID)`, RecordEvent com `ErrDuplicateEvent→200` idempotent, state machine mesma que o endpoint genérico. Ambos compartilham o secret `X-Torque-Billing-Secret` (Asaas dashboard permite setar um webhook access token custom por URL). **Config** (`config/config.go`): `AsaasAPIKey` (env ASAAS_API_KEY, empty = mock fallback), `AsaasBaseURL` (env ASAAS_BASE_URL, empty = sandbox default na `NewAsaas`). **main.go wiring**: `quotaRepo := quotarepo.New(pool)` shared across leads/quotas/billing-webhook. `billinghandler.NewWebhook(subRepo, bus, cfg.BillingWebhookSecret).WithQuotaRepo(quotaRepo).Routes(wh)`. `leadshandler.New(leadrepo.New(pool), bus).WithQuota(quotaRepo).Routes(t)`. `quotasHandler := quotashandler.New(quotaRepo); quotasHandler.MemberRoutes(t)` dentro do tenant-scope group; `quotasHandler.MasterRoutes(mst)` dentro do master-only group. Provider switch em admin routes: `case "asaas": asaasProv, err := billing.NewAsaas(...)` — erro → logger.Warn + fallback mock; sucesso → logger.Info com base_url. **Frontend**: `hooks/useQuotas.ts` — useQuotas (list, staleTime 60s, unwrap .data), useQuota(resource) (detail URL-encoded), `KnownQuotaResource` const mirror. `ui/quota-meter.tsx` — compact 3-tone progress indicator (≤80 neutral accent / 80-99 warning / 100 danger), `Math.min(100, round(pct))` clamp, ARIA `role=meter` + valuemin/max/now, custom label opcional, effective_limit<=0 retorna null (não renderiza pra unconfigured). FunisHubPage `<QuotaMeter quota={leadsQuota.data} label="Leads do plano" />` na header quando data presente. `api/errors` estende FRIENDLY_MESSAGES com QUOTA_EXCEEDED ("Você atingiu o limite do seu plano para esse recurso. Atualize o plano ou compre add-ons em Configurações → Plano e faturamento.") + QUOTA_LOOKUP_FAILED ("Não foi possível conferir o limite do plano. Tente de novo em instantes."). **Tests backend 20 funcs novos**: asaas_test.go 13 (dual-review tripwire empty-key-errors, Name=asaas, CreateCharge happy customer→payment→pix multi-hop com externalReference/billingType assertion, rejeita non-BRL + zero amount, CancelCharge 404 success + 5xx propagates, UnauthorizedMapsToCleanError, RateLimitSurfaces, Normalize PAYMENT_CONFIRMED→paid + OVERDUE→overdue + DELETED+REFUNDED→cancelled table + unknown→asaas.<event> prefix + empty-payment-id errors + garbage-json errors + event_id composed type+id). quota_test.go 7 (admits+headers + 402 at cap body `QUOTA_EXCEEDED` + fail-closed ErrNotFound=402 + master bypass (reader.Get must-not-be-called) + unauthenticated 401 + missing-tenant 401 + factory panic empty-resource). **Tests frontend 9 cases**: useQuotas list shape + detail + URL-encoding (`special%2Fslash`); QuotaMeter undefined→null + effective_limit<=0→null + pct+copy rendering + clamp 100 + ARIA meter role+values + custom label. **Full suite frontend 296/296 em 86 files** (+9 cases, +2 files vs S50 D070); typecheck (tsc --noEmit) + eslint --max-warnings 0 verde. **Acceptance criteria runtime Go host**: (1) BILLING_PROVIDER=asaas + ASAAS_API_KEY=<sandbox> → checkout real gera PIX válido com QR code; (2) POST /payments Asaas webhook → charge.paid → MarkPaid + SeedPlanDefaults + subscription.activated WS event; (3) Tenant no cap recebe 402 QUOTA_EXCEEDED com body JSON canônico em `POST /leads`; (4) Master user impersonando bypassa quota check; (5) PATCH /quotas/leads com admin_adjustment=+50 master-only ajusta effective_limit imediatamente. **Deferreds explícitos**: team_members/workflows/agents wiring idêntico (pattern estabelecido, drop-in 3-linhas por handler); Asaas boleto + cartão (PIX é MVP; código já abstrai billing_type por query se follow-up quiser); pentest de staging + cosign keyless + gosec high + OpenAPI refresh final → **S52**. **Commits**: `8209b44` db · `e7be776` backend · `3156135` tests · `cddaae7` frontend. Branch `sprint/S51` → merge `--no-ff` para develop via PR. Próxima: **S52 — OpenAPI refresh final + cosign keyless + gosec high + pentest staging + k6 load test (FASE G FINAL)**. |
| D070 | 2026-04-22 | Sprint S50 — Fase F.2 Meta Ads + SZ.Chat + Lead Webhook + TinyERP sync + F02→GCal (FASE F CONCLUÍDA) | **Segunda e última sprint da Fase F (Integrações externas).** Fecha todos os deferreds de S49 (sync-products TinyERP, IntegrationsSection rich UI, F02→gcal wiring) + entrega o escopo original S50 do Plano Paridade v8 (Meta Ads insights, lead webhook público, SZ.Chat messaging). **Migration 0025** (`0025_s50_lead_webhook_meta_szchat.up.sql`): `lead_webhook_events` com `UNIQUE(organization_id, external_id)` absorvendo retries do provider sem mutar lead duas vezes + `source` CHECK IN webhook/meta/szchat/manual + `raw_payload jsonb` forensic + `signature_ok bool` + partial idx (org, received_at DESC); `meta_insights_cache` com `UNIQUE(org, account_id, date_range)` — TTL enforced em query time (`fetched_at > now() - 15min`) em vez de write-time, ON CONFLICT DO UPDATE substitui stale; provider CHECK extendido com `szchat`. **Meta provider** (`service/integration/meta/meta.go`): `InsightsProvider` interface nova em `service/integration/integration.go` (AdAccountInsights + AdCampaign value objects + InsightsWindow); provider usa circuit breaker S27 pattern, `appsecret_proof=hex(hmac_sha256(access_token, app_secret))` opcional via querystring, 2 GETs (`level=account` rollup + `level=campaign` breakdown) pois Graph não aceita múltiplos levels em uma call sem `time_increment` gymnastics; error code mapping 190/102 → `ErrAuthFailed`, 17/4/613 → `ErrRateLimited`; `decimalToCents` truncates past 2 decimals + handles BR/US (`.` and `,`), `sumLeadActions` case-insensitive sobre `action_type LIKE *lead*` (cobre "lead", "onsite_conversion.lead_grouped", "offsite_conversion.fb_pixel_lead", etc), `cpLead` retorna `-1` quando leads=0 (distingue "no leads" de "free"). **SZ.Chat provider** (`service/integration/szchat/szchat.go`): `MessagingProvider` completo — Bearer auth + per-tenant API key, POST `/messages` JSON `{channel, to, kind, body, media_url}` + `normalizeKind` default text para unknown kinds; 401/403 → ErrAuthFailed, 429 → ErrRateLimited, 5xx → ErrUnreachable, body-level `{error:{code,message}}` também captura erro (provider retorna 200 com error body em alguns cenários); credential.ExternalAccountID opcional guarda default channel_id pra fallback. **TinyERP sync** (extend `service/integration/tinyerp/tinyerp.go`): nova `SyncProductsSink` interface narrow `UpsertBySKU(name, sku, description, price_cents, currency) (inserted bool, err error)` — bridgeia sem cyclic import; `SyncProducts(ctx, orgID, sink)` paginado `/produtos.pesquisa.php?pagina=N` com `maxPages=100` guard (50k produtos é acima de qualquer tenant realista); `productrepo.UpsertBySKU` usa `INSERT ... ON CONFLICT (organization_id, sku) WHERE sku IS NOT NULL DO UPDATE` — o `(xmax = 0) AS inserted` retorna bool que distingue insert vs update no mesmo roundtrip; `SyncProductsResult{fetched, inserted, updated, skipped}`. **Lead webhook** (`handler/leadwebhook/webhook.go`): público POST `/webhooks/lead` montado em `r.Route("/webhooks", ...)` sem Authenticator/RequireAuth/CSRF middleware (escape fora de `/api/v1`). Assinatura HMAC-SHA256 sobre raw body em `X-Torque-Lead-Signature: sha256=<hex>` — `subtle.ConstantTimeCompare` constant-time, rejeita prefix ausente pra defeater downgrade attack (unprefixed hash = reject), rejeita hex malformado/truncado. Tenant header `X-Torque-Tenant: <uuid>` obrigatório (uuid.Parse). Body cap 64KiB (`io.LimitReader(r.Body, 64KiB+1)` + comparison reject on exceed). Empty secret = refuse every call (secure default 503). `leadwebhookrepo.Record` insere event primeiro — `ErrDuplicate` on `(org,external_id)` UNIQUE → HTTP 200 OK idempotente sem tocar leads table; success → `leadrepo.Create` + `AttachLead` best-effort + `bus.Publish(lead.created)` (workflow `BusSubscriber` já mapeia `lead.created`→`lead_created` trigger e fans out para workflows ativos). Respose 201 `{lead_id, external_id}`. **F02→GCal wiring** (deferred S49 closure): `handler/confirmations/confirmations.go` ganha `WithIntegrations(gcal, store, logger)` analógico ao meetings S49. `confirm` handler agora (1) snapshot `repo.Get` pré-MarkConfirmed pra capturar `meeting_at`/`meeting_channel`/`meeting_notes`, (2) marca confirmed + publica `confirmation.confirmed`, (3) goroutine detached 20s com `domain.WithOrgID` cria evento GCal título "Reuniao confirmada — <channel>" + description "Confirmada via Torque CRM (F02).\n\n<notes>" + default duration 30min (F02 schema não carrega end_at); failure log+MarkError via breaker sem afetar 204 response. **IntegrationsSection rich** (frontend, deferred S49 closure): extraído de inline 127 linhas em `SettingsPage.tsx` para `features/settings/IntegrationsSection.tsx` standalone (~400 linhas); cards Google/TinyERP/Meta/SZ.Chat live com cada um próprio componente + `ProviderCard` compartilhado + `AmbientCard` pra WhatsApp/Asaas/n8n. `last_success_at` renderiza como `relativeTime(iso)` pt-BR 5-bucket (agora < 1min / há X min < 60 / há X h < 24 / há X d < 30 / absolute localeDateString); `last_error_text + last_error_at` surface warning badge (AlertCircle icon + bg-warning/5 + shadow-inset warning border). Connect modals via Sheet (Radix Dialog): TinyERP api_key (minLength 10), Meta `access_token` minLength 20 + account_id optional, SZ.Chat api_key minLength 16 + channel_id optional; erros inline em `text-danger`; `exactOptionalPropertyTypes`-safe via conditional field injection (`Parameters<typeof mutateAsync>[0]` + `if (trim) input.x = trim`). TinyERP sync action inline com loading spinner + result `{inserted} novo(s) · {updated} atualizado(s) · {skipped} ignorado(s)`. A11y: `htmlFor={useId()}` + `id` em todo input. **Hooks novos** (`hooks/useIntegrations.ts`): `useConnectMeta/Disconnect`, `useConnectSZChat/Disconnect`, `useSyncTinyERPProducts` (invalida `[integrations]` + `[products]`), `useMetaAdsInsights({accountId, dateRange, enabled})` — staleTime 5min alinhado com cache backend 15min, `enabled: false` suspende query, querystring via `URLSearchParams`. `IntegrationProvider` type extendido com `szchat`. **Config** (`internal/config/config.go`): `META_GRAPH_BASE_URL` default `https://graph.facebook.com/v18.0`, `META_APP_SECRET` (empty OK — provider skip appsecret_proof), `SZCHAT_BASE_URL` default `https://api.szchat.com/v1`, `LEAD_WEBHOOK_SECRET` (empty = webhook 503). **Repo extensions**: `integrationrepo.ProviderSZChat` constant + `IsValidProvider` accepted; `leadwebhookrepo.Record/AttachLead/ErrDuplicate`; `metainsightsrepo.Get/Upsert/ErrStale/ErrNotFound` 15min TTL check em query. **main.go wiring**: `metaProvider` + `szchatProvider` built unconditionally (nil-guarded at handler layer pra 503 se env não setado); `metaCache = metacacherepo.New(pool)` + `leadWebhookEvents = leadwebhookrepo.New(pool)`; **productSyncSink adapter** local (type + UpsertBySKU method) bridgeia `*productrepo.Repository` → `tinyerp.SyncProductsSink` sem importar productrepo dentro do package tinyerp (cyclic import avoidance); `integrationsHandler.New` recebe `Meta/SZChat/MetaCache/ProductSink` novos; confirmations handler `.WithIntegrations(gcalProvider, credStore, logger)` agora aplicado; public `/webhooks/lead` montado alongside `/webhooks/billing` no `r.Route("/webhooks", ...)` sem Authenticator middleware. **Tests backend** (toolchain Go pendente host — pattern D010 mantido): meta_test.go 10 (decimalToCents 9 cases, sumLeadActions case-insensitive, cpLead boundaries, appsecret_proof empty/hmac-shape, get happy/oauth/rate-limit, fetchInsights rollup account+campaign) + szchat_test.go 7 (postJSON happy + 401+403 ErrAuthFailed + 429 ErrRateLimited + 5xx ErrUnreachable + provider-error body + normalizeKind table) + tinyerp/sync_test.go 7 (decimalToCents BR comma separator, listProductsPage OK/last-page/auth-error, processSyncRows fakeSink insert-vs-update counts + empty-sku skip + error-counts-as-skip) + leadwebhook/webhook_test.go 6 (verifyHMAC happy + no-prefix-downgrade + tampered body + wrong secret + malformed-hex + truncated-sig) + integration_test.go extend 1 (MockInsights + compile-time `var _ integration.InsightsProvider = (*integration.MockInsights)(nil)`) = **31 funcs novas** cobrindo wire contract sem exigir pgx. Integration-tier coverage (dedup + lead creation path + metainsights cache TTL + F02→GCal goroutine) fica para CI com host Postgres — hand-reviewed. **Tests frontend**: 12 hook assertions novas (useSyncTinyERPProducts result shape, useConnectMeta body shape, useDisconnectMeta path, useConnectSZChat body shape, useDisconnectSZChat path, useMetaAdsInsights querystring + enabled=false suspension) + 6 relativeTime buckets (agora/min/h/d/absolute/invalid). **Full suite frontend 287/287 em 84 files** (+13 cases +1 file vs S49), typecheck `tsc --noEmit` + `eslint --max-warnings 0` verde. **Acceptance criteria runtime (Go host)**: (1) Lead via n8n → POST /webhooks/lead com HMAC correto cria lead + dispara workflow trigger lead_created; (2) POST /webhooks/lead duplicado retorna 200 sem duplicar; (3) GET /integrations/meta/ads-insights retorna rollup + campaigns com cache header X-Torque-Cache: miss|hit; (4) POST /integrations/tinyerp/sync-products com credencial válida retorna `{fetched, inserted, updated, skipped}` + products tabela populada; (5) POST /confirmations/:id/confirm com google credential cria evento no Calendar do tenant. **Deferreds explícitos**: SZ.Chat webhook inbound (via /webhooks/lead quando configurado como lead source) e conversations integration (via /webhooks/message no futuro) — primeiro release foca outbound SendMessage. Meta Ads leads ingestion via /webhooks/lead com source=meta (Meta Lead Ads relay para webhook) vs pull via /ads-insights — primeiro release do insights endpoint é pull-based; webhook-based leads ingestion continua via /webhooks/lead genérico com caller (n8n) relayando. **Commits**: `092e397` db · `1f40812` backend · `3a77357` tests backend · `191d25a` frontend. Branch `sprint/S50` → merge `--no-ff` para develop via PR. **FASE F CONCLUÍDA** — 2 sprints em topologia linear (S49→S50). 2 migrations (0024 credentials, 0025 webhook+cache+szchat). Próxima fase: **G — Hardening produção (S51-S52)** Asaas real + dual-review + quota enforcement + OpenAPI refresh + cosign + gosec + pentest staging + k6 load test. |
| D069 | 2026-04-20 | Sprint S49 — Fase F.1 Google Calendar real + TinyERP foundation | **Primeira sprint da Fase F (Integrações externas).** Leva os providers de S27 (D036) de mock para implementação real atrás de OAuth 2.0 + API keys criptografadas. **Migration 0024**: `integration_credentials` (org+provider unique, AES-GCM bytea tokens, expires_at/scopes/external_account_id observability, last_success_at/last_error_text 2000-cap/at health fields, partial index em (org, last_success_at DESC) WHERE not null). Provider CHECK IN ('google','tinyerp','meta') — meta pre-declarado pra zerar schema churn em S50. Seeds permission keys `integrations.view` (default=true) + `integrations.manage` (is_admin_only=true). **Crypto** (`service/crypto/aesgcm.go`): `Cipher.Encrypt` prepende nonce 12B crypto/rand por chamada; `Decrypt` retorna `ErrShortEnvelope` ou `ErrDecrypt` opaco (nunca distingue wrong-key de tamper pra não vazar info ao atacante); base64 decoder tolera RawURL/URL/RawStd/Std; key strict 32 bytes. **Credential store** (`repository/integration/credential.go`): Upsert INSERT ON CONFLICT (organization_id, provider) DO UPDATE encripta tokens antes de persistir; Get decripta no retorno; List retorna metadata sem tokens; MarkSuccess/MarkError/Delete observability; toda query WHERE organization_id = $1. **GCal adapter** (`service/integration/gcal/gcal.go`): `Provider` satisfaz `integration.CalendarProvider`. `DefaultEndpoints()` produção Google; `WithEndpoints` escape hatch pra httptest. `AuthURL(state)` monta auth endpoint com `response_type=code` + `access_type=offline` + `prompt=consent` + `scope=https://www.googleapis.com/auth/calendar.events openid email` + `state`. `ExchangeCode`/`RefreshToken` form-encoded com mapeamento 401→`ErrAuthFailed`, 5xx→`ErrUnreachable`, 429→`ErrRateLimited`. `CreateMeetingForOrg(orgID, in)` fetcha credencial, refresca se `expires_at < now+2min`, POST `/calendars/primary/events` JSON {summary, description, start.dateTime, end.dateTime, attendees[{email}]} com `Authorization: Bearer <access>`; retorna `ProviderEventID=resp.id`, `MeetingURL=resp.htmlLink`. `CreateMeeting(ctx, in)` satisfaz interface extraindo orgID de `domain.OrgIDFrom(ctx)`. `CancelMeetingForOrg(orgID, eventID)` DELETE com 404 tratado como success (já se foi). `Disconnect(orgID)` revoga refresh via `https://oauth2.googleapis.com/revoke` best-effort + `store.Delete`. Body capped `io.LimitReader 1 << 20`. Success → `store.MarkSuccess`; auth/permanent error → `store.MarkError`. **TinyERP adapter** (`service/integration/tinyerp/tinyerp.go`): idem padrão. TinyERP usa `token` como query/form param (estilo incomum, não header); `CreateOrderForOrg` POST `/pedido.incluir.php` form `token=<key>&formato=json&pedido=<json>`; parseia envelope `{retorno:{status,erros,codigo_erro,registros}}`; codigo_erro=6→ErrAuthFailed, 30→ErrRateLimited, outros→fmt.Errorf. `StoreAPIKey(orgID, apiKey)` valida 10-200 chars + Upsert com `expires_at=NULL`. **Handler** (`handler/integrations/integrations.go`): estado HMAC format `base64url(org_uuid_16B + ts_BE_4B + nonce_8B) + "." + base64url(hmac_sha256(secret, payload))[:16]` com TTL 10 min + ConstantTimeCompare + reject far-future (clock skew guard). Rotas: `GET /integrations` (member) lista `[{provider, connected, external_account_id, scopes, expires_at, last_success_at, last_error_*}]`; `GET /integrations/google/connect` (admin) gera state + 302 → gcal.AuthURL; `GET /integrations/google/callback` (admin, **CSRF-exempt subgrupo** — OAuth redirect via 302 full-page não carrega X-CSRF-Token) verifica state, ExchangeCode, decodifica email do ID token middle-segment JSON (sem JWT signature verify — TLS-delivered direto pelo Google), Upsert, 302 `/settings?tab=integrations&provider=google&status=connected`; erro → `status=error&reason=<code>`; `POST /integrations/google/disconnect` (admin); `POST /integrations/tinyerp` (admin) body `{api_key}` → StoreAPIKey; `POST /integrations/tinyerp/disconnect`; `POST /integrations/tinyerp/push-order` (admin) body OrderInput → CreateOrderForOrg → OrderResult. **Domain carrier** (`domain/context.go`): `WithOrgID/OrgIDFrom` novo segundo keying carrier — a middleware key é unexported e não atravessa goroutines de background (meetings goroutine cria ctx fresh). **Meetings wiring** (`handler/meetings/meetings.go`): `WithIntegrations(gcal, store, logger)` opcional nil-safe; `AttendeeEmail *string` em createReq validado RFC5322 relaxado, nunca persistido. Apos insert local: se credencial google existe, goroutine `context.WithTimeout(Background, 20s)` + domain.WithOrgID chama `gcal.CreateMeetingForOrg`, em sucesso UPDATE meetings SET external_provider='gcal', external_id=eventID; falha → store.MarkError + log. Delete espelha com `CancelMeetingForOrg` best-effort. **Repo meeting** ganha `SetExternal(ctx, orgID, id, provider, externalID)`. **Config** (`config/config.go`): `INTEGRATION_ENCRYPTION_KEY` fatal non-dev; dev fallback via `sync.Once` WARN (32 bytes determinísticos); `GOOGLE_OAUTH_{CLIENT_ID,CLIENT_SECRET,REDIRECT_URL}` opcionais; `TINYERP_BASE_URL` default `https://api.tiny.com.br/api2`; `INTEGRATION_STATE_SECRET` fallback JWTSecret. **main.go** wira cipher → credStore → gcal/tinyerp → meetingsHandler.WithIntegrations + integrationsHandler.MemberRoutes(t) / AdminRoutes(admin) / CallbackRoute(cb subgrupo CSRF-exempt). **Frontend** `hooks/useIntegrations.ts`: useQuery list stale 60s + unwrap `.data`; `googleConnectURL()` retorna `/api/v1/integrations/google/connect` (caller window.location.href — full-page nav obrigatório pro 302 pra Google); `useDisconnectGoogle`/`useConnectTinyERP(apiKey)`/`useDisconnectTinyERP` via useAppMutation. **SettingsPage**: ?tab query param preseleciona seção; IntegrationsSection agora lê credenciais reais do backend para google+tinyerp (WhatsApp/Meta/Asaas/n8n continuam como labels até S50); card Google com Conectar/Desconectar real; card TinyERP badge "em breve" até S50 trazer modal de api_key + IntegrationsSection rich (last_success_at, scopes, etc). **Tests backend** (Go toolchain pendente host — D010 pattern desde S01, validação runtime transferida): crypto 7 (round-trip, empty, tampered, truncated, wrong-key, base64 interop, wrong-decrypt) + gcal 10 (AuthURL shape, ExchangeCode 3 casos, RefreshToken 2, DecodeIDToken 2, Health 1, Breaker 1) + tinyerp 8 (wire, codigo_erro 6/30, 5xx, 429, mapOrder valid/invalid, cents) + credential 2 (integration-gated: upsert round-trip + cross-tenant isolation) + integrations handler 9 (state round-trip/tampered/expired/malformed + redirect 2 + callback missing/bad-state/provider-error) = 36 funcs total, 34 sob `-short`. **Tests frontend**: useIntegrations test 5 cenários (connectURL shape + list unwrap + 3 mutations paths). **274/274 em 83 files** (+5 cases, +1 file vs remediation-C D068). Coverage preservada acima ratchet 70/70/70/60: lines **78.48** / stmts **75.30** / funcs **74.75** / branches **62.22**. Acceptance criteria runtime (Go host): (1) Connectar Google Calendar em dev + criar meeting cria evento real no Calendar. (2) Push order para TinyERP sandbox retorna order_id. **Deferred explícitos**: sync-products endpoint; IntegrationsSection rich (modal apiKey TinyERP, last_success_at display, disconnect confirm) → S50; confirmation F02 → gcal.CreateMeeting wiring → S50; Meta Ads + SZ.Chat + Lead Webhook inteiro → S50. Branch `sprint/S49` → merge `--no-ff` para develop via PR. Commits: 4d0161a db · 32d5ffa backend · c95bda7 tests · 636fb3e frontend. Próxima: **S50 — Meta Ads Insights + Lead Webhook + SZ.Chat + IntegrationsSection rich**. |
| D068 | 2026-04-20 | Remediation-C — lift de cobertura pré-Fase F (ratchet 55→70) | Terceira rodada de remediation (A=D029, B=D055, C=D068). Fecha o gap de cobertura que permanecia 72/100 pós-B. **15 test files novos** em 4 áreas: **(a) 5 page integration tests** — CampaignDetailPage (render header + actions contextuais), AgentMetricsPage (window picker + KPI cards + bar stacked), CustomPipePage (board horizontal + badges is_final_*), TVDashboardPage (widget rotation + 3 widgets + progress dots), UpsellPage (grid cards + segment/origin badges). **(b) 6 hook extras cobrindo branches antes descobertos**: useAnalytics.extra (enabled gating + leaderboard), useCampaigns.extra (launch/pause/resume/cancel + recipients), useInbox.extra (WS invalidate + template apply), useMeetings (CRUD + WS), usePerformance (ranking window + set commission status + awards), useWorkflows.extra (steps CRUD + run cancel + entry set). **(c) 2 lib**: ws.test (parse frame types + reconnect backoff jitter), utils.test (formatters + classname merge). **Vitest ratchet step 4**: 55/55/50/48 → **70/70/70/60** (branches fica em 60 porque else-paths defensivos inflam denominator). **Medido live** (vitest --coverage): lines **78.21** (+18.99pp vs D055), stmts **75.02** (+18.32pp), funcs **74.20** (+18.87pp), branches **62.22** (+12.64pp). **269/269 em 82 files** (+101 cases, +23 files vs S48 D067). Score testes: 72/100 → **88/100** (+16pp). **Score composto**: 87/100 → **89/100** (+2pp). Gitignore ajustado: `*.tsbuildinfo` (artifact TS incremental) + `.claude/scheduled_tasks.lock` (runtime lock do /loop skill) — evita drift futuro. **No backend changes**. Branch `sprint/remediation-C` → merge `--no-ff` para develop via PR. Próxima: **Fase F — Integrações externas (S49-S50)** Google Calendar real (schema já carvado em 0023) + TinyERP + Meta Ads + Lead Webhook + SZ.Chat. |
| D067 | 2026-04-20 | Sprint S48 + Fase E concluída — F12 Pipes custom + Upsell + F13 Agenda + TV Dashboard | **Terceira e última sprint da Fase E.** Fecha a trilha Produto completo: S46 (F08 Campanhas UI) → S47 (F09 Performance) → **S48 (F12 Pipes custom + Upsell + F13 Agenda + TV)**. **Migration 0023**: `meeting_status` enum (scheduled/completed/cancelled/no_show); `meetings` table com `title 2-200`, `description ≤4000`, `starts_at/ends_at tstz` + CHECK `ends_at > starts_at`, `location ≤400`, `lead_id` FK ON DELETE SET NULL (reunião preservada se lead for apagada), `owner_member_id` FK SET NULL, `external_provider` CHECK IN ('gcal','outlook') + `external_id` (unique pair DEFERRABLE — ponto de extensão S49 sem mudança de schema). Seeds `meetings.view` + `meetings.manage` (default true — todos podem agendar). Índices: (org, starts_at), partial (org, owner_member_id) WHERE NOT NULL, partial (org, lead_id) WHERE NOT NULL. **Repo `meeting.go`**: Create (validates title + window), Get, **ListRange** com intersection `starts_at < to AND ends_at > from` (cobre overlap total), SetStatus (valida enum), Delete. **Handler `meetings.go`**: `GET /meetings?from&to` (default [-7d, +30d], INVALID_FROM/TO/WINDOW), POST create, GET /:id, POST /:id/status, DELETE /:id. WS `meeting.{created,updated,deleted}`. **main.go** wira member-accessible. **Frontend hooks `useMeetings.ts`** com useMeetings (WS-invalidated + 30s stale)/useCreateMeeting/useSetMeetingStatus/useDeleteMeeting. **AgendaPage** em `/agenda`: janela default [-7d, +30d]; agrupa por dia via `Map<day, Meeting[]>` com toLocaleDateString pt-BR (weekday/day/month); MeetingRow com time range + title + status badge + trash; CreateMeetingModal com datetime-local (title + location opcional + start/end ISOString). Lista em vez de react-big-calendar (1385 LOC no v8) porque bundle-weight > benefit para a operação. **UpsellPage** em `/upsell`: useLeads cursor-paginated (page_size=50) + query.items; grid 2-col de cards com segment/origin badges + company + updated_at. Materialized view `upsell_opportunities` deferred (precisa cruzar proposals + products). **CustomPipePage** em `/pipe/:id`: reusa usePipes/usePipeStages/usePipeEntries; board horizontal min-w-[220px] por stage ordered por `position` ASC; contagem de entries agregada em-memória via Map; badges `is_final_positive` (tone=success) + `is_final_negative` (tone=danger); Navigate para /pipeline quando id missing ou pipe não-encontrado. **TVDashboardPage** em `/tv` **fora do AppShell** (rota top-level no routes.tsx após onboarding, antes do AppShell gate): fundo dark bg-ink + text-surface cinematográfico; `setInterval(ROTATION_MS=20_000)` troca widgetIdx via modulo; 3 widgets reusando hooks existentes — ProposalsWidget (useProposalsSummary: sent_in_window fontsize 7xl + accepted + won_amount BRL no rodapé), RankingWidget (useRanking top 5 ol com #i + member_name + revenue BRL), LeadsWidget (useLeadsSummary in_window fontsize 7xl + assigned/unassigned split). Progress dots na base marcam widget ativo com bg-accent. **Routing**: /agenda + /upsell + /pipe/:id dentro do AppShell; /tv como rota top-level fora (paridade com /onboarding). **Tests `AgendaPage.test.tsx`** 2 cenários (empty state no período + render de reunião com status badge). Pipe custom + TV + Upsell validados por smoke via typecheck + lint — tests profundos de rotação (setInterval com fake timers) ficam para quando um regression aparecer. **195/195 em 68 files** (+4 cases, +3 files vs S47). Commits: `b62e6b1` backend · `4728323` frontend. **FASE E CONCLUÍDA**: 3 sprints em topologia linear cumulativa (S46→S47→S48), cada uma com branch+merge preservado. 2 migrations (0022 Performance, 0023 Meetings). Frontend 195/195 em 68 files (+8 cases, +4 files desde início da fase). **Deferreds da fase documentados**: (1) dispatch rules SDR/Closer na CreateCampaignModal (schema assign_rule ainda não existe); (2) progresso % das metas + cálculo automático de comissão ao fechar proposta (depende de workflow handler novo + DB trigger OU event bus hook em proposals.SetStatus); (3) materialized view `upsell_opportunities` para filtros ricos (leads em stage won com produto X mas sem produto Y); (4) react-big-calendar agenda grid (bundle-weight > benefit até primeiro pedido real); (5) Google Calendar sync — columns já carvadas em 0023 prontas para S49. **Próxima fase: F — Integrações externas (S49-S50)** Google Calendar real + TinyERP + Meta Ads + Lead Webhook + SZ.Chat. |
| D066 | 2026-04-20 | Sprint S47 — Fase E.2 F09 Performance consolidado (Ranking + Metas + Comissões + Premiações) | **Segunda sprint da Fase E.** Consolida 1443 LOC do v8 `Performance.tsx` + `Comissoes.tsx` em uma página com 4 tabs + backend dedicado. **Migration 0022**: `goal_metric` enum (5 métricas: deals_won, revenue_cents, leads_contacted, response_time_ms, first_response_minutes); `goals` table (org, member_id FK nullable = org-level quando NULL, metric, target bigint, period_start/end tstz com CHECK period_end>period_start, created_by team_member); `commission_status` enum (pending/approved/paid/cancelled); `commissions` table (org, proposal_id FK ON DELETE SET NULL = comissão preservada se proposta for apagada, member_id, percentage numeric(5,2) CHECK 0-100, amount_cents bigint, currency 3-char, status, earned_at default now, approved_at + paid_at NULL stampados na transição, notes ≤2000 chars); `awards` table (org, title 2-120, description ≤2000, criteria_json jsonb default {}, winners_json jsonb default [] snapshot, awarded_at NULL). Índices: goals (org, period_start DESC), (org, member_id) WHERE NOT NULL; commissions (org, member_id, earned_at DESC), partial (org, status) WHERE pending|approved; awards (org, awarded_at DESC) WHERE NOT NULL. Seeds performance.view (default=true) + performance.manage (admin-only). **Repo `performance.go`**: `CreateGoal/ListGoals` (period_start DESC); `ListCommissions` com optional `member_id` filter + LIMIT 200 earned_at DESC; `SetCommissionStatus` valida enum + COALESCE stamps (`approved_at = COALESCE(approved_at, now())` quando status='approved', paid_at idem quando paid); `CreateAward/ListAwards` (awarded_at DESC NULLS LAST cap 100); **`Ranking(orgID, since, until)`** LEFT JOIN proposals com `status='won' AND updated_at ∈ [since, until)` agregando `COUNT(p.id)` + `COALESCE(SUM(p.value_cents), 0)` per active team_member, JOIN users pra nome, ORDER BY revenue DESC, deals_won DESC. Drift-free (nenhum materialized counter — source of truth é proposals). **Handler `performance.go`**: routing split em `Routes()` member-accessible (GET /performance/ranking?since&until, /goals, /commissions?member_id, /awards) + `AdminRoutes()` admin-only (POST /goals, POST /commissions/:id/status, POST /awards). Ranking params padrão S42 (RFC3339, default last 30d, INVALID_SINCE/UNTIL/WINDOW). WS events `goal.created`, `commission.updated`, `award.created`. SessionFrom hidrata created_by via TeamMemberID. **main.go**: performanceHandler declarado no outer t.Group scope, `.Routes(t)` no member subrouter, `.AdminRoutes(admin)` no admin subrouter (closure visibility). **Frontend hooks `usePerformance.ts`**: useRanking com queryKey[since, until] staleTime 60s disabled sem window; useGoals/useCreateGoal WS-invalidated; useCommissions(memberId?)/useSetCommissionStatus WS-invalidated; useAwards/useCreateAward WS-invalidated. **PerformancePage** em `/performance`: 4 tabs segmented (Ranking | Metas | Comissões | Premiações). **Ranking tab**: 7d/30d/90d inline picker recomputa range; RankingBars ol renderiza `#ord` + member_name + deals counter + BRL toLocaleString pt-BR + horizontal bar com pct relativo ao max revenue (accent/60 background). **Metas tab**: rows com metric badge + member prefix[:8] OR "org" badge + target pt-BR + período locale pt-BR. **Comissões tab**: 2 AmountCards totalizadores (Pendentes / Aprovadas+Pagas em BRL) + lista rows com member prefix + percentage 2 decimals + amount BRL + status badge (approved/paid=success, cancelled=danger, pending=neutral). **Premiações tab**: rows com título + awarded_at badge locale + description. **Tests `PerformancePage.test.tsx`** 2 cenários (renders ranking bars asserta Alice/Bob + "3 deals"; switches to Comissões tab via fireEvent + empty state). Commits: `652b4d7` backend · `d24817e` frontend. **Deferred explícito**: (1) **Progresso % das metas** — exige endpoint `GET /goals/:id/progress` que cruza metric enum vs série atual (proposals/leads/messages COUNT ou SUM no period window); a lista atual mostra target+período mas não "x/target (y%)". (2) **Cálculo automático de comissão** ao fechar proposta — hoje commissions entram via POST manual admin; fluxo autônomo precisa workflow handler `ComputeCommissionAction` OU DB trigger em proposals.SetStatus('won') publicando bus event → async creator (~3 integrações). (3) **Visx charts** (LeaderboardBars/GoalProgress/CommissionTimeline do plano) — SVG inline cobre acceptance atual; adopção quando primeiro dashboard sair do retângulo simples. |
| D065 | 2026-04-20 | Sprint S46 — Fase E.1 F08 Campanhas UI (CampaignsPage + CreateCampaignModal + CampaignDetailPage) | **Primeira sprint da Fase E.** Substitui placeholder anterior por UI live wired ao backend S19 existente. **CampaignsPage** em `/campaigns`: 2 tabs "Em andamento"/"Arquivadas" particionadas por status (archived = `cancelled|completed`); grid 2-col de cards clicáveis (Link → `/campaigns/:id`) com name + status badge + 4 Stat counters (enfileirados/enviados/falhas/pulados, toLocaleString pt-BR, falhas em danger quando >0); empty state contextual + Skeleton. **CreateCampaignModal** wizard 3 steps via state `Step = 1|2|3`: step 1 identidade (Name ≥2 chars + Description optional + Template textarea 5 rows); step 2 audiência DSL reusando TriggerFilter shape do S40 `{all: [{field, op, value}]}` com 4 ops (eq/in/contains/present) + preview JSON inline em `<code>` font-mono; step 3 schedule `<input type=datetime-local>` opcional + resumo (Nome/Template/Audiência). Progress pill header com 3 ovals mostrando avanço. Validação canAdvance por step antes de "Próximo". Submit via useCreateCampaign com ISO string no scheduled_at. **CampaignDetailPage** em `/campaigns/:id`: PageHeader com CampaignActions contextuais (Pausar running, Retomar paused, Cancelar draft|scheduled|running|paused); grid 5-col StatCard+4 NumberCards (Status, Enfileirados, Enviados, Falhas, Pulados); lista de recipients com lead_id prefix[:8] + status badge (sent=success, failed=danger) + sent_at locale pt-BR. Empty state + Skeleton. **Routing**: `/campaigns/:id` lazy + `withSuspense`. **Tests** `CampaignsPage.test.tsx` 2 cenários (partição active vs archived com tab switch via fireEvent assertando count e visibilidade; empty state na tab ativa). **Deferred honesto**: (1) **Kanban por stage** do plano original — não implementado; a kanban de campanhas duplicaria a lista já que campanhas não têm sub-stages (o workflow runner abstrai isso); grid view cobre a operação real. (2) **Dispatch rules SDR/Closer distribution** — schema `assign_rule` não existe em migration 0010; quando ele vier, wizard step 3 ganha a tab. Commit: `877f446` frontend. |
| D064 | 2026-04-20 | Sprint S45 + Fase D concluída — F07.3 Workflow actions extras + Execuções UI + debug run | **Terceira e última sprint da Fase D.** Fecha a trilha Automação comercial: S43 (canvas xyflow + WorkflowListPage) → S44 (executor + ActionDispatcher com 4 kinds + event-bus subscriber) → **S45 (3 novos kinds + executions UI + debug run)**. **Backend `actions_s45.go`**: `CreateTaskAction` (kind=create_task) echoa `{title, assignee_id, due_in_hours, created:false}`; `CallAgentAction` (kind=call_agent) echoa `{agent_id, greeting, attached:false}`; `HTTPRequestAction` (kind=http) echoa `{url, method, accepted, status:0}` onde `accepted = validateHTTPURL(url)` — guard literal-prefix `https://` (S29 SSRF invariant, case-sensitive — uppercase scheme rejected by design; full private-CIDR check espera real fetch com DNS). `NewDispatcherS45(logger)` factory preserva S44 (`NewDispatcher`) intacto + Register de create_task/call_agent/http = **7 action kinds total** no dispatcher default. **runner.go eventTriggerMap** estendido: `lead.stage_changed → lead_stage_changed` (precisa publisher no pipe repo — S45 entrega só mapa), `message.received → message_inbound` (inbox S35 já publica — já funciona). Schedule trigger fica fora do bus (cron engine separado). **Handler `handler/workflows.go`**: nova rota `GET /runs/:id/steps` + `runStepView` DTO (id, run_id, step_id, status, input+output+error_payload como json.RawMessage, started_at/ended_at nullable, created_at). `ListRunSteps` do repo já existia de S44. **main.go**: swap `NewDispatcher` → `NewDispatcherS45` (uma linha). **Frontend hook `useWorkflowRunSteps(runId)`**: scoped GET `/runs/:id/steps` com `refetchInterval=2s` enquanto há step `pending|running`, auto-stop em terminal (paridade com S42 metrics polling pattern). **Page `WorkflowExecutionsPage`** em `/workflows/:id/executions` (lazy + Suspense): 2-column layout `grid-cols-[minmax(280px,380px)_1fr]` (RunsList esquerda + RunTimeline direita); RunsList mostra 50 runs mais novos como botões com status badge + trigger_source + `new Date().toLocaleString('pt-BR')`, highlight na seleção; RunTimeline renderiza TimelineStep ol com per-step `#<ord>` + status badge + `step_id.slice(0, 8)` mono + duration ms computada de `ended_at - started_at` + `<pre>` JSON preview (output quando succeeded, error_payload quando failed); empty state + skeleton. **`DebugRunButton`** no PageHeader chama `useEnqueueRun.mutateAsync({trigger_source: 'debug'})` — runner S44 pega em ≤2s com default poll; label muda para "Enfileirando…" durante pending. **Link "Execuções"** adicionado no `WorkflowCanvasPage` status row (ml-auto antes do Publicar/Pausar). **Routes**: `/workflows/:id/executions` lazy registrado. **Tests novos**: backend `actions_s45_test.go` **4 cenários** (dispatcher S45 registra exatamente 7 kinds + listing; create_task output shape round-trip; call_agent output shape; http accept https true, reject http/ftp/empty/uppercase-scheme); frontend `WorkflowExecutionsPage.test.tsx` 2 cenários (empty state com runs[], render run com badge + trigger_source). **191/191 em 66 files** (+2 cases, +1 file vs S44). Coverage lines 63.33 → **64.2** (+0.87pp), stmts 60.78 → **61.52** (+0.74pp), funcs 57.79 → **58.79** (+1.00pp), branches 52.99 → **53.16** (+0.17pp). Commits: `cad7d10` backend · `ce915eb` qa backend · `1045e6f` frontend. **FASE D CONCLUÍDA**: 3 sprints em topologia linear cumulativa (S43→S44→S45), cada uma com branch própria preservada como audit anchor + merge --no-ff. **Deferreds da fase documentados**: (1) handlers **side-effect reais** — SendMessageAction via Evolution adapter (S35 existe), UpdateLeadAction via lead repo, CreateTaskAction via tasks repo, CallAgentAction invoca F06 session (S37+), HTTPRequestAction real fetch com DNS+private-CIDR check — todos os 7 stubs têm forma 1:1 para migração quando primeiro tenant exigir. (2) **Retry + dead-letter queue** — arquitetura preparada (workflow_runs.error_payload jsonb + status failed existem), falta nova migration com `retry_remaining int + dead_letter_queue table` + backoff policy no runner. (3) **Schedule trigger** — cron engine separado (workflow_runs.next_run_at + scheduler goroutine); não é bus-driven. (4) **Pipe publisher** de `lead.stage_changed` — gancho no pipe move repo; eventTriggerMap já pronta. (5) Frontend **canvas debug inline** — step highlight verde/vermelho sobreposto no xyflow durante run (hoje debug é na página separada de Execuções; adequate por não bloquear scope fechado). **Próxima fase: E — Produto completo (S46-S48)** F08 Campanhas UI + F09 Performance consolidado + F12 Pipes custom + Upsell. |
| D063 | 2026-04-20 | Sprint S44 — Fase D.2 F07 Workflow executor + ActionDispatcher + event-bus subscriber (data plane) | **Segunda sprint da Fase D.** Entrega o engine que anda o DAG + dispatcher extensível + subscriber do bus — com honestidade explícita de que os side-effects reais dos 4 handlers ficam para S45 (hoje são stubs que registram intent no output trace). **Repo executor_queries.go** (split de workflow.go pra reviewer separar CRUD de runtime): `ClaimPendingRun` abre tx com `SELECT ... FOR UPDATE SKIP LOCKED LIMIT 1` sobre `workflow_runs WHERE status='pending' ORDER BY created_at` + `UPDATE status='running', started_at=now()` em uma mesma tx (idempotente sob concorrência multi-replica, o SKIP LOCKED evita dupla-claim); `SetRunCurrentStep(runID, stepID|nil)` para observadores externos verem onde um run longo está parado; `MarkRunSucceeded(runID, result jsonb)` + `MarkRunFailed(runID, errorPayload jsonb)` stampam `ended_at=now()`; `AppendRunStep` cria workflow_run_steps row em `status='running'` + `started_at=now()`, retorna id; `CompleteRunStep(id, status, output, err)` valida terminal `succeeded|failed|cancelled` + stamp `ended_at`; `ListRunSteps(orgID, runID)` ordena por `created_at ASC` para trace cronológico; `ListStepsByIDs(orgID, []ids)` batch via `= ANY($2)` — executor usa pra resolver `next_step_ids` sem round-trip por passo; `ListActiveWorkflowsByTrigger(orgID, kind)` scan por `trigger=$2 AND status='active' AND entry_step_id IS NOT NULL` — dois guards contra zumbis (workflow pausado + workflow sem entry). **Service workflow/dispatcher.go**: `ActionHandler` interface (`Kind() string` + `Execute(ctx, StepContext) (StepOutcome, error)`); `Dispatcher` registry com `Register()`, `Dispatch()`, `Handlers()` introspect; `ErrActionUnknown` envolvendo kind missing (runs que batem isso falham — handler ausente é config bug, não transitory). `StepContext` carrega OrgID, RunID, StepID, LeadID, Config (jsonb), PreviousOutputs (map stepID→output para referências cruzadas) com convenções `__input` (run input) e `__lead` (lead facts hidratados pelo executor). `StepOutcome` carrega Output JSON + optional NextStepID (override do walk default — marcador synthetic `__branch:true|false` para branch handlers). **4 handlers S44** todos stubs side-effect-free com logging + output JSON: SendMessageAction (template+body+trigger_kind+sent=false), UpdateLeadAction (fields+applied=false), WaitAction (duration_seconds+suspended=false), BranchAction com **expression evaluator próprio minimal** — ops `==`/`!=`, LHS = dotted path resolvido contra PreviousOutputs (`lead.origin` → `PreviousOutputs["__lead"].origin`, `input.amount` → `PreviousOutputs["__input"].amount`, `prev.<stepId>.field` → PreviousOutputs[stepId].field), RHS = quoted string literal OR dotted path; unknown op → (true, false) fail-closed para permissive (mantém run progredindo, flag `expression_valid=false` no output trace). **Service executor.go**: `Run(ctx, claimedRun) error` walks entry_step_id → step handler → next_step_ids; `MaxStepsPerRun=100` loop guard (runs com mais são quase certamente cycle, fail-out). Per-step pipeline: SetRunCurrentStep → AppendRunStep (running) → Dispatch → CompleteRunStep (succeeded OR failed com error payload) → propaga output para PreviousOutputs; handler err termina run com MarkRunFailed sem retry (S44 explicit deferred). Terminal: SetRunCurrentStep(nil) + MarkRunSucceeded(result com steps_visited+completed_at). Hydration: `__input` de run.Input, `__lead` com {id} quando run.LeadID != nil (full lead fact bag depende de lead repo lookup — S45+). nextStep(step, outcome) mapeia "__branch:true" → next_step_ids[0], "__branch:false" → next_step_ids[1], default → next_step_ids[0]; missing index → terminal. **Service runner.go**: `Runner` goroutine polla ClaimPendingRun com `cfg.PollInterval=2s + PollJitter=500ms` (anti-thundering-herd entre replicas); per-run `context.WithTimeout(2*time.Minute)`; graceful Shutdown aguarda wg com ctx deadline cap. `BusSubscriber` via `bus.Subscribe(256)` buffered channel; `eventTriggerMap` hardcode `lead.created → lead_created`; on event → `ListActiveWorkflowsByTrigger` + um `EnqueueRun` por match (lead_id de evt.EntityID, trigger_source=evt.Type); mapa extensível para S45. **main.go**: wfRepo (partilhada com handler existente) + NewDispatcher(logger) + NewExecutor(wfRepo, dispatcher, logger) + NewRunner + NewBusSubscriber, ambos com shutdown blocos defer com 5s ctx; ordem de teardown: runner → busSub → worker pool. **Tests novos**: `dispatcher_test.go` **11 cenários** cobrindo registry (unknown → ErrActionUnknown + Handlers() listing), send_message (stub output shape com template + sent=false), wait (round-trip duration), branch (eq true, eq false, neq, unparseable → true+expression_valid=false, input.* path, prev.<stepId>.field path), register override (fake handler substitui). **Coverage**: frontend sem novos tests (scope é backend); backend Go tests não executáveis no host sem Go instalado (runtime validation segue pendente como sprints anteriores). **189/189 frontend em 65 files** mantido. Commits: `32413d7` backend · `82d7728` qa. **Escopo deferido explícito** (justificado): (1) handlers side-effects reais — SendMessage via Evolution adapter (S35 já existe), UpdateLead via lead repo, WaitAction com suspension+re-enqueue via scheduler — precisam de >300 LOC adicionais por handler + integração tests E2E; cabe em S45 que lista exatamente "6 more action types" já prevendo essa migração de stub→real. (2) Retry + dead letter queue — requer migration nova (coluna `retry_remaining` em workflow_runs + tabela `dead_letter_queue`) + retry-backoff logic no runner — adiado até ver a primeira falha real em produção ser mais informativo que a especulação. (3) Triggers `lead.stage_changed` + `message.received` + `schedule` — eventTriggerMap é 3 linhas a mais quando os handlers reais shiparem; sem side-effects reais os triggers extras só gerariam runs no-op. **Próxima**: S45 (6 action types reais + WorkflowExecutionsPage UI + debugger run). |
| D062 | 2026-04-20 | Sprint S43 — Fase D.1 F07 Workflow Builder canvas + list | **Primeira sprint da Fase D.** Substitui o mockup seed-based de WorkflowBuilderPage por duas páginas live wired ao backend CRUD existente (S17). **Dep**: `@xyflow/react ^12.10.2` (v8 parity). **Pages**: (a) `WorkflowListPage` em `/workflows` — useWorkflows hook com WS-invalidation, status/trigger badges em PT-BR (Manual/Lead criado/Estágio mudou/Mensagem recebida/Agendado), empty state "Sem workflows ainda", botão "Novo workflow" abre inline form que cria draft com trigger manual e navega para canvas. QueryBoundary com loadingFallback+isEmpty+emptyFallback. (b) `WorkflowCanvasPage` em `/workflows/:id` — 3-column shell (palette 256px esquerda / ReactFlow fluid center / inspector 320px direita). **Palette**: 6 node variants draggable (lead_created + message_received como triggers via send_message kind + `trigger_kind` config; action.send_message com template/body; action.update_lead como "Mudar estágio"; wait com duration_seconds; branch com expression). HTML5 DnD encoda PaletteItem via `dataTransfer.setData('application/torque-step')`. **Drop**: `canvasRef.getBoundingClientRect()` + `reactFlow.screenToFlowPosition()` converte screen→flow coords; POST `/workflows/:id/steps` via useCreateStep; quando `workflow.entry_step_id` é null e o node dropped é trigger, chama setEntry automaticamente. **Drag node existente**: `onNodeDragStop` PUT position_x/y (Math.round para int — schema é `int NULL`). **Connect edges**: `onConnect` lê source step, faz `Array.from(new Set([...next_step_ids, target]))` (dedupe) + PUT. Self-loops refused cliente-side (`connection.source === connection.target`). **NodeInspector** (inspector lateral quando node selecionado): Nome Input + Config textarea (JSON 10 rows com font-mono + try/parse com erro inline), Salvar PUT, "Definir como entry" quando !isEntry, Excluir. `useEffect` reseta draft ao trocar step. **Header canvas**: Publicar/Pausar — Publicar disabled quando !entry_step_id. `ReactFlowProvider` envelope + Background/MiniMap/Controls. **Routing**: `/workflows` → WorkflowListPage, `/workflows/:id` → WorkflowCanvasPage, ambas lazy + lazyRetry; WorkflowBuilderPage antigo (357 LOC seed) deletado. **Canvas internals**: `updateStepAsync`/`deleteStepAsync` callbacks via raw `put`/`del` + `queryClient.invalidateQueries(['workflows', id, 'steps'])` — os hooks existentes `useUpdateStep(workflowId, stepId)` / `useDeleteStep(workflowId, stepId)` bindam stepId na construção e não servem pra canvas que edita steps arbitrários; documentado inline em um `StepPayload` type derivado de `Parameters<ReturnType<typeof useCreateStep>['mutateAsync']>[0]` pra ficar sincronizado. **Tests novos**: `WorkflowListPage.test.tsx` 2 cenários (render com dados mostra nome+status+trigger labels em PT-BR; empty state). **189/189 em 65 files** (+2 cases, +1 file vs S42). Coverage lines 63.21 → **63.33** (+0.12pp), stmts 60.67 → **60.78**, funcs 57.57 → **57.79**, branches 52.99 mantém. Todos acima do piso 60/60/55/50. Commit: `e24899d` frontend. **Escopo honesto**: canvas tests profundos (DnD, connect, inspector) adiados — require @testing-library/user-event + xyflow-specific test harness. O list page test cobre smoke; canvas behavior valida manualmente com as mutations já testadas em `useWorkflows.test.tsx` (S17). **Próxima**: S44 backend executor worker + ActionDispatcher + event bus hooks. |
| D061 | 2026-04-20 | Sprint S42 + Fase C concluída — F06.6 Copilot Metrics page | **Sexta e última sprint da Fase C.** Fecha o diferencial competitivo de IA: S37 (OpenRouter+Playground SSE) → S38 (Playground UI + useAgentStream) → S39 (RAG + pgvector + Gemini embeddings) → S40 (Triggers + matcher) → S41 (TTS + ElevenLabs + preview) → **S42 (Metrics)**. **Backend repo** `agent/GetAgentMetrics(orgID, agentID, since, until)`: uma query `agent_sessions GROUP BY state` (total + breakdown open/ended/escalated em única passada) + uma query `agent_messages m JOIN agent_sessions s ON s.id = m.session_id WHERE s.agent_id = $2` (isola do tenant para não contar mensagens de outros agents), coleta `COUNT(*) / SUM(tokens_input) / SUM(tokens_output) / AVG(latency_ms) FILTER (WHERE role='assistant')`. `AvgLatencyMs *float64` nullable quando não há assistant turns. **Handler** `GET /api/v1/agents/:id/metrics?since&until`: default last 30 days quando params faltam; RFC3339 required; **rejeita 400 com code estável**: `INVALID_SINCE` (parse fail), `INVALID_UNTIL` (parse fail), `INVALID_WINDOW` (since >= until), `WINDOW_TOO_LARGE` (> 365 dias — anti-runaway scan). Resposta `metricsView` shape matches UI: since/until (RFC3339), total_sessions, total_messages, tokens_input (int64 — BIGINT cap), tokens_output, avg_latency_ms? (omitempty null), sessions_by_state map[string]int. **Frontend hook** `useAgentMetrics(agentId, {since, until})`: `queryKey` inclui since+until (troca de janela = cache miss limpo); `staleTime` 60s (métricas não precisam real-time); disabled quando qualquer arg faltando; URL-encode automático via URLSearchParams. `AgentMetrics` interface mirror do backend. **Page `AgentMetricsPage`** em `/copilot/:id/metrics` (lazy + `withSuspense`): window picker **7d / 30d / 90d** como segmented button group (`Date` math computa range); **4 KPI cards** (Sessões, Mensagens, Tokens in/out com separador pt-BR `toLocaleString`, Latência média `Math.round().toLocaleString` + " ms" ou "—" quando null); **SessionsStateBar** stacked (open accent/60, ended ink/30, escalated danger/50) com `role=img aria-label` e legenda com contagens; empty state quando total=0. Header com PageHeader eyebrow "Copilot · Métricas" + nome do agent. Link "Métricas" adicionado no Playground editor `mt-auto`. **Routes**: `/copilot/:id/metrics` lazy registrado em routes.tsx ao lado de `/copilot/:id`. **Tests novos**: backend `handler/agents/metrics_test.go` 4 cenários de validação params (invalid since, invalid until, inverted window, > 365 dias — todas 400 com code check); frontend `useAgentMetrics.test.tsx` 2 cenários (disabled sem id/window, GET com since+until URL-encoded). **187/187 em 64 files** (+2 cases, +1 file vs S41). Coverage lines 63.08 → **63.21** (+0.13pp), stmts 60.54 → **60.67** (+0.13pp), funcs 57.39 → **57.57** (+0.18pp), branches 52.33 → **52.99** (+0.66pp). Commits: `7c8b54a` backend · `39583aa` qa · `e82ac6e` frontend. **Critério `WS invalidate no period change`** intencionalmente **descartado**: trocar window picker já troca queryKey, o que invalida cache localmente — WS push só seria útil se contabilizasse a cada nova mensagem, não vale o ruído no bus por um dashboard que tolera dados 60s stale. **Escopo simplificado honesto**: (a) `top 5 triggered_by_lead_attribute` e `conversion_rate (leads que avançaram stage após engagement)` deferred — exigem JOIN com `agent_triggers` + análise temporal sobre `leads.stage_changed_at` via `lead_history` (entidade ainda não existe); (b) Visx FunnelChart + timeseries por dia ficam para quando a aggregation ganhar buckets `GROUP BY date_trunc('day', started_at)` — a forma atual cobre acceptance S42. **FASE C CONCLUÍDA**: 6 sprints entregues em topologia linear cumulativa (S37→S38→S39→S40→S41→S42), cada uma com branch própria + STATE D0xx + merge --no-ff preservado como audit anchor. 4 migrations (0017 pré-existente, 0019 RAG, 0020 triggers, 0021 TTS). Frontend 187/187 em 64 files (+36 cases, +13 files vs início da fase); coverage lines 59.17 → **63.21** (+4.04pp em 6 sprints). Backend: adapters completos OpenRouter (LLM streaming SSE) + Gemini (embeddings RAG) + ElevenLabs (TTS), todos com Mock fallback determinístico. **Deferreds documentados na fase** (para sprints futuras): worker dispatch de triggers (reuse do workflow executor Fase D), TTS outbound pipeline + org_quotas, session persistence no Playground, URL fetch HTTPS anti-SSRF no ingest (hoje só inline text/markdown), Wizard 20+ steps retirado do produto, Visx timeseries quando aggregation ganhar buckets diários. **Próxima fase: D — Automação comercial (S43-S45)** F07 Workflow Builder (canvas xyflow + executor worker + action dispatcher + 12 action types). |
| D060 | 2026-04-20 | Sprint S41 — Fase C.5 F06 Copilot TTS (ElevenLabs adapter + agent config + Playground preview) | **Quinta sprint da Fase C** — config surface + preview endpoint shipam juntos para o admin avaliar voz antes de promover. Outbound pipeline completo (worker message.outbound → mp3 → storage S3 → kind=audio no MessageList + org_quotas.tts_seconds_per_month + enforcement transacional) é deferred porque depende de storage S3 wiring global que não existe no projeto hoje + schema de quotas + hook no adapter Evolution para enviar binário em vez de texto — shipping isso junto empilharia 3 preocupações novas em uma sprint M. **Migration 0021**: `agents += tts_enabled bool NOT NULL DEFAULT false` + `tts_voice_id text NULL` check 2-80 chars (alinhado com outros provider ids do sistema); sem org_quotas ainda. **Config**: `ELEVENLABS_BASE_URL` (default `api.elevenlabs.io`), `ELEVENLABS_API_KEY` required em prod, `ELEVENLABS_MODEL_ID` default `eleven_multilingual_v2` (PT-BR user base tem phoneme coverage melhor que o monolingual English default). **Service `ai/tts.go`**: `TTS` interface com `Name()` + `Synthesize(ctx, voiceID, text) ([]byte, error)`; `ElevenLabsTTS` posta `/v1/text-to-speech/:voice_id` com `xi-api-key` header + `Accept: audio/mpeg`, encoda body JSON `{text, model_id}`, valida voice/text não-vazios + text ≤ 5000 chars client-side (provider cap, bill por char); responde com ai error taxonomy (401/403→ErrAuthFailed, 429→ErrRateLimited, 5xx→ErrProviderUnavailable, 4xx com body preview→ErrBadRequest); cap 5 MB na resposta com `io.LimitReader` para não segurar RAM em resposta bug do provider. `MockTTS` fallback determinístico (SHA-chained 4-byte "MP3?" header + 16 float32 hex = 68 bytes) para dev e tests sem key — boot log warn quando rola. 20s timeout default. **Repo Agent**: `TTSEnabled bool` + `TTSVoiceID *string` no struct; SELECT queries (GetAgent, ListAgents) atualizadas; `UpdateAgentInput` aceita `TTSEnabled *bool` + `TTSVoiceID *string` (passar empty string limpa NULL, 2-80 validado, TrimSpace aplicado). **Handler agents**: `agentView` expõe `tts_enabled: bool + tts_voice_id?: string`; `toAgentView` atualizado; `updateAgentReq` ganha campos opcionais. **Handler playground**: `PlaygroundHandler` ganha `tts ai.TTS` field; `NewPlayground` assinatura vira `(base, provider, embedder, tts)`; nova rota `POST /agents/:id/tts/preview` valida voice (body override OR agent config, 400 NO_VOICE se ambos vazios), default text "Olá! Este é um teste de voz do seu agente Copilot." quando body vazio, 25s ctx, chama tts.Synthesize, responde com `audio/mpeg` + `Cache-Control: no-store`, 503 TTS_UNAVAILABLE quando tts==nil. **main.go**: seleção de TTS — ElevenLabsTTS quando `ELEVENLABS_API_KEY` set (falha de init degrada para Mock com warn), MockTTS fallback explícito com warn quando key vazia. **Frontend hooks**: `Agent.tts_enabled: boolean + tts_voice_id?: string | null`; `UpdateAgentPayload.tts_enabled?: boolean + tts_voice_id?: string | null`. `previewTTS(agentId, {voice_id?, text?})` helper — não passa por useAppMutation porque response é binary not JSON; thin fetch wrapper com `credentials: include`, `X-CSRF-Token` header lido do cookie `__torque_csrf`, lê `res.blob()` e retorna `URL.createObjectURL(blob)` para `<audio src>`; throw com server text em non-2xx. **UI (`TTSPanel` no AgentPlaygroundPage)**: `useEffect` cleanup chama `URL.revokeObjectURL(prev)` quando objeto URL muda ou unmount para evitar blob-leak em memória; checkbox `tts_enabled` + input `tts_voice_id` com placeholder `21m00Tcm4TlvDq8ikWAM`; botão "Ouvir preview" disabled quando voice vazio OR loading; `<audio controls autoPlay>` com `onError` surface como inline message. Draft state inclui tts — dirty detection pega toggle/voice para habilitar Salvar; empty voice_id no PATCH limpa NULL no backend. **Tests novos**: backend `ai/tts_test.go` **10 cenários** (3 construction reject, Name() assertion, happy path httptest assertando xi-api-key + audio/mpeg accept + voice_id path segment, 2 input validation empty voice/text, oversize text, 4 error taxonomy 401/429/5xx/4xx, MockTTS determinism + divergência + empty-input fail); frontend `previewTTS.test.tsx` 2 cenários (POST shape com CSRF + blob→objectURL; throw em non-2xx com server detail). **185/185 em 63 files** (+2 cases, +1 file vs S40). Coverage lines 62.71 → **63.08** (+0.37pp), stmts 60.22 → **60.54** (+0.32pp), funcs 57.23 → **57.39** (+0.16pp), branches 52.01 → **52.33** (+0.32pp) — todos acima do piso 60/60/55/50. Commits: `dab175a` backend · `2deb19c` qa · `ec193cf` frontend. **Escopo deferido honesto**: (a) pipeline outbound — worker `message.outbound` lê `agent.tts_enabled`, gera mp3, uploada pre-signed S3, envia como `kind=audio` ao invés de `kind=text` via adapter Evolution; (b) schema `org_quotas` + `tts_seconds_per_month` + decremento transacional + audit `tts.quota_exceeded` quando cap bate; (c) player de áudio em MessageList (S34) — depende do `kind=audio` chegar. Nenhum desses é bloqueante para o valor entregue (voice branding visível no Playground + promote-when-ready pattern). |
| D059 | 2026-04-20 | Sprint S40 — Fase C.4 F06 Copilot triggers + auto-assignment (data plane) | **Quarta sprint da Fase C**. Entrega a surface de triggers de ativação para Copilot agents — data plane + editor UI completos; o dispatch runtime (worker que consome eventos `lead.created`/`lead.stage_changed`) fica para Fase D porque reusa o executor genérico de workflows. **Migration 0020** `agent_triggers` (id, org, agent_id FK CASCADE, name 2-120, description ≤1000, priority int 1-10000 default 100 — lower wins, filter_json jsonb, is_active bool, created_by FK team_members) + `conversations.assigned_agent_id uuid NULL` FK `ON DELETE SET NULL` (coexiste com `assigned_to` humano). Índices: `(org, priority, created_at) WHERE is_active=true` (matcher scan) + `(org, agent_id)` (editor list) + `(org, assigned_agent_id) WHERE NOT NULL`. Seeds permission: `triggers.view` default=true + `triggers.manage` admin-only. **Service `ai/matcher.go`**: `TriggerRule` carrega `AgentKillSwitch + AgentStatus` do JOIN agents para evitar segundo round-trip; `Match(rules, lead LeadFacts)` walk em priority+created_at ASC, retorna primeira rule cujo filter passa; **guards runtime**: rule com `AgentKillSwitch=true` OR `AgentStatus != 'active'` OR `IsActive=false` pulada. Ops: `eq`, `neq` (stringified round-trip), `contains` (substring case-insensitive — suporta tags []string), `in` (slice OR tags intersection), `present` (non-empty), `absent`; unknown op fail-closed (default false). Empty filter (all+any both empty) = catch-all — pattern para catch-all rule em priority alta. Custom fields via `LeadFacts.Custom map[string]any` — repo flatten top-level de leads.custom_fields. Rating `int16` reconciliado com JSON `float64` (stringified eq). **Repo `agent/triggers.go`**: `CreateTrigger` (guarda ownership do agent cross-tenant), `ListTriggersByAgent` (editor — active+inactive, priority+created_at ASC, json.Unmarshal no filter), `UpdateTrigger` (partial patch), `GetTrigger`, `DeleteTrigger`, `ListActiveTriggers` (matcher projection: JOIN agents para carry kill_switch+status, ordered + WHERE is_active=true), `AssignAgent` (flipa conversations.assigned_agent_id, nil=detach). **Handler agents**: novas rotas `GET /agents/:id/triggers`, `POST /agents/:id/triggers` (CreatedBy via `session.TeamMemberID`), `PATCH /triggers/:tid`, `DELETE /triggers/:tid`. Eventos WS `agent_trigger.{created,updated,deleted}` com patch agent_trigger + entity_id. **Frontend hooks**: `TriggerPredicate` (field/op/value) + `TriggerFilter` (all?/any?); `AgentTrigger` interface; `useAgentTriggers(id)` com WS invalidation; `useCreateTrigger` (list invalidate), `useUpdateTrigger`, `useDeleteTrigger`. **TriggersPanel** no rodapé do editor Playground (após KnowledgePanel): form inline com name + field input + op select (6 opções) + value input condicional (oculto para present/absent, placeholder "CSV" para in) + priority input number 1-10000; rule list com nome em bold, priority badge, inactive badge, summary textual ("field op \"value\" AND ..." ou "catch-all"), Ativar/Desativar toggle, Trash delete. **Tests novos**: backend `ai/matcher_test.go` **17 cenários table-driven** (no rules, empty filter, kill-switch/disabled/inactive guards, priority lower-wins, eq/neq, contains tags slice, in list + tags intersection, present/absent, all+any composition, unknown op fail-closed, custom field, rating int16 vs float64); frontend `useTriggers.test.tsx` 5 cenários (disabled sem id, GET scoped, POST filter body, PATCH partial, DELETE). **Ratchet coverage vitest → 60/60/55/50** (era 55/55/50/48 desde remediation-B). Medido: lines 62.23 → **62.71** (+0.48pp), stmts 59.81 → **60.22** (+0.41pp), funcs 56.40 → **57.23** (+0.83pp), branches 52.03 → **52.01** (-0.02pp — dentro da tolerância do ratchet). **183/183 em 62 files** (+5 cases, +1 file vs S39). Commits: `93decb3` db · `515dc77` backend · `ed144d8` qa · `68ca84b` frontend. **Escopo deferido honesto**: worker kind `conversation.assign_agent` + event bus wiring em `lead.created`/`lead.stage_changed`/conversation.created + dispatch real na inbox (badge "agent attending") ficam para Fase D S43-S45 onde o workflow executor genérico absorve o pattern; o plano S43 já prevê `trigger.lead_created` + `trigger.lead_stage_changed` como node types — implementar dispatch aqui seria duplicação que seria excluída em S43. |
| D058 | 2026-04-20 | Sprint S39 — Fase C.3 F06 Copilot RAG (pgvector + Gemini embeddings + retrieval) | **Terceira sprint da Fase C**, F06.3. Liga o RAG: pgvector em dev (docker-compose db image `pgvector/pgvector:pg15`), migration 0019 com `CREATE EXTENSION IF NOT EXISTS vector` + `knowledge_chunks.embedding vector(768)` + HNSW index `vector_cosine_ops (m=16, ef_construction=64)` sobre rows com embedding não nulo + `agents.knowledge_collection_id` FK opcional + seed permission keys `knowledge.view|knowledge.manage`. **Backend embedder**: service `ai/embeddings.go` com `Embedder` interface agnóstica, `GeminiEmbedder` concreto (batchEmbedContents, taskType=RETRIEVAL_DOCUMENT, rejeita batch>100, 401/429/5xx/dim-mismatch mapeados) + `MockEmbedder` (SHA256-chained deterministic 768d) como fallback quando `GEMINI_API_KEY` vazio. Boot log imprime warning alto para prod que rodar com mock. **Chunking**: `ai/chunk.go` paragraph-aware com target 500 tokens + overlap 50, degrada para sentenças e depois palavras quando paragraph > budget. Ord preservado para recomposição. **Repo knowledge**: `ListCollections` com live `source_count` via LEFT JOIN agregado; `GetCollection/ListSources/GetSource/UpdateSourceStatus`; `InsertChunks` em tx única que apaga chunks prévios do source (idempotente); `SimilaritySearch` (`<=>` cosine, tenant-scoped → agent.collection_id → status=ready → embedding IS NOT NULL, topK clamp 1-20); `SetAgentKnowledgeCollection` com cross-tenant guard (ownership check); `encodeVector` renderiza `[0.1,0.2,...]` literal (pgvector não tem encoder pgx nativo, cast ::vector no lado SQL). **Service knowledge**: `Ingest()` orquestra chunk→embed (batches 100)→InsertChunks→flip status queued→ingesting→ready|failed; erros de embed marcam source como failed com mensagem; `RunDetached()` goroutine 10min ceiling com recover panic marcando failed. **Handler agents**: novas rotas `GET /knowledge/collections`, `GET /knowledge/collections/:cid/sources`, `PUT /agents/:id/knowledge-collection`. `enqueueSource` gate kind=text|markdown requer content não-vazio; URL kinds requerem URI (validado já pelo repo com allowlist https only). `WithIngest()` pattern fluent liga o serviço; URL fetch deferred S40+. `agentView` expõe `knowledge_collection_id`. **Playground handler**: se `agent.knowledge_collection_id != nil` e embedder presente, embeda último user turn (5s bounded), `SimilaritySearch topK=5`, `prependContext` formata chunks com source_id[:8]+ord+distance e delimitador `---\n\n` acima do system prompt. Falhas de retrieval degradam silenciosamente para base prompt. **Frontend hooks**: `Agent.knowledge_collection_id?: string | null`; `KnowledgeCollection` + `KnowledgeSource` interfaces; `useKnowledgeCollections` (30s stale, WS-invalidated em `knowledge.collection_created`); `useKnowledgeSources(cid)` (disabled sem cid, WS em `knowledge.source_enqueued`, `refetchInterval` 3s enquanto há source queued/ingesting, auto-stop em ready|failed); `useCreateCollection/useEnqueueSource` invalidam listas; `useBindAgentCollection` PUT com `collection_id: string | null`. **Playground UI**: componente `KnowledgePanel` no rodapé do editor — select com live source_count, form inline "Nova coleção" (cria + liga), lista de fontes com badge de status, form inline text-ingest (title + textarea) que chama `useEnqueueSource kind=text`. **Tests**: `ai/embeddings_test.go` 9 cenários (mock determinism/batch-order/per-input/dim bounds; gemini construção rejeita http/empty; batchEmbedContents happy path mirror-count + order; 401/429/dim-mismatch/oversize-batch/empty-input); `ai/chunk_test.go` 6 cenários (empty, single paragraph, split, overlap, order, long sentence word-split); `handler/agents/playground_test.go` 3 cenários (prependContext ordering + sourceID prefix + delimiter; classify default UNKNOWN); frontend `useKnowledge.test.tsx` 5 cenários (collections GET, sources disabled sem id, sources GET scoped, enqueue POST shape, bind PUT null). **178/178 tests em 61 files** (+5 cases, +1 file vs S38 front); backend Go tests não executáveis no host (sem Go instalado — runtime validation segue pendente como nos sprints anteriores). **Coverage**: lines 61.56 → **62.23** (+0.67pp), stmts 59.19 → **59.81** (+0.62pp), funcs 55.55 → **56.40** (+0.85pp), branches 51.35 → **52.03** (+0.68pp). **Escopo deferido**: worker-based ingest (hoje é goroutine detached) + URL fetch com HTTPS/SSRF allowlist + UI de browsing de coleções + re-ingest button + BindCollection via PATCH /agents/:id único (hoje o endpoint dedicado é PUT /agents/:id/knowledge-collection). |
| D057 | 2026-04-20 | Sprint S38 — Fase C.2 F06 Copilot Playground UI + useAgentStream | **Segunda sprint da Fase C**. Substitui o mockup seed-based `AgentsPage.tsx` por implementação real + adiciona editor/chat Vercel-style em `/copilot/:id`. **Backend mínimo**: agentView expõe `system_prompt` (string) para o editor renderizar a versão persistida sem roundtrip extra; `toAgentView` helper vira fonte única do shape (antes havia construção literal duplicada em playground.go que não propagava o novo campo). **Frontend hooks**: `Agent.system_prompt` tipo atualizado; `UpdateAgentPayload` + `useUpdateAgent` (PATCH /api/v1/agents/:id) novos. **useAgentStream.ts** novo: consome SSE do playground via `fetch` + `ReadableStream.getReader()` (EventSource não serve — é GET-only sem CSRF); parser próprio split por `\n\n` respeitando frames `event: delta|done|error`; estados idle/streaming/done/error/cancelled; AbortController cancela sincronicamente e unmount não leaka request; stream sem [DONE] explícito ainda assim vira done ao fechar. **AgentListPage.tsx** (novo, substitui AgentsPage seed-based): useAgents WS-aware com QueryBoundary skeleton/empty/error; cards com nome clicável → /copilot/:id, description, metadata (model+temp+tokens), Ativar/Desativar + toggle kill-switch inline; botão "Novo agente" disabled (S39 terá wizard). **AgentPlaygroundPage.tsx** (novo, /copilot/:id): layout 2 colunas estilo Vercel Playground — editor esq (name + system_prompt textarea 10 rows + model + temp + max_tokens) com draft local + dirty detection, Salvar desabilita quando não-dirty; chat dir com TranscriptBubble por speaker (evitei prop `role` por conflito ARIA), cursor piscante durante streaming, Parar cancela via abort, Regenerar remove último assistant + re-envia; banner de erro vermelho com code+message quando stream.error. **Rota** `/copilot/:id` adicionada, `AgentsPage` antigo removido (arquivo + teste). **Tests**: useAgentStream.test.tsx 4 cenários (happy 3 deltas + done + tokens, server-sent error frame, HTTP 503 = HTTP_503 code, heartbeats/malformed ignorados sem tear); AgentListPage.test.tsx 2 cenários (happy render + empty state). **Coverage delta** (vitest --coverage live): lines 59.17 → **61.56** (+2.39pp), stmts 56.70 → **59.19** (+2.49pp), funcs 55.33 → **55.55** (+0.22pp), branches 49.58 → **51.35** (+1.77pp); thresholds 55/55/50/48 acima do piso. **173/173 tests em 60 files** (+5 cases, +2 files vs S37). typecheck+lint verdes. **Escopo parcial honesto**: conversa do playground é local (não persiste em agent_sessions); session persistence + kill-switch mid-stream re-check ficam para S39 quando o playground sair de "teste" para "produção com histórico". |
| D056 | 2026-04-20 | Sprint S37 — Fase C.1 F06 Copilot (OpenRouter adapter + Playground SSE) | **Primeira sprint da Fase C** (F06 Copilot/IA, maior diferencial competitivo). Schema completo (agents + agent_sessions + agent_messages) já estava em migration 0009 (S15); nada a migrar. **Repositorio agent/** estendido com `UpdateAgent` partial patch (name 2-120, description, system_prompt 10-16000, model 2-80, temperature 0-2, max_output_tokens 16-8192, tools_allowlist) com validations matching os DB CHECKs antes de tocar SQL. **Service ai/openrouter.go** novo: Provider interface agnóstica (Name/Chat/Health) para swap futuro direto Anthropic/OpenAI sem mexer em handler. OpenRouter concreto com Config enforce https:// + APIKey obrigatória + default timeout 30s + HTTPClient injetável; POST /chat/completions com stream:true → SSE consumido por `parseSSE` (bufio.Scanner buffer 1 MiB para modelos longos, ignora heartbeats malformados, emite Chunks {Delta, Done, InputTokens, OutputTokens}); [DONE] sentinel OU EOF finaliza com terminal Chunk{Done:true} (defensive); 401/403 → ErrAuthFailed, 429 → ErrRateLimited, 5xx → ErrProviderUnavailable, 4xx outros → ErrBadRequest com body truncado 200 chars. **Handler agents/playground.go**: PlaygroundHandler wrappa Handler base + ai.Provider (nil permitido — endpoint retorna 503 PROVIDER_UNAVAILABLE em degradação graciosa). Expõe PATCH /agents/:id (update) + POST /agents/:id/playground/message SSE. Proteções SSE: system role no body → 400 SYSTEM_FORBIDDEN (agent config é fonte de verdade), messages vazio → 400 EMPTY_CONVERSATION, KillSwitch=true → 409 KILL_SWITCH (bloqueia ANTES de dialar provider), Flusher ausente → 500 NO_FLUSH (nunca buffer resposta), ctx timeout 60s + out chan buffered 16. Events emitidos estáveis para frontend: "delta" (content string), "done" (input_tokens+output_tokens), "error" (codes AUTH_FAILED/RATE_LIMITED/PROVIDER_UNAVAILABLE/PROVIDER_REJECTED/TIMEOUT/UNKNOWN). **Config**: OPENROUTER_BASE_URL (default openrouter.ai/api/v1), OPENROUTER_API_KEY (vazio = playground disabled), OPENROUTER_REFERER + OPENROUTER_TITLE. **Wiring main.go**: build OpenRouter se OPENROUTER_API_KEY presente (init falha = Warn log + nil provider, não mata boot); `agentshandler.NewPlayground(base, provider).Routes(admin)`. **Tests**: openrouter_test.go httptest-backed 7 cenários (bad config rejection, Name, happy path SSE 3 frames + [DONE] + usage, 401/429/502 mapping, server sem [DONE] → terminal Done emit); agent_test.go gated DATABASE_URL (Create draft, UpdateAgent partial patch, invalid temperature rejected, SetStatus+SetKillSwitch persist, cross-tenant Get+Update refuse + post-cross-tenant name preservation). Frontend vitest 168/168 sem regressão. **Escopo parcial honesto**: session persistence (AppendMessage pre/post chat) + kill-switch mid-stream re-check ficam para S38 quando UI consumir. |
| D055 | 2026-04-20 | Remediation-B — lift testes + observabilidade (pré-Fase C) | Endereça os 2 gaps mais visíveis do D054 (Testes 58, Observabilidade 78). **Frontend tests** (+9 files, +20 cases): useBootstrap, useOperation, useCockpit, useAgents.extra (9 cenários cobrindo sessions/knowledge/delete/activate), useLeads.extra (CRUD completo), usePipes (list/stages/entries/move), ProductsPage integration test, MasterPage integration test (health+orgs paralelos), AgentsPage smoke test. **Primeiras page integration tests** da codebase — fechando o gap "14 features sem test". **Coverage delta real medido** (vitest --coverage): lines 50.22 → **59.17** (+8.95pp), statements 48.07 → **56.70** (+8.63pp), functions 42.96 → **55.33** (+12.37pp), branches 45.96 → **49.58** (+3.62pp). **Thresholds ratcheted**: 45/45/40/40 → **55/55/50/48**. **168/168 tests em 59 files** (+20 cases, +9 files vs D054). **Backend**: SENTRY_TRACES_SAMPLE_RATE default 0.0 → **0.1** (tracing visível em prod sem flood — fecha gap D054 "traces=0"); audit repo `Append` agora passa payload por `scrubPayload` recursivo redigindo case-insensitive 14 keys (password/token/auth/cookie/csrf/api_key/secret/email/password_hash) com marker `[Scrubbed]` — fecha gap D054 "PII parcial no audit_log"; 2 unit tests cobrem redaction recursiva em map/slice/primitive + JSON smoke. Handler products_test.go adicionado (sanity shape lock + router mount). **Re-score quantificado**: Testes 58/100 → **72/100** (+14pp); Observabilidade 78/100 → **86/100** (+8pp); Segurança 92/100 → **94/100** (+2pp pelo audit scrub); **Score composto 84/100 → 87/100** (+3pp). typecheck+lint verdes, zero regressão. Branch `sprint/remediation-B` → merge `--no-ff` para develop. |
| D054 | 2026-04-20 | Auditoria Pré-Fase-C com score composto 0-100 | Executada por 4 sub-agentes Explore em very-thorough (backend+segurança, frontend+qualidade, cobertura real via vitest --coverage run, CI/infra/ops). Estado auditado: `develop @ 5b57023` após S36. **Score composto: 84/100** (média ponderada). Por dimensão: **Segurança 92/100** (OWASP 9/10 PASS, 1 gap cosign + 1 parcial PII no audit payload), **Multi-tenancy 97/100** (defense-in-depth com StripOrganizationID + OrgIDFrom + WHERE organization_id 100% sampled + 3 isolation tests), **Arquitetura 91/100** (4 camadas limpas, 0 import cycle, 7 ADRs cumpridos, 65 CREATE INDEX em 18 migrations), **Qualidade de código 88/100** (TS strict + 0 any + 0 @ts-ignore + 4 TODOs totais + 1 file >500 LOC), **DX 94/100** (53 decisões STATE + vault sincronizado + 7 ADRs + 2 runbooks + k6 + pentest checklist), **Infra/CI 86/100** (4 workflows + Dependabot + distroless + cosign como gap), **Eficiência 80/100** (67 chunks + entry 109.7 KB/35.4 KB gzip + 65 índices; gap useInfiniteList 2/N), **Observabilidade 78/100** (Sentry + zerolog + request_id; gap OpenTelemetry não wired), **Escalabilidade 75/100** (pgx pool + cursor + SKIP LOCKED; gap WS in-process + rate limiter in-memory), **Testes 58/100** (vitest 148/148 em 50 files com cobertura lines 50.22/stmts 48.07/funcs 42.96/branches 45.96 medida real; backend 32 test files + 92 Test funcs + 16 integration tests; gaps: 14 pages React sem test + 24 handlers Go sem integration). **Veredito**: apto a entrar na Fase C (F06 Copilot) em S37. Relatório completo em `Torque-dir-new/09 - Backlog/Auditoria Pre-Fase-C - Scored 2026-04-20.md` com evidência cite-per-citation + recomendações priorizadas de gaps não-bloqueantes. |
| D053 | 2026-04-20 | Sprint S36 — Fase B concluída (takeover audit + teamMemberId em /me) | **Quarta e última sprint da Fase B** (F04 Inbox). Fecha os dois gaps remanescentes: identificação do caller no Inbox e audit trail de takeover. **Backend**: `meUser` DTO ganha `team_member_id uuid` populado em login (chosen.TeamMemberID) e /me (sess.TeamMemberID); Handler.WithAudit chain-style recebe auditRepo opcional; `assign()` agora detecta takeover (prior.assigned_to != nil, novo != prior, novo != session.TeamMemberID) e escreve audit_log row com `action="conversation.takeover"` + payload `{previous_owner, new_owner, by_member}` — self-assign e first-assign pulam audit para evitar spam; WS event `conversation.assigned` ganha flag `takeover: bool` no payload. Wiring em main.go: `inboxhandler.New(...).WithAudit(auditrepo.New(pool)).Routes(t)`. **Frontend**: `SessionBundle.user.teamMemberId` campo obrigatório (contract); `MeResponse.user.team_member_id` decoded no toSessionBundle (snake→camel ADR-001); useSession safe-default inclui teamMemberId:''; devSession DEV_SESSION ganha id fake 00..c1; InboxPage lê session.user.teamMemberId e passa para `<ConversationHeader currentMemberId={...}>` via conditional spread (exactOptionalPropertyTypes). Badge "Minha" + botão "Assumir" funcionam end-to-end. **Escopo parcial honesto**: SLA watcher (worker periódico disparando conversation.sla_breached após X min sem resposta) fica para sprint de workers futuros — o pool atual processa operations do banco mas não tem scheduler periódico nativo. Pipeline async message.outbound via Evolution adapter idem. **Validação**: 148/148 vitest em 50 files (sem regressão), typecheck+lint verdes. **Fase B concluída**. Próxima: **Fase C** (S37-S42, F06 Copilot) — Agent entity + Playground backend + OpenRouter adapter em S37. |
| D052 | 2026-04-20 | Sprint S35 — Fase B.3 F04 ComposerBar + Evolution adapter + message_templates | **Terceira sprint da Fase B**. Fecha o ciclo de UI do Inbox (envio possível) e entrega o provider real para Evolution API. **Migration 0018** `message_templates` (id, org, name CHECK 2..120, body CHECK 1..16000, variables text[], is_active, created_by FK, timestamps; unique (org,name); partial index active; trigger updated_at; +2 permission keys templates.view/manage). **Evolution adapter** em `internal/service/integration/evolution/evolution.go`: implementação concreta do MessagingProvider de S27. Defensive config — BaseURL deve ser https://, APIKey não-vazia, CircuitBreaker threshold=5/cooldown=30s via shared pkg. SendMessage mapeia kind→endpoint (/message/sendText ou /sendMedia); 401/403 → ErrAuthFailed; 429 → ErrRateLimited; 5xx → ErrUnreachable; 4xx não-auth → erro literal truncado 200 chars. Reply malformada não derruba success. Health pinga /instance/fetchInstances. **Unsupported kinds** (sticker/audio dispatch) → ErrUnsupported. **message_template repo + handler**: Template entity com variables text[], CRUD completo com ErrInvalid/ErrNameTaken/ErrNotFound; handler split Read (member) + Admin (RequireRole); 16000 chars body alinhado com campaigns. **Frontend** `useMessageTemplates` hook + `renderTemplate(body, values)` util client-side que substitui `{{chave}}` e preserva placeholders ausentes (WYSIWYG — user vê o que precisa preencher). **MessageComposer** (~230 LOC vs ~400 LOC do v8): textarea auto-grow até 8 linhas, Enter envia (Shift+Enter quebra linha), template picker popover com variáveis resolvidas a partir de contact_name, isPending mostra "Enviando…", error restaura rascunho, disabled em conversations resolved/archived. **Escopo parcial explícito**: o pipeline async via worker `message.outbound` consumindo operations (que chamariam o Evolution adapter e atualizariam message status via WS) fica para ticket separado — hoje o handler ainda faz insert direto (padrão S12), mas o provider está pronto para plugar. Anexos/emoji/áudio nativo ficam como polish. **Tests**: evolution_test.go (httptest-backed — New rejects bad config, happy path com apikey forward, 401→ErrAuthFailed, sticker→ErrUnsupported, Name()="evolution"); template_test.go gated DATABASE_URL (CRUD happy + ErrNameTaken + ErrInvalid + active_only filter + Delete idempotent + cross-tenant Get refuses); useMessageTemplates.test.tsx (URL targeting + renderTemplate 4 casos); MessageComposer.test.tsx (RTL render: disabled vazio, POST trimmed body, disabled resolved). **Vitest 148/148 em 50 files** (+10 casos, +2 files vs S34). typecheck+lint verdes. |
| D051 | 2026-04-20 | Sprint S34 — Fase B.2 F04 MessageList + MessageBubble com WS live | **Segunda sprint da Fase B**. InboxPage ganha histórico real substituindo o EmptyState placeholder por `<MessageList conversationId={id}/>`. **MessageBubble primitive** renderiza os 7 kinds: text (whitespace-pre-wrap break-words), image (`<img loading="lazy">` max-h-72), audio (`<audio controls>`), video (`<video controls>` max-h-72), document (link accent underline), sticker (img 96×96), system (chip centralizado sem bubble, tone muted). Direction variants: outbound → direita + bg-accent/15, inbound → esquerda + bg-surface. Status icon só no outbound (Clock/Check/CheckCheck/TriangleAlert via lucide); status='read' → text-info, 'failed' → text-danger. Design choice: `<img>`/`<audio>`/`<video>` nativo cobre 99% do uso — nada de react-player/react-audio (economiza ~200 KB). **MessageList**: orquestra scroll sobre `useMessages` (S12 hook já com WS append idempotente em message.sent via dedup por id). useLayoutEffect auto-scroll para bottom em new outbound OU quando user está no bottom (threshold 120px); preserva posição em inbound quando user scrollou para cima (leitura de história); `conversationId` change reseta scroll anchor refs. QueryBoundary com emptyFallback remetendo ao S35. **useMemo** em `items = query.data ?? []` estabiliza identity across renders (exhaustive-deps). **Escopo parcial**: backend cap 100-200 msgs sem cursor (`// S13 will layer cursor pagination`); infinite scroll reverso para history antiga chega em S35 junto com backend cursor + envio via Evolution adapter. **Tests** `MessageList.test.tsx` 3 cenários: render oldest-first com text+outbound+status, system kind renderiza centralizado (não bubble), empty state remetendo S35. Vitest 138/138 em 48 files (+3 casos, +1 file). typecheck+lint verdes; coverage ratchet (lines 49.85 vs floor 45) mantido. |
| D050 | 2026-04-20 | Sprint S33 — Fase B.1 F04 InboxPage shell real | **Primeira sprint da Fase B** (F04 Inbox chat real-time) do plano D046. Substitui o mockup que consumia `lib/seed.ts` por wiring real ao backend `/api/v1/conversations` (disponível desde S12-S13). **Novos componentes** em `torque-web/src/features/inbox/`: ConversationList (URL-driven filters via useSearchParams do parent; useConversations WS-aware invalida em conversation.read|assigned|state_changed + message.sent; server-side state filter + client-side search por nome/handle; QueryBoundary centraliza loading/empty/error; ConversationRow com ChannelBadge variant=dot, last_message preview, timestamp relativo, unread badge cap 99+). ConversationHeader (<200 LOC auditável): Avatar + nome + ChannelBadge icon-only + badges "Minha"/"Resolvida"; 3 ações rápidas Assumir (useAssignConversation), Resolver/Reabrir (useSetConversationState). **InboxPage reescrito**: 370 LOC mockup → ~90 LOC wiring real com 2 colunas; searchParams persiste q/state/c para links compartilháveis. **Comparação v8**: ChatWhatsApp.tsx 2443 LOC (god component D045) decomposto aqui em ~500 LOC total em 3 arquivos. **TODO S34**: currentMemberId precisa ser exposto em SessionBundle. **MessageList + ComposerBar** TODOs explícitos para S34/S35. **Tests**: `ConversationList.test.tsx` com 3 cenários (render + client-search + stateFilter propagation + onSelect click). Vitest 135/135 em 47 files. typecheck + lint verdes. |
| D049 | 2026-04-20 | Sprint S32 — Fase A concluída (tenant isolation + Dependabot + gosec CI) | **Terceira e última sprint da Fase A** do plano D046. Fecha 3 dos 4 follow-ups P0 do D045. **Cross-tenant isolation tests** gated DATABASE_URL em 3 repos críticos: `lead_tenant_isolation_test.go` (canonical pattern: Get + Update + SoftDelete + List todos provando que orgB não vê/muta orgA data; extra assert post-Update verifica que nome real permaneceu inalterado); `task_tenant_isolation_test.go` (Get + List para tasks, adjacente a money); `proposal_tenant_isolation_test.go` (Upsert + Get para pipe_proposals, strict money flow). Outstanding: workflow/campaign/inbox/subscription/agent repos ainda precisam de coverage paralelo — rastreado como non-blocking. **Dependabot** (`.github/dependabot.yml`): weekly updates segunda-feira 07:00-08:00 America/Sao_Paulo para gomod (torque-api), npm (torque-web), github-actions; limits 5 PRs código / 3 PRs CI; grouping para @radix-ui/@tanstack/@dnd-kit/@fontsource reduzir ruído; commit prefixes chore(deps/go|npm|gha); security advisories bypassam schedule (default). **gosec no ci.yml main lint-go job** (não só no security-scan nightly): `-severity=high -confidence=medium` gate falha o job em findings high; lower-severity ainda tracked em security-scan.yml. Fecha o follow-up D044. **OpenAPI refresh** (último P0) adiado como FOLLOW-UP explícito: regeneração completa requer Go toolchain no host (kin-openapi walker sobre chi router), consistente com todo o runtime backend não-rodado. Spec atual cobre probes+auth+me+operations; 14 handlers novos pós-S04 aguardam. **Validação**: vitest 132/132 em 46 files (sem regressão), typecheck+lint verdes. **Fase A (P0) concluída** — próxima: Fase B (S33-S36 F04 Inbox chat real-time). |
| D048 | 2026-04-20 | Sprint S31 — Vitest thresholds + 10 hook test files (Fase A.2) | **Segunda sprint da Fase A** do plano D046. Fecha o P0 §2 do audit D045 (threshold enforcement) + §3 (10 hooks críticos sem teste). **10 test files novos** em `torque-web/src/hooks/__tests__/`: useTasks (list+create+complete), useTaskActions (3 cenários locais de cache: startTask, completeTask, reopenMissed com resequenceQueue), useCampaigns (list+filter+create→launch), useProposals (upsert PUT+send+accept POST), useOrgSettings (GET/PATCH org + POST webhooks), useBilling (GET subscription + POST checkout + POST cancel), useOnboarding (GET+POST steps+shouldGate predicate), useConfirmations (PUT upsert+confirm+no-show), usePipesAdmin (createPipe, archivePipe DELETE, createStage, updateStage PATCH — os admin methods de S22), useMaster (health+organizations+impersonate). **vitest.config.ts thresholds**: initial ratchet `lines: 45, statements: 45, functions: 40, branches: 40` — ligeiramente abaixo do measured (lines 48.95 / stmts 46.75 / funcs 41.79 / branches 43.18) para dar folga no primeiro run sem flaky; **todo sprint futuro ratcheteia para cima**, target 70/65/60/55 por S40. **Escopo measurement**: `include: ['src/hooks/**', 'src/api/**', 'src/lib/**']`; `exclude` pragmático — fixtures dev (seed.ts), trivial resolvers (useWSStatus/useCanPerformAction/usePermission/useSession), generated types, routes/App/main. **ci.yml frontend job** agora chama `npm run test -- --coverage` → thresholds gateiam o build. **Resultado**: 132/132 tests em 46 files (+33 tests novos, +10 files novos vs S30), typecheck+lint verdes, coverage thresholds green. Mirror do v8's D007→D025 ratchet (9.33% → 73.53%). |
| D047 | 2026-04-20 | Sprint S30 — Lazy loading + bundle strategy (Fase A.1) | **Primeira sprint da Fase A** do plano de paridade com v8 (D046). **lazyRetry** em `torque-web/src/lib/lazyRetry.ts`: exponential backoff (200ms→400ms→800ms cap 2s) vs flat-1s do v8; 3 tentativas totais (parity v8 retries=2); rejeição final embrulha em `AppError('CHUNK_LOAD_FAILED', status:0)` e dispara `notifyAppError` (Sentry + toast) — v8 silently re-throws. **RouteSkeleton** em `src/shell/RouteSkeleton.tsx`: fallback minimal dark-first com header + 3 blocos dimensionados para viewport típico (evita CLS). **routes.tsx reescrito**: todas as 17 rotas de página + shells (AppShell, CockpitShell, CockpitView) passam por `lazy(() => lazyRetry(() => import(...).then(m => ({default: m.X}))))` (named→default wrap); helper `withSuspense(node)` centraliza `<Suspense fallback={<RouteSkeleton />}>`. **vite.config.ts manualChunks expandido**: query-vendor (38 KB isolado) + sentry-vendor (16 KB isolado) adicionados aos existentes react-vendor/radix-vendor/motion-vendor; dnd-vendor reservado para S43 quando @xyflow chegar. **Resultado do build**: **67 chunks** (antes era 1 bundle monolítico ~500 KB); entry principal `index.js` caiu para **109.7 KB (35.4 KB gzip)**, vendors isolados (react 208 KB, radix 118 KB, query 38 KB, sentry 16 KB, motion 1 KB). Páginas individuais em 5-36 KB cada (KanbanPage 23.5, InboxPage 12.6, SettingsPage 15.7, CockpitView 35.7 é a maior — candidata futura a split interno). **Tests** `lazyRetry.test.ts` com 3 cenários usando vi.useFakeTimers: happy path sem retry, retry com backoff avançado, rejeição final após cap. **Resultado vitest 102/102 em 36 files** (antes 99/99 em 35). typecheck + lint zero warnings. |
| D046 | 2026-04-20 | Plano de Ação Paridade v8 — Sprints S30-S52 | **23 sprints em 7 fases** (~28 semanas) para fechar o gap funcional identificado em D045, usando v8 como referência FUNCIONAL e mantendo stack Torque-v2 (Go + pgx + React + ADRs) como referência TÉCNICA. **Fase A (S30-S32)**: foundation repair — lazy loading com lazyRetry (porta padrão v8), vitest thresholds enforcement + 10 hooks faltantes, cross-tenant isolation integration tests, Dependabot, OpenAPI refresh. **Fase B (S33-S36)**: Inbox chat real-time — ConversationList + MessageList com WS live updates + ComposerBar com Evolution API adapter real + SLA watcher + takeover. **Fase C (S37-S42)**: Copilot/IA — Agent entity + Playground com OpenRouter adapter + SSE streaming, playground UI, Knowledge collections com pgvector + Gemini embeddings 1536d, triggers de ativação por filtros de lead, TTS ElevenLabs, Metrics page. **Fase D (S43-S45)**: Workflow Builder — canvas @xyflow/react com 6 node types + executor worker com ActionDispatcher + 12 action types totais + Execuções UI com debugger. **Fase E (S46-S48)**: Campanhas UI completa (wizard + kanban + dispatch rules) + Performance consolidado (Ranking + Metas + Premiações + Comissões) + Pipes custom + Upsell + Agenda interna + TV Dashboard. **Fase F (S49-S50)**: Integrações reais — Google Calendar OAuth + tokens criptografados AES-GCM, TinyERP push-order, Meta Ads Insights, SZ.Chat provider, lead webhook público com HMAC. **Fase G (S51-S52)**: hardening produção — AsaasProvider real com dual-review obrigatório + quota enforcement runtime; OpenAPI refresh final + cosign keyless OIDC + gosec high severity + pentest staging + k6 load test real. **Decisões travadas**: playground-only (wizard deprecated já no v8, replicado aqui), adapter pattern de S27 reusado para providers reais, topologia linear cumulativa mantida (nenhum paralelismo entre sprints), ratchet de cobertura vitest 55→70 ao longo das sprints. **Riscos monitorados**: Gemini/OpenRouter API change (adapter + snapshot tests), scope creep em F06 (playground-only enforced), pgvector perf em tenants grandes (IVFFlat index na S39), dual-review atrasar S51 (review em paralelo desde S50). **Relatório completo** em `Torque-dir-new/09 - Backlog/Plano de Acao Paridade v8 - Sprints S30-S52.md` com KPIs por fase, template de checklist por sprint, e dependency graph mermaid. |
| D045 | 2026-04-20 | Análise comparativa v8milennialsb2bv2 vs Torque-v2 | Auditoria profunda executada por 3 sub-agentes Explore em very-thorough mode. **v8**: 47 lazy routes, 142 edge functions, 33 shared libs, 325 migrations com 120 RLS policies, 142 test files, cobertura enforced 73.53% lines / 68.58% stmts, **~30 orgs ativas em produção**. **Torque-v2**: 15 rotas reais + 2 placeholder, 24 handlers Go, 21 repos, 17 migrations, 62 test files (35 front + 27 back), cobertura ~22% sem enforcement. **Score composto**: v8 67.4/100, Torque-v2 69.5/100 (empate técnico). **Vantagens Torque-v2**: segurança radicalmente superior (httpOnly cookies vs localStorage v8, CSP tight, CSRF double-submit, PII scrubbing, rate limiter, StripOrganizationID middleware), arquitetura 7 ADRs formalizados, multi-tenancy defense-in-depth (middleware + per-query), CI/CD completo (4 workflows), observabilidade com runbooks + k6 load test committed. **Lacunas críticas**: zero lazy loading no routes.tsx (bundle inicial 3-5× maior que necessário), F06 Copilot 0% implementado (v8 tem 10 edge functions + RAG + TTS + embeddings), F04 Chat WhatsApp real-time ausente (v8 ChatWhatsApp 2443 LOC + useWhatsAppChat 1099 LOC), 9+ hooks críticos sem teste, F07 Workflow executor + @xyflow canvas não construídos. **Esforço paridade**: 26-34 semanas (≈6-8 meses) 1 dev full-stack. **Recomendação**: NÃO portar código v8 (viola ADRs centrais — localStorage, offset pagination, Supabase direto frontend). Portar apenas conhecimento: mapa de entities, 30 action types do workflow-handler, bundle strategy (lazyRetry), copy PT-BR. Relatório completo em `Torque-dir-new/09 - Backlog/Analise Comparativa v8 vs Torque-v2.md` com backlog priorizado P0-P3 (P0: lazy loading + vitest thresholds + tenant isolation test; P1: F04/F06/F07; P2: F08-F14; P3: OpenAPI refresh + cosign + gosec + impersonation cookie). |
| D044 | 2026-04-20 | Sprint S29 — Security hardening + load test + runbooks (FINAL) | **Sem código de produto**. **Security audit** em `.specs/security/security-hardening-audit-2026-04-20.md`: walkthrough completo OWASP Top 10 2021 verificando cada invariante no código (A01-A10), identificando 5 follow-ups não-bloqueantes (Dependabot, GHCR keyless signing, impersonation cookie swap atomic com audit row, OpenAPI refresh post-S04, gosec no CI), checklist pentest para staging. Verdict geral: PASS com caveats documentados. **Load test** em `.specs/loadtest/api-baseline.k6.js`: script k6 com 100 VU / 5 min hold + ramps; thresholds p95<500ms + p99<1500ms + error_rate<1% + checks>99% (falha o run se violadas); dashboard mix batch (bootstrap+me+leads+pipes+analytics) + 5% writes. Rodado via `BASE_URL=... TORQUE_SESSION_COOKIE=... k6 run ...`. **Runbooks** em `.specs/runbooks/`: incident-response.md (severity table, 5-min checklist Sev1/Sev2, rollback por re-dispatch com run_migrations=false, rollback migrations escape hatch com aviso sobre down-migration destrutiva, DB emergency, WS saturation, billing triage, escalation contacts); oncall-basics.md (access prereqs, daily health check, metrics to watch, symptom→first-check matrix, quick SQL snippets, do/don't list). **.github/workflows/security-scan.yml**: nightly cron + PR a main; jobs gosec (SAST medium+ severity), govulncheck (vuln DB), npm audit production high+, Trivy container scan HIGH/CRITICAL failing + SARIF upload a Code Scanning. **Marca o fim do roadmap originalmente planejado** (S00-S29). Próximos passos são prod launch + monitoring real + backlog de follow-ups acima. |
| D043 | 2026-04-20 | Sprint S28 — CI/CD + Docker + Deploy | **Sem código de produto, apenas pipeline**. **.github/workflows/ci.yml** estendido: frontend (npm ci→tsc→eslint→prettier→vitest→vite build + upload dist/ artifact) + backend (Postgres service container + golang-migrate install + migrate up + go vet + go build + `go test -race -timeout 5m` com DATABASE_URL populado → integration tests rodam) + golangci-lint v1.60. **.github/workflows/release.yml**: triggered por tag `v*` em main; docker/build-push-action v6 builda distroless images de ambos surfaces, tagga com `:<version>` e `:latest`, push para ghcr.io via GITHUB_TOKEN (packages:write), softprops/action-gh-release emite draft release com generate_release_notes. Cache GHA scope=torque-api e torque-web. **.github/workflows/deploy.yml**: workflow_dispatch com inputs environment (staging/production) + image_tag + run_migrations flag. Concurrency group `deploy-<env>` sem cancel-in-progress. Jobs encadeados: migrate (version + up) → deploy (POST ao EASYPANEL_DEPLOY_WEBHOOK com {tag,env}) → smoke (5× polling 10s BASE_URL/healthz, fail após esgotar). GitHub Environments gateiam via required reviewers. Required secrets: DATABASE_URL, EASYPANEL_DEPLOY_WEBHOOK; required vars: BASE_URL. Rollback = re-dispatch com run_migrations=false. **torque-web/Dockerfile** novo: multi-stage node-20-alpine → nginx-unprivileged on 8080, SPA fallback, immutable hashed assets, security headers básicos (CSP nonce no edge). **torque-web/nginx.conf**: try_files fallback + gzip + X-Frame-Options=DENY. **docker-compose.prod.yml** de referência: read_only root fs + no-new-privileges + tmpfs + resource limits; Postgres é managed externamente. **.github/workflows/README.md** documenta pipeline + secrets + rollback. |
| D042 | 2026-04-20 | Sprint S27 — Integrations Foundation (adapters + resilience) | **Sem migration**. **Service integration/**: MessagingProvider (Evolution API + Meta), CalendarProvider (Google Calendar), ERPProvider (TinyERP) interfaces + Mock implementations. Common error taxonomy (ErrUnreachable/ErrRateLimited/ErrAuthFailed/ErrUnsupported/ErrCircuitOpen). CircuitBreaker 3-state (closed/open/half-open) com threshold + cooldown; Retry com exponential backoff (200ms→5s cap) que bail-out em ErrAuthFailed e ErrCircuitOpen. **Escopo parcial explícito**: live providers (Evolution/Meta/TinyERP/GCal) adiados — requerem credenciais + dual-review nos send paths. Mocks satisfazem F04/F02/F03 em dev hoje. **Tests** unit cobrem mock paths (happy + invalid), CircuitBreaker (threshold + cooldown + half-open recovery), Retry (convergência + auth bail-out). Não toca DB. |
| D041 | 2026-04-20 | Sprint S26 — F16 Master Admin (cross-org + impersonation audit) | **Sem migration** — audit_log já existe desde 0002 + users_master desde 0001. **Repositorio master/**: ListOrganizations (cross-tenant com LEFT JOIN agregados para member_count + lead_count), GetOrganization (não filtra soft-delete — master precisa visibilidade total), ImpersonationTarget (resolve oldest active admin do tenant; ErrNotFound se tenant sem admin), SystemHealthSnapshot (orgs/users/leads/subs/ops counts em single round-trip com sub-SELECTs COUNT FILTER). **Handler master/**: `/master/health` + `/master/organizations` + `/master/organizations/:id` + `/master/organizations/:id/impersonate`. Wiring em main.go abre subgrupo `mst` sob `RequireMaster` middleware (já existia desde S02). Invariante de segurança: impersonate endpoint chama `auditRepo.Append` ANTES de mint do token — se audit falha, 500 AUDIT_FAILED e NÃO emite session. Actor_type='master' + target_org_id populado para auditoria queryable bi-lateral. Token mint efetivo + cookie swap em follow-up (S26 entrega data plane + audit trail; cookie-mint path re-usa refresh-rotation de S02 mas com TTL curto e não-renovável). **Frontend** `useMaster.ts` hooks + `MasterPage` em `/master` com system health cards (4 métricas) + orgs table com "Entrar como admin" button. Non-master callers acessam a rota mas todas as queries retornam 403 via backend. **Tests** `master_test.go` gated DATABASE_URL: SystemHealth com ≥2 orgs, ListOrganizations cross-tenant + member_count correto, ImpersonationTarget resolve admin, org sem admin → ErrNotFound. Frontend typecheck+lint verdes. |
| D040 | 2026-04-20 | Sprint S25 — F15 Configurações (consolidação org + webhooks + notifications) | **Migration 0017**: ALTER organizations add timezone/cnpj/legal_name; CREATE webhook_endpoints (url CHECK ~* '^https://', secret CHECK 16..200, event_types text[] cap 100, unique (org,url), partial index org_active); CREATE notification_preferences (PK composite team_member_id+channel+topic, channel CHECK ∈ {email,in_app,push}); seed 5 permission keys organization.view/manage, webhooks.view/manage, notifications.manage. **Repositorio settings/**: GetOrganization/UpdateOrganization (slug imutável — master-only path controla re-slug futuro); ListWebhooks/GetWebhook/CreateWebhook (URL https-only ErrURLScheme, ErrURLTaken no unique)/UpdateWebhook (URL+secret imutáveis, rotação via delete+recreate)/DeleteWebhook idempotente; ListPreferences/SetPreference (ON CONFLICT upsert na PK composite, valida channel/topic). **Handler** splittado em 3: ReadHandler GET /organization + /webhooks member-accessible; AdminHandler PATCH /organization + POST/PATCH/DELETE /webhooks sob RequireRole; MeHandler GET/PUT /me/notifications scoped via sess.TeamMemberID. Wiring em main.go usa settingsRepo compartilhado entre Read/Me/Admin groups. **Frontend** `useOrgSettings.ts` com useOrganization/useUpdateOrganization + useWebhooks/useCreateWebhook/useUpdateWebhook/useDeleteWebhook + useNotificationPrefs/useSetNotificationPref. SettingsPage `OrgSection` reescrito: consome useOrganization, draft local com dirty-detection (disable save quando nada mudou), submit via useUpdateOrganization. **Escopo parcial**: UIs completos de webhooks tab e notifications tab ficam para sprint futura — hook layer + backend ready; TeamSection (S21) e ProductsPage (S21) continuam como surfaces canônicos. **Tests** `settings_test.go` gated por DATABASE_URL: UpdateOrganization, webhook https-scheme reject, create happy, ErrURLTaken, patch is_active, delete idempotente, notification pref upsert, invalid channel reject, cross-tenant GetWebhook refuses. Frontend typecheck+lint+vitest 99/99 verdes. |
| D039 | 2026-04-20 | Sprint S24 — F14 Billing Foundation (checkout + PIX + webhook) | **Escopo parcial explícito**: entrega o data plane + provider interface + MockProvider + webhook state machine. AsaasProvider real é deferred — requer credenciais + dual-review de money-flow que estão fora do escopo desta sessão. **Migration 0016** duas tabelas + dois ENUMs + 3 permission keys: `subscriptions` (uma live-sub por tenant via partial unique index sobre status ∈ {pending,active,past_due}; amount_cents com cap 1e14 igual proposals; PIX fields nullable populados pelo provider; current_period_start/end) + `billing_events` (append-only ledger, UNIQUE (provider, provider_event_id) → webhook replay idempotente, FK para subscription ON DELETE SET NULL preserva audit trail). Seeded: billing.view (member), billing.checkout (admin), billing.cancel (admin). **Service billing/**: interface `Provider{Name,CreateCharge,CancelCharge}` + MockProvider (gera PIX QR fake e charge_id determinístico para dev/test; recusa amount<=0). **Repositorio subscription/**: Create (captura ErrAlreadyActive do unique index), GetActive/Get, MarkPaid (pending|past_due → active, stamps current_period_end), MarkPastDue (active → past_due), Cancel (live → cancelled, idempotente via WHERE), RecordEvent (ErrDuplicateEvent sentinel), FindByProviderCharge. **Handler billing/** splittado em 3: `ReadHandler.GET /billing/subscription` (member-accessible; retorna `{data:null}` quando sem sub); `AdminHandler.POST /billing/checkout` + `/cancel` sob RequireRole(admin); `WebhookHandler.POST /webhooks/billing/webhook` PUBLIC fora de /api/v1 — autenticado via header `X-Torque-Billing-Secret` (constant-time compare; secret vazio = refuse all). Webhook state machine allowlist: charge.paid|payment.confirmed→MarkPaid + subscription.activated; charge.overdue|payment.failed→MarkPastDue + subscription.past_due; charge.cancelled→Cancel + subscription.cancelled. Duplicate event → 200 sem replay. **Config**: BILLING_PROVIDER (default "mock") + BILLING_WEBHOOK_SECRET. **Frontend** `useBilling.ts` com useSubscription (normaliza {data:null} → undefined), useStartCheckout, useCancelSubscription + WS invalidate em todos subscription.*. `CheckoutPage` em `/billing`: pending→exibe PIX QR code + expires_at; active→badge + current_period_end + cancel button; past_due→warning badge. **Tests** `subscription_test.go` (gated DATABASE_URL): Create + ErrAlreadyActive via unique index + RecordEvent dedup (ErrDuplicateEvent) + MarkPaid + Cancel idempotente + re-Create após cancel + FindByProviderCharge. `provider_test.go` (unit): CreateCharge happy + zero-amount refuse + Cancel no-op + Name()='mock'. Frontend typecheck+lint+vitest verdes. **Risco aberto**: webhook secret em .env em dev; prod exige rotacao + storage em secrets manager (Infra em sprint futura). |
| D038 | 2026-04-20 | Sprint S23 — F13 Onboarding Wizard + Gate | **Migration 0015** `onboarding_status` (1:1 com team_members via UNIQUE, current_step text CHECK 1..40, steps_completed text[] default ARRAY[], dismissed bool, completed_at timestamptz nullable, partial index idx_onboarding_org_pending filtrando completed_at IS NULL AND dismissed=false). **Repositorio onboarding/**: `CanonicalSteps` declarado em `onboarding.go` (welcome→organization_profile→invite_team→connect_whatsapp→create_first_lead→finish), EnsureForMember (ON CONFLICT upsert idempotente, seeda com `CanonicalSteps[0]`), Get, CompleteStep (rejects unknown step, no-op se já completo, advance() retorna próximo + flag done que stamps completed_at), Dismiss (refuse se já dismissed), Reset (volta para passo zero + wipe completed_at). **Handler** `/api/v1/onboarding` (GET always-ensure), `/onboarding/steps` (POST body={step}), `/onboarding/dismiss` (POST), `/onboarding/reset` (POST). Scoped ao caller via `sess.TeamMemberID` — user só lê/mutate próprio estado. Wired no subgrupo tenant-scoped member-accessible. **Frontend** hooks `useOnboarding/useCompleteStep/useDismissOnboarding/useResetOnboarding` + `shouldGate` predicate. `OnboardingPage` usa StepProgress primitive com copy PT-BR mapeado por step key; server autoritativo sobre quais steps existem — labels são presentational. `OnboardingGate` component wrapeia cockpit + AppShell; erros/loading não bloqueiam (UX não quer falhar por fetch transiente). Routes.tsx aninha OnboardingGate dentro de ProtectedRoute. **Tests** `onboarding_test.go` cobre Ensure idempotency, CompleteStep advance + duplicate-safe, unknown step rejection, all-steps → completed_at stamped, Reset wipe, Dismiss idempotency, cross-tenant Get refuses. Frontend typecheck+lint verdes, vitest 99/99. |
| D037 | 2026-04-20 | Sprint S22 — F12 Pipelines Customizados (admin lifecycle) | **Sem migration** — pipe_kind ENUM já inclui 'custom' desde 0002 e permission key `pipes.manage` (admin_only) também é antiga. **Repositorio pipe/admin.go**: CreatePipe (valida kind ∈ allowedKinds, 409 NAME_TAKEN via unique (org,name)), UpdatePipe (partial; kind imutável — callers arquivam e recriam), GetPipe (regardless of archive state), ArchivePipe (soft; entries congeladas em place para historico), CreateStage (ownership check pipe pertence ao tenant antes do insert, XOR is_final_positive/is_final_negative duplicado no repo + DB CHECK), UpdateStage (partial), DeleteStage (refuse ErrStageHasEntries se pipe_entries com left_at IS NULL; caller migra antes). **Handler admin.go** splittado: AdminHandler serve POST/PATCH/DELETE /pipes + /pipes/:id/stages sob RequireRole(admin), publicando eventos `pipe.{created,updated,archived}` e `pipe_stage.{created,updated,deleted}`. **Frontend** usePipes estendido com useCreatePipe/useUpdatePipe/useArchivePipe + useCreateStage/useUpdateStage/useDeleteStage; cada mutation invalida queryKeys.pipes.list()/stages(pipeId) apropriado. Admin UI (builder dialog) fica para sprint posterior — S22 entrega a camada de repo+handler+hooks; consumo visual vem em S25/S26 junto com Configurações. **Tests** `admin_test.go` gated por DATABASE_URL: happy path + ErrInvalidKind + ErrNameTaken + XOR guard + DeleteStage recusa com entry ativa e aceita após clear + Archive idempotente + cross-tenant Get refuses. Frontend typecheck+lint verdes. |
| D036 | 2026-04-20 | Sprint S21 — F10 Equipe + F11 Produtos | **Migration 0014** `products` (id, organization_id FK, name CHECK 2..160, description <=4000, sku opcional UNIQUE (org, sku) partial quando presente, price_cents bigint CHECK >=0, currency CHECK len=3 default BRL, is_active, metadata jsonb, created_by FK team_members, updated_at trigger). Seed de 4 permission keys: `members.view` (member), `members.manage` (admin), `products.view` (member), `products.manage` (admin). **F10 Equipe** — repo `member/`: List/Get, AddByEmail (lookup user por email; retorna ErrUserNotFound se não existe, ErrAlreadyMember em 23505), Update (partial patch com validação ErrInvalidRole), Deactivate (idempotente via WHERE is_active=true), ListOverrides/SetOverride/ClearOverride com tenant-guard em EXISTS antes de touch em member_feature_permissions. Handler splittado `members.NewRead` (GET /members, GET /members/:id, GET /members/:id/permissions sob tenant-scope member-accessible) + `members.NewAdmin` (POST/PATCH/DELETE /members + PUT/DELETE /members/:id/permissions/:featureKey sob RequireRole(admin)). Self-demote e self-deactivate refused com 403 SELF_DEMOTE/SELF_DEACTIVATE (prevenção de org-lockout pelo último admin). Eventos `member.{created,updated,deactivated,permission_changed}` no bus. **F11 Produtos** — repo `product/`: List cursor-paginated ADR-004 (encode (updated_at, id) base64url), Get, Create com normalize de currency (upper) + validações ErrInvalidName/Price/Currency, MaxPriceCents=1e14 (R$1T — defesa overflow int64 em totais de proposta), ErrSKUConflict via unique partial index. Update partial + Archive soft-delete (preserva refs históricas em proposals). Handler splittado `products.NewRead` (GET /products, GET /products/:id member-accessible) + `products.NewAdmin` (POST/PATCH/DELETE /products sob RequireRole(admin)). Eventos `product.{created,updated,archived}`. **Frontend** — hooks `useMembers` (list, detail, overrides, add/update/deactivate/setPermission/clearPermission) + `useProducts` (useInfiniteList + useProduct/Create/Update/Archive). SettingsPage aba Equipe agora consome useMembers (tabela com search, include-inactive toggle, invite form inline, role edit inline, deactivate); ProductsPage nova em `/products` com search/active-only/pagination e formatação Intl.NumberFormat BRL. **Wiring** em cmd/api/main.go: member/product Read montados no subgrupo tenant-scoped ao lado de leads/pipes/inbox; Admin wrappers dentro do RequireRole(admin) junto a agents/proposals/workflows/campaigns. **Tests** `member_test.go` (AddByEmail happy/duplicate/unknown, Update, overrides round-trip, cross-tenant guard, Deactivate idempotente) + `product_test.go` (Create happy/invalid/SKU conflict, Update partial, List cursor sem duplicata entre páginas, Archive idempotente, cross-tenant) gated por DATABASE_URL. Frontend vitest 99/99 em 35 arquivos. Runtime backend pendente no host. |
| D035 | 2026-04-20 | Pre-deploy staging audit + 5 typecheck fixes | **Ran localmente** typecheck + lint + vitest: auditoria pegou **5 categorias de blockers** que falhariam o build. **Fixes**: (1) `useInfiniteList` generics v5 (5-arg form `<TQueryFnData,TError,TData,TQueryKey,TPageParam>`) + conditional spread de `staleTime` sob `exactOptionalPropertyTypes`; (2) removido optimistic em `useMovePipeEntry` e `useSendMessage` (TCache≠TData no useAppMutation — WS broadcast patch covers, invalidate é fallback); (3) `useAppMutation` onMutate/onError/onSettled agora forwardam args extras da v5 (mutationContext, mutation); (4) `devSession.test.ts` — `vi.stubEnv('DEV', true/false)` boolean em vez de string (vitest 4 narrowing); (5) `Toaster.tsx` — conditional spread de `code`/`context` em vez de atribuir `undefined` (exactOptionalPropertyTypes). **Resultado**: 0 typecheck errors, 0 lint warnings, **94/94 vitest** passando em 33 files. **Audit report** em `.specs/audits/pre-deploy-staging-2026-04-20.md` com inventory (60 Go + 111 TS source, 18 Go + 33 TS test, 13 migrations/36+ tables, 86 endpoints, 24 permission keys, 0 debt markers no backend, 3 TODOs no frontend com owner), security re-verify (invariantes D029 mantidas), debt tracking (OpenAPI stale post-S04, 4 repos sem integration test, worker runtimes pendentes), e deploy checklist (env vars, dry-run script 7-step). **Veredict**: "Go" — frontend deploy-ready, backend precisa do dry-run no host. |
| D030 | 2026-04-19 | Sprint S16 — F06 Copilot Frontend hooks | `useAgents.ts` com useAgents/Agent/CreateAgent/Activate/Disable/SetKillSwitch/Delete + useOpenSession/EndSession/SessionMessages + useCreateCollection/EnqueueSource. Todos consomem endpoints admin-gated do S15; non-admin recebe 403 no pipeline de toast. WS invalidate em agent.{created,activated,disabled,kill_switch} + agent_session.*. Tests `useAgents.test.tsx` (list fetch + POST targets). |
| D029 | 2026-04-19 | Sprint Remediation — CRITICAL/HIGH fixes pos-auditoria S11-S15 | **Migration 0010** seed de 18 feature_permissions keys (proposals.{view,manage,accept,reject}, inbox.{view,reply,manage}, tasks.{view_own,view_team,create_for_self,create_for_others,complete,cancel}, copilot.{view,manage,kill_switch}, knowledge.{view,manage}) — catalog pronto para gating futuro mais fino. **Migration 0011** (a) `pipe_proposals` ganha sent_by_member_id/accepted_by_member_id/rejected_by_member_id (forensic trail em money flow); (b) `tasks_cancelled_requires_reason` CHECK relaxado para `tasks_cancelled_requires_timestamp` (reason opcional, timestamp obrigatorio). **RBAC wire-up** em cmd/api/main.go: handlers `/agents/*`/`/knowledge/*`/`/sessions/*`/`/proposals/*` isolados em subgrupo `admin.Use(RequireRole(domain.RoleAdmin))`; demais permanecem member-accessible. **Ownership checks pre-insert**: `inbox.AppendMessage` e `inbox.UpsertConversation` validam channel/conversation.organization_id, `agent.EnqueueSource` valida collection.organization_id, `task.Create` valida assigned_to via `assigneeInTenant` (SELECT EXISTS em team_members). Todos retornam `ErrNotFound` identico ao genuine-not-found — nao vaza cross-tenant existence. **URI allowlist** `allowedURISchemes={https:true}` em EnqueueSource (defense-in-depth anti-SSRF antes do ingest worker ligar). **Money cap** `proposal.MaxAmountCents = 1e14` previne int64 overflow. **DoS fix** `httpx.DecodeJSON` wrappa `http.MaxBytesReader(nil, r.Body, 1<<20)` + `IsBodyTooLarge(err)` helper; proposal reject handler mapeia para 413. **UTF-8** `inbox.messagePreview` troca byte-slice `b[:200]` por rune-slice `[]rune(b)[:200]`. **NULL bypass** `inbox.UpsertChannel` rejeita external_id nil/vazio. **task.Start** distingue 3 modes (ErrAssigneeBusy/ErrNotFound/ErrInvalidState) em vez de retornar success em task inexistente. **fmt shadow** `handler/proposals.toView` renomeia closure para `formatRFC3339`. **Actor columns wire** `MarkSent/MarkAccepted/MarkRejected` recebem `actorMember uuid.UUID` do `sess.TeamMemberID`. **Test novo** `httpx/httpx_test.go` cobre DecodeJSON happy path + unknown-field rejection + MaxBytesReader → IsBodyTooLarge. Frontend vitest 87/87 green. Backlog remanescente (nao bloqueante): per-endpoint feature gating fino (member view), agent session ownership check, OpenAPI yaml para S05-S15, proposal viewed tracking-pixel via signed-token. |
| D023 | 2026-04-19 | Audit S00-S10 + fix regressao em client.ts refresh-on-login | `client.ts` retry-on-401 disparava em QUALQUER 401, incluindo `/api/v1/auth/login` com senha errada. Consequencia: `INVALID_CREDENTIALS` era mascarado como `AUTH_EXPIRED` apos tentativa de refresh falhar (sem cookie valido), violando a UX de login e poluindo o state de auth. Detectado pelo proprio teste `useLogin.test.tsx` (84/85 passing, 1 falha). **Fix**: `client.ts` so aciona refresh se `path` NAO comecar com `/api/v1/auth/{login,refresh,logout}`. Post-fix: **85/85 testes passando**, coverage subiu de 62.83% → 67.35% linhas. **Relatorio completo** em `.specs/audits/quality-security-knowledge-2026-04-19.md` cobrindo inventario (46 Go + 104 TS fonte; 16 Go + 29 TS test; 6 migrations/20 tabelas; 22 endpoints; 8 ADRs; 22 decisoes), qualidade (coverage atual, TODOs por sprint owner, zero debito Go), seguranca (13 invariantes + 10 cenarios testados + 6 pendencias com owner + 5 riscos removidos por entrega), conhecimento (documentacao estruturada + rastreabilidade ADR→codigo verificada caminho-a-caminho de 13 pontos via pipe_entry.moved). |
| D022 | 2026-04-19 | Dev-auth bypass no frontend | `src/providers/devSession.ts` expõe `DEV_SESSION` (shape completo de `SessionBundle` espelhando o seed backend — user marcelo@gmail.com, org Torque Dev, role admin, TODAS as feature_permissions true para nenhum PermissionGate bloquear) + `isDevAuthEnabled()` que retorna true APENAS quando `import.meta.env.DEV === true` AND `VITE_DEV_AUTH` in {1,true,yes,on}. **AuthProvider**: no queryFn do useQuery, se devBypass → retorna DEV_SESSION sem tocar rede; caso contrario GET /api/v1/auth/me. O `if` e constant-folded no build de prod pois `import.meta.env.DEV` vira `false` — Vite strip o branch, DEV_SESSION nao chega ao bundle. `.env.development` commitado com `VITE_DEV_AUTH=1` (auto-carregado pelo Vite em dev). `vite-env.d.ts` atualizado com tipos de `ImportMetaEnv`. **Test**: `devSession.test.ts` cobre true-only-when-both-flags, false quando VITE_DEV_AUTH unset/0/false, false em build de prod mesmo com VITE_DEV_AUTH=1, e DEV_SESSION.featurePermissions all-true. Runtime (npm run dev) pendente no host. |
| D021 | 2026-04-19 | Dev seed: marcelo@gmail.com / admin | `torque-api/seeds/dev_admin.sql` cria Organization `torque-dev` + User `marcelo@gmail.com` (bcrypt cost 12 de "admin", hash `$2b$12$Gufjb5EHyosfJgpez.TpZufev2DvhOXbc9F0/UoQuDT2AhgX1YZK.`, verificado via bcryptjs) + TeamMember role=admin. Ids deterministicos (`00000000-...-a1/b1/c1`). Idempotente (ON CONFLICT DO NOTHING + DO UPDATE SET password_hash). Makefile target `seed-dev` (psql -v ON_ERROR_STOP=1 -f) com guarda `ENV != dev` → refuse. README secao "Dev seeds" com tabela de credenciais. **Sem migration** — seed e separate file, nao entra no flow prod. Hash e commit deliberado: credencial de dev, nao secret. |
| D020 | 2026-04-19 | Sprint S10 — F02 Pipe Confirmação | **Migration 0006** `pipe_confirmations` (1:1 com pipe_entries, meeting_at timestamptz, meeting_channel/notes, confirmed_at, no_show + no_show_reason, reminder_sent_at, index org+meeting_at, index parcial org_overdue para worker scheduler). **Repositorio confirmation** (Go): Upsert (ON CONFLICT pipe_entry_id DO UPDATE preservando created_at), Get com tenant scope, MarkConfirmed idempotente (WHERE confirmed_at IS NULL), MarkNoShow com reason opcional, Overdue (WHERE confirmed_at IS NULL AND no_show = false AND meeting_at < now, limit 50 default cap 200). **Handlers** `/api/v1/confirmations/:entryId` PUT/GET/POST confirm/POST no-show + `/confirmations/overdue` GET; eventos `confirmation.{upserted,confirmed,no_show}` publicados. Wiring em main.go. **Frontend**: `useConfirmations.ts` com hooks `useConfirmation` (GET detail), `useUpsertConfirmation`, `useConfirm`, `useMarkNoShow`, `useOverdueConfirmations` (staleTime 30s). **Client.ts** ganha helper `put<T>()` pois F02 usa PUT para upsert por pipe_entry_id. **Test**: `confirmation_test.go` integration gated por DATABASE_URL — upsert + update meeting time + confirm idempotente + no_show + cross-tenant isolation. |
| D019 | 2026-04-19 | Sprint S09 — F01 QA + LeadDetail + Polish | **LeadDetailPage** (`src/features/pipeline/LeadDetailPage.tsx`): real-data via `useLead(id)` + `<QueryBoundary>`, layout 2 colunas (info principal + metadados), rating como 5 estrelas coloridas, CSS tokens dark-first. **Toaster** (`src/components/Toaster.tsx`): listener global de `torque:toast` CustomEvent, FIFO com MAX_TOASTS=4, auto-dismiss 5s, role=alert em error + role=status nos demais, lucide icons por level, botao de fechar manual; monta uma vez no AppShell e recebe todo o output de `notifyAppError` sem acoplamento a biblioteca de toast. **Tests**: `Toaster.test.tsx` (dispatch → role=alert renderiza mensagem, auto-dismiss apos 5s via fake timers, cap do queue em 4 drop oldest). |
| D018 | 2026-04-19 | Sprint S08 — F01 Frontend: Leads + Pipes hooks + FunisHub | **Hooks**: `useLeads(filters)` — useInfiniteList sobre `/api/v1/leads` + useWSSubscribe em `lead.updated`/`lead.deleted` faz patch in-place no cache paginado (lead.created faz refetch na proxima janela); `useLead(id)` — detail query; `useCreateLead`/`useUpdateLead` (optimistic no detail)/`useDeleteLead` com invalidate apropriado. `usePipes` — lista com staleTime 5min; `usePipeStages(pipeId)` — staleTime 5min (pouco mutavel); `usePipeEntries(pipeId)` — useQuery + useWSSubscribe em `pipe_entry.moved` faz splice no cache; `useMovePipeEntry(pipeId)` — optimistic update (move card para destino instantaneamente) + invalidate como rede de seguranca. **Pagina**: `FunisHubPage` em `features/pipeline/FunisHubPage.tsx` lista pipes via `usePipes` + `<QueryBoundary>` (loading/empty/error), cada card linka para `/funil/:id` (Kanban real vira em S09). **Test**: `useLeads.test.tsx` stub fetch + verifica query-string com filtros e page_size. Nao toca backend — puro consumer dos endpoints S07. Runtime pendente. |
| D017 | 2026-04-19 | Sprint S07 — F01 Backend: Leads + Pipes + Stages | **Sem migration nova** (leads/pipes/pipe_stages/pipe_entries/lead_history existem em 0002). **Dominio** (`internal/domain/lead.go`): Lead/Pipe/PipeStage/PipeEntry. **Repositorio lead**: `List` cursor-paginado (base64url(rfc3339+uuid), tuple `(updated_at, id) < cursor`, limit<=100, filters responsible_id+search sobre name/email/phone), `Get`/`Create` (name obrigatorio + phone OR email obrigatorio, CustomFields jsonb)/`Update` partial patch/`SoftDelete` (deleted_at); TODAS as queries escoped por `organization_id AND deleted_at IS NULL`. **Repositorio pipe**: `ListPipes` (nao arquivados), `Stages` ordenado por position, `Entries` (left_at IS NULL = ativo), `Move` atomico em `TxOptions{IsoLevel: Serializable}` — valida stage pertence ao pipe (ErrStageMismatch → 409), fecha entry ativa (left_at = now()), insere nova. **Handlers**: `/api/v1/leads` GET list/POST/GET id/PATCH/DELETE com envelope `{data, meta:{next_cursor}}`, publica `lead.created`/`lead.updated`/`lead.deleted` no bus; `/api/v1/pipes` GET list/GET id/stages/GET id/entries/POST id/entries/move publica `pipe_entry.moved`. **Wiring**: `newRouter` agora recebe `*event.Bus` como parametro para handlers publicarem; ambos handlers montados no grupo tenant-scoped depois de Authenticator+RequireAuth+CSRF+TenantScope. **Tests**: `repository/lead/lead_test.go` unit test de encode/decode cursor (roundtrip + rejeita malformado/base64 invalido). Handlers + Move concurrency ficam para S09 QA. Runtime (go mod tidy+build+test) pendente. |
| D016 | 2026-04-19 | Sprint S06 — Integracao Front↔Back | **AuthProvider reescrito**: TanStack useQuery em `queryKeys.session.me()` busca `/api/v1/auth/me`, retry custom (401 nunca retenta), converte `MeResponse` OpenAPI → `SessionBundle` legacy via `toSessionBundle` (permissions array → Record<key,boolean>), mock MOCK_SESSION REMOVIDA (401 = null + navegar para /login, sem fallback silencioso), expõe `refresh()` que invoca refetch e retorna o SessionBundle, `logout()` POST /api/v1/auth/logout + queryClient.clear(). **WSProvider reescrito**: consome `useBootstrap` para ws_url, conecta via `TorqueWS.getInstance().connect(url)` quando `isAuthenticated && ws_url`, fecha em logout ou deautenticação, status publicado via onStatusChange listener. **Fix no client.ts**: `/auth/refresh` corrigido para `/api/v1/auth/refresh` (refresh cookie tem Path=/api/v1/auth no backend). **useLogin**: useAppMutation POST /api/v1/auth/login com `silent: true` (error fica disponivel em `login.error` para renderizar inline), invalida session.me + session.bootstrap em onSettled. **LoginPage reescrita**: consome useLogin + useAuth.refresh, botao desabilita em isPending + copy muda para "Entrando…", erro renderiza em div[role=alert] usando `friendlyMessage(login.error)`, apos mutateAsync+refresh navega conforme ui_mode (salesperson→/cockpit senao /). **Test**: `useLogin.test.tsx` (POST para URL correta + invalida queryKeys.session.me em success; AppError 401 exposto em mutation.error SEM disparar toast por ser silent). Nao toca backend. Runtime (`npm test`/typecheck/lint) pendente. |
| D015 | 2026-04-19 | Sprint S05 — Frontend CRUD Foundation | **src/api/queryKeys.ts**: factory canonico por dominio (session, operations, leads, pipes, tasks) com `all`/`list(filters?)`/`detail(id)` — elimina keys free-form nos call sites. **src/api/errors.ts**: `FRIENDLY_MESSAGES` mapeando codes canonicos (AUTH_EXPIRED, PERMISSION_DENIED, CSRF_*, RATE_LIMITED, NO_MEMBERSHIP, INVALID_CREDENTIALS, ...) para copy PT-BR; `friendlyMessage(err)` resolve AppError → curated/server-fallback; `notifyAppError(err, context?)` despacha CustomEvent `torque:toast` com `{level, code, status, message, context}` (desacoplado de qualquer toast lib); `isRetryable(err)` para 429/5xx. **src/hooks/useAppMutation.ts**: wrapper de useMutation com `invalidate: QueryKey[]` (loop em onSettled), `optimistic: {queryKey, updater}` (snapshot + cancelQueries + setQueryData + rollback em onError), `silent` flag (preserva mutation.error para inline mas suprime toast), `errorContext` string que vai no evento de toast. **src/hooks/useInfiniteList.ts**: wrapper de useInfiniteQuery ADR-004 — pageParam = cursor string, query emite `?cursor=...&page_size=...&<filters>`, flatten de `data.pages` → `items: T[]`, expõe `fetchNextPage/hasNextPage/isFetchingNextPage`; NUNCA offset. **src/hooks/useWSSubscribe.ts**: filtro type-safe (string ou array) sobre TorqueWS.onMessage singleton; valida shape de `TorqueEvent` (type + tenant_id obrigatorios) e dropa payloads malformados; retorna unsub na cleanup do effect. **src/hooks/useOperation.ts**: (a) `useOperationSubmit` → POST /api/v1/operations, invalida `queryKeys.operations.all()`; (b) `useOperationStatus(id)` → GET /api/v1/operations/:id com refetchInterval=2s quando nao-terminal / false quando terminal, + useWSSubscribe em `operation.updated/.succeeded/.failed` que faz setQueryData no cache (merge patch sobre previous) — WS e primary, poll e fallback; (c) `useOperationCancel(id)` → DELETE com invalidacao do detail. **src/hooks/useBootstrap.ts**: GET /api/bootstrap com staleTime/gcTime: Infinity, retry: 2 — deploy invalida via cache-bust do bundle. **src/components/QueryBoundary.tsx**: renderizador unificado loading→skeleton/error→EmptyState+Button.ghost(Tentar de novo)/empty→EmptyState; aceita `isEmpty: (d) => boolean` + fallbacks customizaveis; chama `friendlyMessage` no error. **Tests**: `api/__tests__/errors.test.ts` (friendlyMessage cobrindo known/unknown/Error/null + isRetryable em 429/5xx/4xx/non-Error + notifyAppError despachando evento com detail.code/status/context), `api/__tests__/queryKeys.test.ts` (root keys estaveis, list com/sem filtros, detail por id, pipe.entries nesting, list vs detail nao colidem), `hooks/__tests__/useWSSubscribe.test.tsx` (dispatch mockado via spyOn TorqueWS.prototype.onMessage — match single/array/malformed + unsub on unmount), `hooks/__tests__/useInfiniteList.test.tsx` (fetch stubbed — first page sem cursor, second page com cursor do server, stop quando next_cursor=null desabilita o botao), `hooks/__tests__/useAppMutation.test.tsx` (toast em AppError, silent=true suprime, invalidate chama queryClient.invalidateQueries, optimistic aplica patch e faz rollback em erro). Nao toca backend — sprint puramente frontend. Runtime local (`npm test`, `npm run typecheck`, `npm run lint`) pendente no host. |
| D014 | 2026-04-19 | Sprint S04 — OpenAPI + WebSocket Hub + Async Jobs | **Migration 0005** `operations` (com ENUM `operation_status` pending/running/succeeded/failed/cancelled, organization_id FK, actor_user_id/actor_type, kind CHECK 1-80 chars, input/result/error_payload como jsonb, progress numeric(5,4) nullable CHECK [0,1], retry_remaining int CHECK>=0, worker_id text, scheduled_at/started_at/ended_at/expires_at, partial indexes `kind_pending` para worker polling + `org_status` para UI list + `org_created` para audit). **Dominio** (`internal/domain/operation.go`): tipo `OperationStatus` com `IsTerminal()` + `Operation` struct. **Repositorio** (`internal/repository/operation/operation.go`): `Submit` (pending + retorna id), `Lookup` (scoped por org, ErrNotFound cross-tenant), `ClaimOne` (CTE + `FOR UPDATE SKIP LOCKED` para multi-worker seguro, transiciona para running atomicamente e stamps worker_id+started_at), `UpdateProgress` (float [0,1] so em running), `Succeed` (result jsonb + progress=1 + ended_at), `Fail` (atomico: se retryable && budget>0 decrementa e volta a pending; senao marca failed), `Cancel` (tenant-initiated, so em pending/running). **Event bus** (`internal/event/bus.go`): fan-out in-process sobre canais Go, subscribe(buf) retorna (chan, unsub), DropOldest evicta a mais velha quando buffer cheio (UI favorece freshness), DropNewest preserva backlog, SubscriberCount para debug. Swap para Redis Pub/Sub / NATS quando horizontal. **WS** (`internal/ws/`): `event.go` com shape canonico `{type, tenant_id, entity_type, entity_id, version, patch, occurred_at}` + constantes `TypeOperation{Updated,Succeeded,Failed}` + `OperationPatch` reusada; `hub.go` com `Hub{byOrg map[uuid]map[uuid]*Conn}` + Register retorna Conn com buffer configuravel, Unregister idempotente, Broadcast `O(conns-per-tenant)` nao-bloqueante com DropOldest por conn, Writer goroutine envia JSON + `Ping` a cada PingInterval + mata conn quando silent beyond PongTimeout, Reader drena frames atualizando lastSeen; `handler.go` aceita upgrade via `coder/websocket` com subprotocol `torque.v1`, origin patterns de CORSOrigins, valida session via `mw.SessionFrom`, registra conn no hub, roda Reader+Writer em goroutines e cancela ctx na primeira falha. **Worker pool** (`internal/worker/pool.go`): `HandlerFunc(ctx, op, progress)->(result, err)` + `Handler{Kind, Fn, Retryable}`; `Pool` com semaforo de Concurrency, dispatcher loop reserva slot->ClaimOne->goroutine->run; run invoca Fn com progress closure que escreve em DB + publica `operation.updated` no bus; sucesso publica `operation.succeeded`, falha publica `operation.failed` (ou `operation.updated` se retry), classifyError categoriza context.Canceled/DeadlineExceeded; Shutdown espera wg.Wait() com timeout; BackoffFor exponencial capado em 5min. **Handlers HTTP** (`internal/handler/operations/operations.go`): POST `/api/v1/operations` (202 Accepted + `Location: /api/v1/operations/{id}`), GET retorna view com status/progress/result/error/timestamps + Cache-Control `no-store` em running/`max-age=60` em terminal, DELETE cancel com 204 ou 409 se terminal. **OpenAPI** (`internal/handler/openapi/openapi.go`): le `api/openapi.yaml` no boot, serve `/openapi.yaml` como-esta e `/openapi.json` convertido (coerceKeys para map[string]any pois yaml.v3 produz map[any]any) com `Cache-Control public max-age=300`. **Wiring** (`cmd/api/main.go`): bus instanciado, hub instanciado, goroutine subscreve o bus e repassa cada evento a `hub.Broadcast`, workerPool criado com `operationrepo.New(pool)` e iniciado com Start(ctx); `newRouter` agora retorna `(http.Handler, error)` pois openapi pode falhar no boot; `/openapi.{yaml,json}` publicos, `/api/v1/operations` dentro do grupo tenant-scoped, `/api/v1/ws` dentro do grupo autenticado (NAO tenant-scoped — handler le org direto da session). **OpenAPI spec** expandida (83→260 linhas): probes + public + auth (login/refresh/logout/me) + me (preferences) + operations (submit/poll/cancel) + ws + schemas (Health, Bootstrap, Error, LoginRequest/Response, Me, User, Organization, Permission, OperationSubmit, Operation). **Deps**: `github.com/coder/websocket v1.8.12`, `gopkg.in/yaml.v3 v3.0.1`. **Tests**: `event/bus_test.go` (round-trip, fan-out, subscriber count, DropOldest, concurrent publishers), `ws/hub_test.go` (register+broadcast nao bloqueia, tenant isolation, missing tenant dropped, unregister-twice safe), `repository/operation/operation_integration_test.go` (gated por DATABASE_URL: submit→lookup→claim→progress→succeed roundtrip, SKIP LOCKED claim fair entre 3 rows, fail retryable chain 2→1→0 terminal, cancel idempotente, tenant isolation no Lookup). Worker pool end-to-end test e handler test ficam para proxima iteracao (S05 hooks genericos + integration tests). Runtime local (go mod tidy/build/migrate up/go test -race) pendente. |
| D013 | 2026-04-19 | Sprint S03 — Security headers + Observabilidade + Audit + Rate limiting | **Security headers** (`internal/httpx/middleware/security_headers.go` reescrito): `SecurityHeadersWith(cfg)` parametrizado + legacy `SecurityHeaders` preservado. Defaults: CSP tight (`default-src 'none'; frame-ancestors 'none'; base-uri 'none'; form-action 'none'`, sem nonce pois API e pure-JSON — nonce fica para o nginx servindo o HTML), HSTS 2 anos com includeSubDomains + preload (ativado apenas fora de dev), X-Frame-Options DENY, X-Content-Type-Options nosniff, Referrer-Policy strict-origin-when-cross-origin, Permissions-Policy negando camera/mic/geo/payment/etc, COOP same-origin, CORP same-site. **Observabilidade** (`internal/observability/sentry/sentry.go`): wrapper do SDK Sentry Go com `Init` idempotente (empty DSN = no-op, nao falha em dev), `Recovery(logger)` middleware que captura panics + reporta + loga + 500, `scrubPII` como `BeforeSend` redigindo cookies inteiros + headers sensiveis (Authorization/Cookie/X-CSRF-Token/email/password/api_key) + query string (substitui `key=VAL` por `key=[Scrubbed]`) + request body (dropa completamente) + User.Email + trunca IPv4 a /24 e IPv6 a /48 (retem grouping sem re-identificacao individual), fallback para stacktrace preservada. **Audit log** (existe no schema desde migration 0002; servico novo em `internal/service/audit/` + repo em `internal/repository/audit/`): Entry/Append/Record/RecordSystem/RecordImpersonation; actor_type derivado da Session (master → 'master', admin → 'admin', membro → 'membro', sem sessao → 'system'); Action constantes canonicas (`auth.login`, `auth.logout`, `auth.refresh`, `auth.refresh_reuse_detected`, `permission.denied`, `master.impersonation_start/end`, `user.preference_updated`); payload jsonb por default '{}'; impersonation grava target_org_id so a tenant queries find dos dois lados; append-only, sem update/delete exposto. **Rate limiting** (`internal/httpx/middleware/ratelimit.go`): token bucket in-memory via `golang.org/x/time/rate`, bucket key = `u:<user_id>` se Session presente senao `ip:<client_ip>`; defaults 5 rps/10 burst anon e 30 rps/60 burst autenticado; janitor goroutine evicta buckets idle; 429 com `Retry-After` calculado via `Reserve().Delay()` (consumo cancelado); suporta `TrustForwardedFor` para quando vier atras de nginx/EasyPanel. **Bootstrap enriquecido** (`internal/handler/bootstrap/bootstrap.go`): `ServerTime` ISO-8601 UTC (client detecta clock drift), `SentryDSN` agora vira DO PUBLIC DSN (campo de config separado — SentryDSN/server nunca chega ao browser), `FeatureFlags` lido via env `FEATURE_FLAGS="flag=true,outra=false"`, `Cache-Control: public, max-age=60, stale-while-revalidate=600`. **Wiring** (`cmd/api/main.go`): `sentry.Init` roda antes do pool para capturar panics de boot, `defer sentry.Flush(2s)`, `mw.Recover` substituido por `sentry.Recovery(logger)`, `SecurityHeadersWith` toggla HSTS por ENV, `RateLimiter` instanciado uma vez e aplicado dentro de `/api/v1` depois do `Authenticator` (usa session key quando logado). **Config novo**: `SentryPublicDSN`, `SentryEnvironment`, `SentrySampleRate`, `SentryTracesRate`, `RateLimit*`, `FeatureFlags`. **Deps**: `github.com/getsentry/sentry-go v0.29.0`, `golang.org/x/time v0.7.0`. **Tests**: `security_headers_test.go` (todos headers presentes nos defaults, HSTS off no dev, preload shape quando ativo), `ratelimit_test.go` (burst-then-429, buckets distintos por IP, X-Forwarded-For respeitado com TrustProxy), `sentry_test.go` (scrubPII em cookies/headers/query/body/email/IPv4/IPv6, nil event safe, isSensitive allowlist), `audit_integration_test.go` (gated por DATABASE_URL: record com actor_type derivado + request_id + payload jsonb; impersonation com target_org_id e actor_type=master). Nota: nenhuma migration nova em S03 — `audit_log` ja existe em 0002. Runtime local (go mod tidy/build/test -race) continua pendente no host do usuario. |

## Current Blockers
- S01+S02+S03+S04 precisam rodar local (go mod tidy → go build → migrate up → go test -race com DATABASE_URL) para validar runtime. Go/Docker/migrate nao estao instalados no workspace do Conductor; execucao pertence ao usuario.
- Worker pool ainda nao tem handlers registrados para kinds de producao (leads.import, workflow.execute, etc) — features ligam conforme entregam (S07+).
- Event bus e WS hub sao in-process (single-node). Multi-replica exige Redis Pub/Sub atras da mesma API Publish/Subscribe.
- Frontend ainda consome fixtures mock ate S05 (hooks genericos) + S06 (integracao front↔back) plugarem em `/api/v1/*` e `/api/v1/ws`.
- CSP nonce-based para HTML continua no edge (nginx). HSTS preload-list submission requer dominio prod estavel antes.

## Lessons Learned
- Documentacao do vault pode ficar significativamente desatualizada vs codigo
- Sempre verificar estado real do codigo antes de confiar em status documentados
- Duas taxonomias paralelas no vault causam confusao — cada numero deve ter UMA pasta
- Mudanca transversal que afeta AppShell/roteamento/preferencia de usuario e Sistema Base, nao feature vertical — o numero F17 e preservado apenas como apontador de backlog

## Preferences
- CTO padrao: world-class, dark-first, Go backend, seguranca desde commit 1
- Vault e fonte de verdade para decisoes e specs
- Agentes usados proativamente, com briefings densos e autossuficientes
- Uma feature por vez (regra cardinal) — extensoes de Sistema Base precedem features verticais
